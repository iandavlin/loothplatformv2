# LIVE-GIT-DRIFT-TRIAGE.md — /var/www/dev non-core file audit

**Read-only audit, 2026-06-23.** Inventory of loose NON-WordPress files in `/var/www/dev`
(dev2) classified against the monorepo per [REPO-MANDATE.md](REPO-MANDATE.md). **Nothing was
moved or deleted.** Repo-additions and the 6 mu-plugin drift reconciles go through the keeper
merge gate, per-file.

Classes (REPO-MANDATE): **(a)** source/config that should live in repo · **(b)** box-local
runtime/secret/vendor (keep; needs provisioning recipe + pointer) · **(c)** stale/.bak/cruft
(propose cull).

## Summary

- **Docroot overlay (30 JS/PHP/CSS in `webroot/`):** all tracked and **byte-identical** to repo.
  The JS deploy path is reproducible — no action.
- **mu-plugins:** 11 byte-identical · **6 genuinely drifted** (reconcile) · 3 already
  served-from-git via symlink · 2 vendor (BuddyBoss/Burst) · 2 custom not-in-repo
  (temp killswitch + a renamed-in-repo bridge).
- **~10 docroot orphans** + **~50 `.bak-*`** files — box-local runtime or cull.
- The drift is **confined to mu-plugins** — exactly the "COPIES that drift" gap.

## mu-plugins (`/var/www/dev/wp-content/mu-plugins/`)

| File | What it is | In repo? | Class | Recommendation |
|---|---|---|---|---|
| archive-poc-sync.php | Archive CPT sync glue | yes `platform/mu-plugins/` | **DRIFT** live ahead (+334B/23ln, 6/13) | diff → pull live into repo |
| bb-mirror-sync.php | BB forum→mirror sync | yes (also `bb-mirror/deploy/`) | **DRIFT** 25ln, live newer 6/17 but smaller | reconcile both copies |
| lg-article-materializer.php | Managed-CPT renderer | yes | **DRIFT** repo ahead, 13ln | resolve; repo likely canonical |
| lg-membership-chrome.php | Membership header chrome | yes | **DRIFT** 16ln | hand-merge |
| loothdev-sheets-bridge.php | Sheet→WP bridge | yes (repo newer 6/23) | **DRIFT** 15ln, repo ahead | deploy repo→box |
| profile-auth.php | Profile JWT/auth shim | yes (live newer 6/17) | **DRIFT** 18ln, live ahead | pull live into repo |
| bb-forum-author-delete.php | Forum author cleanup | yes | (a) SAME | none |
| lg-admin-tools.php | Admin tools | yes | (a) SAME | none |
| lg-comments-frame.php | Comments modal frame | yes | (a) SAME | none |
| lg-error-pages.php | Custom error pages | yes | (a) SAME | none |
| lg-event-reminders.php | Event reminder hooks | yes | (a) SAME | none |
| lg-viewer-tier.php | Viewer tier resolver | yes | (a) SAME | none |
| lgms-admin-view-as-toggle.php | "View as" toggle | yes | (a) SAME | none |
| lgpo-set-password.php | Poller set-password | yes | (a) SAME | none |
| looth-auth-issue.php | Auth cookie issuer | yes | (a) SAME | none |
| profile-sync.php | Profile→PG sync | yes | (a) SAME | none |
| profile-whoami-shim.php | /whoami shim | yes | (a) SAME | none |
| loothdev-sheets-bridge.apps-script.gs.txt | Apps Script source | yes | (a) SAME | none |
| lg-bug-report.php | Bug report | symlink→repo | (a) served-from-git | none (model to copy) |
| lg-secrets-dash.php | Secrets dashboard | symlink→repo | (a) served-from-git | none |
| lg-siteurl-from-env.php | siteurl from env | symlink→repo | (a) served-from-git | none |
| lg-weekly-email-bridge.php | Weekly email HTML loopback | yes, **renamed** → `events/lg-weekly-email-bridge.mu-plugin.php` | (a) name mismatch | reconcile filename; symlink like the others |
| lg-poller-mail-killswitch.php | TEMP: suppress all poller mail | **NO** | (b) box-local temp toggle | fold into poller as env-gated knob + env.template, or document w/ removal condition |
| buddyboss-performance-api.php | BuddyBoss vendor MU-plugin | NO | (b) vendor runtime | keep; shipped by BuddyBoss plugin; note pointer, don't pull |
| burst_rest_api_optimizer.php | Burst Statistics vendor | NO | (b) vendor runtime | keep; auto-generated by Burst plugin |
| lg-user-audit.php.removed-20260609 | Disabled audit plugin | n/a | (c) cruft | cull |
| *.bak-* / *.pre-* (archive-poc-sync, profile-auth, profile-sync) | timestamped backups | n/a | (c) cruft | cull |

## docroot loose files (`/var/www/dev/`)

| File | What it is | In repo? | Class | Recommendation |
|---|---|---|---|---|
| 30× overlay JS/PHP/CSS (hub-polish.js, bottom-nav.js, pwa.js, sponsor-*, profile-sheet.js, …) | FE overlay bundle | yes `webroot/`, **all byte-identical** | (a) tracked & clean | none — deploy path healthy |
| img.php | BB image proxy | yes `bb-mirror/web/` | (a) | none |
| weekly-patreon-paste.html | Paste tool | yes `tools/` | (a) | none |
| manifest.json | PWA manifest | yes `webroot/` (identical) | (a) | none |
| CLAUDE.md | Box agent instructions | distinct from repo CLAUDE.md | (b) box-local | keep; not the repo file |
| cdp_tab.py | CDP tab helper script | NO | (b) box tooling | propose → `tools/`, or keep box-local w/ pointer |
| looth-app.apk (2.4 MB) | Built Android APK | NO | (b) build artifact | keep out of git; document build, store in releases not docroot |
| shop-feed.json / shop-vendors.json | Shop-bubble data feeds (Buck shop surface) | NO | (b) runtime data | needs generator recipe + pointer; note live `shop-bubble.js` exists only as `.bak` at docroot |
| guitardle-icon.png | Guitardle icon (PNG) | NO (512 **webp** is in `archive-poc/web/guitardle/assets/`) | (a)/(c) | pull beside webp, or cull if webp supersedes |
| dev-environment.md / memdir-handoff.md | Box notes / handoff dump | NO | (c) | move to docs/ or cull |
| pwa-test.html / tier-mockup.html | Scratch test + mockup | NO | (c) cruft | cull |
| index.nginx-debian.html | Default nginx landing page | NO | (c) core cruft | cull |
| ~50× *.bak-* (hub-polish×12, pwa×11, wp-config×2, sponsor/shop/bottom-nav…) | timestamped overlay backups | n/a | (c) cruft | cull |

## Notes

- **Drift goes both directions** — per-file decisions required at the gate:
  - **repo ahead** (deploy repo→box): `lg-article-materializer`, `loothdev-sheets-bridge`
  - **live ahead** (pull box→repo): `archive-poc-sync`, `profile-auth`
  - **divergent** (hand-merge): `bb-mirror-sync`, `lg-membership-chrome`
- **Out of scope, flagged:** docroot subdirs NOT in repo — `catapult-game*`/`catapult-online*`
  (~5 game dirs), `cdp-launcher`, `shop`, `shop-img`. Separate triage; left untouched.
- Method: name-match + content-grep + md5 vs `~/loothplatformv2/{webroot,platform/mu-plugins}`.
  Content-grep caught `lg-weekly-email-bridge.php` which name-match missed (renamed in repo).
