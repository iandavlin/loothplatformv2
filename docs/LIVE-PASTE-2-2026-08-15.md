# Live paste 2 — 2026-08-15 evening (28 code commits past d12b18f)

**Everything here is Ian's to run, in this order, on the live box.**
The headline: **nothing member-visible changes.** Every feature in this span
arrives switched off, empty-whitelisted, or admin-only — verified by a
value-change audit of the whole span (the only tracked flag that had flipped,
compose, was moved to a dev2-local switch before this paste was assembled;
same for Guitardle server-play).

**Arrives inert:** Stripe menu+page whitelist (empty list = invisible to
everyone) · monthly/yearly price controls (admin dash only, unset) ·
compose (tracked OFF; dev2 runs it via a box-local switch) · Guitardle
server-play + day-puzzle (OFF, two-stage rule) · notification quick-reply
(OFF) · new-member sign-up step (OFF) · weekly Guitardle front (OFF) ·
board page updates (dev-gated; live serves no dev gate).

## 0. Check first (safe to run any time — read-only)

    sudo -u ubuntu git -C /home/ubuntu/loothplatformv2-clean fetch origin
    sudo -u ubuntu git -C /home/ubuntu/loothplatformv2-clean rev-list --count HEAD..origin/main

`0` means live is already current (this paste has been run — stop).
Any bigger number is how many commits are waiting — proceed.

## 1. The pull

    lg-deploy

No database steps, no new plugin links, no nginx changes this time — the
span was audited for all three couplings and carries none.

## 2. Smoke, on the box (never a plain public curl — Cloudflare 403s it)

    curl -sk --resolve loothgroup.com:443:127.0.0.1 https://loothgroup.com/ -o /dev/null -w '%{http_code}\n'
    curl -sk --resolve loothgroup.com:443:127.0.0.1 https://loothgroup.com/ | grep -ci stripe
    grep -c "SERVER_PLAY', false" /home/ubuntu/loothplatformv2-clean/archive-poc/api/v0/_flags.php

Expected: `200`, then `0` (no Stripe traces for a visitor), then `1`
(cheat-proofing confirmed off on live).

## Nothing to roll back

This paste changes no member-visible behaviour, so there is no rollback
section. If anything looks off afterwards, tell keeper what you saw.

## The switches this paste deliberately does NOT flip (each is its own line, later, when Ian says the word)

- **Compose for live members** — tracked ON reaches the ~1,820 members who
  can already edit and upload. Keeper commits the flip with Ian's quote; it
  rides the next paste as its own labeled line.
- **Guitardle cheat-proofing (server-play, then day-puzzle)** — after Ian's
  dev2 look. Two-stage: the answer-key files can only be restricted once both
  are ON everywhere.
- **Stripe live testers** — Ian recruits real members by name; keeper stages
  a one-liner per name.
