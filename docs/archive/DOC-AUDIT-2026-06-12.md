# Documentation audit — 2026-06-12 (pre-deploy, Ian's ask)

Scope: every .md in the projects tree (127 active since 6/1), checked for
claims that contradict current rulings or would mislead the cut.

## HEADLINE: there were TWO cutover plans. Now there is one.

- `cutover/cut-day-runbook.md` — the live-deploy lane's MASTER runbook,
  blue-green model (new self-contained box, ADOPT live's DBs, flip
  traffic, old box untouched as fallback), Postgres REBUILT from adopted
  live WP (Ian leaning, 6/9). Batches 01–06 + LIVE-INVENTORY behind it.
- `docs/LIVE-DEPLOY-PLAN.md` — written 6/12 by the visibility session
  WITHOUT knowing the above existed; assumed in-place on old live and
  carry-dev-PG. Ian's 6/12 remarks ("copy over current wordpress state",
  "drop a backup of live", "leave old site hooked up") were describing
  the BLUE-GREEN model all along; the 6/12 session misread them.

**Resolution v2 (6/12 pm, after Ian's decision round): `docs/LIVE-DEPLOY-PLAN.md`
is CANONICAL after all** — Ian confirmed ip-172-31-45-223 IS old live (no
second box) and ruled in-place/one-DB + PG rebuild. cut-day-runbook keeps a
superseded-model banner; its inventory/batches stay reference. Original
resolution below kept for the audit trail:** The
6/12 plan's net-new material (visibility matrix as the test gate, F6
fresh-secrets framing, person-vis timer, hardcoded-host fixes, disk/R2
verification, WP-side delta list, runbook owner-tags) is appended there as
a dated addendum; LIVE-DEPLOY-PLAN.md now carries a superseded banner and
survives as the 6/12 audit record. PG REBUILD (not carry) stands —
today's refactor made it SAFER (rulings now live in code + column
defaults, not data passes); known re-applies after rebuild: 2 finder
opt-ins, karriker location fix (re-run bin/fix-divergent-locations.php),
matrix QA fixture. AWAITING IAN: confirm (a) ip-172-31-45-223 = the new
box, (b) rebuild confirmed post-refactor.

## Stale-claim findings (grep sweep, June docs)

| Doc | Issue | Action |
|---|---|---|
| docs/handoff-to-map-chat-directory.md (6/1) | public-precision city default (now 'private', c8977cd) | historical handoff — leave, superseded by memory + matrix |
| docs/briefing-bb-mirror-whoami-fastpath.md, briefing-membership-pages.md, briefing-stripe-poller.md | reference the WP whoami shim as the path (fast-path repoint happened) | consumed briefings — leave; shim retirement is a runbook line |
| docs/briefing-anon-posting-rebuild.md, briefing-cutover-refocus.md | pre-6/7 absence-gating language | consumed — leave |
| docs/lg-layout-schema.md | BROKEN symlink → write-article/SKILL.md (skill renamed write-article-v2) | FIXED this audit |
| profile-app/CUTOVER-CHECKLIST.md | slice-3 era; data steps still valid, framing partly superseded | banner added pointing at canonical runbook |
| docs/SESSION-HANDOFF-visibility-refactor.md | current (6/12 pm) ✓ | none |
| memory project_guitardle_block says DECOMMISSIONED; front-page lane LAUNCHED it 6/12 (4b286fc) | memory stale, not docs | flagged to Ian here; memory updated when he confirms launch is final |

Briefings/relays under docs/ are point-in-time messages by design — they
are NOT corrected retroactively; the authoritative set for the cut is:

1. cutover/cut-day-runbook.md (+ its addendum)  ← THE runbook
2. cutover/LIVE-INVENTORY.md, BATCH-* results
3. profile-app/CUTOVER-CHECKLIST.md (slice-3 data detail)
4. docs/OWNERSHIP-CUTOVER-AUDIT.md
5. docs/STRANGLER-COORDINATION.md (contracts)
6. docs/SESSION-HANDOFF-visibility-refactor.md (visibility state + matrix)
7. docs/LIVE-DEPLOY-PLAN.md (6/12 audit record, superseded banner)
