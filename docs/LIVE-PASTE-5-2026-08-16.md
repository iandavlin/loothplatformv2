# Live paste 5 — 2026-08-16 late

**What members get:** the profile author-archive icon, ACTIVE on arrival
(Ian verified on his own dev2 profile: "It looks good") — one more circle in
the profile icon row, "Posts in the Hub", linking to the author-filtered hub;
hidden entirely for members with no authored discussions or CPTs (the vis-0
rule, decided by the destination's own counter, gate 65 both directions).
Plus two small unflagged wants: the front-rail video-card onerror fallback
(the Fios-night find — a YouTube outage can't render beige blanks again) and
the guitardle rank-number dark ink.

**Dormant cargo:** compose form rulings + chrome (compose flag OFF on live),
comma-name fix (flag OFF pending its own serve-look), invites (test-group
scope, live arming is a separate later step), all board tooling.

**Couplings:** none new (A-status diff clean across the delta).

## Ian runs, on live:

```
lg-deploy
```

## Then verify (30 seconds, when your line is back):

Open your own live profile → the icon row → hover the new circle ("Posts in
the Hub") → it opens the hub filtered to you. A member with no posts shows no
icon.
