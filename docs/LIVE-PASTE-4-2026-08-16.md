# Live paste 4 — 2026-08-16 evening

**What members get:** the hub author-banner fix (arrives ACTIVE — Ian verified
on dev2, tracked default flipped ON at ec3dfcd) and both guitardle fixes (the
shared-device owner stamp + the duplicate-phrase retext). Everything else
aboard is flag-OFF cargo (weekly-front, welcome-activation) or internal
tooling — dormant on arrival.

**Evidence set:** post-deploy suite 63 gates green on dev2 at the shipped
content; the banner gate green standalone (7/7 through the real dropdown) plus
Ian's own click-verify; guitardle gate 59 at 71 assertions incl. phase 7
red-first, Ian verified report 1 by playing. Known non-greens, none aboard
this delta's member-facing surface: work-board threads/relay assertion
(internal board, pre-existing, stripe seat queued), and four no-verdict gates
attributed to dev2 lane-preview churn, re-run pending.

**Couplings:** none. No new mu-plugins, no new webroot files across the whole
delta (verified by A-status diff) — a plain pull suffices. lg-deploy may print
the lg-wp-cron unit-drift WARN again: that is backlog 33, unruled, ignore it
for this paste.

## Ian runs, on live:

```
lg-deploy
```

## Then verify (either or both):

1. Hub banner, ~30 seconds: open the Hub → Adv Search → type an author, pick
   from the dropdown. The green author banner should appear immediately, in
   place. (The comma-name members — e.g. John Lehmann — are a SEPARATE known
   bug, fix in progress; do not judge this deploy on them.)
2. Guitardle, whenever convenient: you and Mikelle on the same device, each
   account sees only its own board/stats/daily allowance.

If the banner does not appear in-place on live after the pull: tell keeper
before anything else — do not re-pull.
