# ⛔ FROZEN — legacy docs (2026-06-19)

The single forward-looking source of truth is **`docs/atlas/`**:
- `SYSTEM-MAP.md` — how the system actually works (audited live from dev2).
- `GIT-PROTOCOL.md` — how we use git.

**Everything else in this `docs/` tree — and the legacy `~/projects/docs` tree — is FROZEN.**
- Emergency reference ONLY. Do **not** build on it, cite it as current, or update it.
- It is the accumulated saga (briefings, handoffs, audits, plans), kept for history/recovery.
- If anything here contradicts `docs/atlas/` **or the running box, atlas + the box win.**
- New canonical docs are written ONLY in `docs/atlas/`, audited from the live box, by the keeper.

Why: clean-slate build (Ian 6/19). Accumulated docs carry the mess forward; the atlas is a
fresh, verified, forward record. Frozen ≠ deleted — nothing was removed.
