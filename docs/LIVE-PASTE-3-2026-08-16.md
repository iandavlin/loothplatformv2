# Live paste 3 — 2026-08-16 (135 commits past 794007a)

**RAN 2026-08-16 ~16:06, clean.** 794007a → a69e7e0 fast-forward; lg-deploy
auto-linked the new webroot file and reloaded nginx itself; smoke 200 / 1
canonical / 0 stripe traces. The sitemap submit is unblocked and handed to
Ian. The lg-wp-cron drift WARNs appeared again — still backlog 33, still
pre-existing. The lg-profile-setup.php mu-plugin symlink was created and
verified 16:13 — every step of this paste is done.

**Everything here is Ian's to run, in this order, on the live box.**

**Why this one is urgent rather than routine:** it FIXES A LIVE PRIVACY LEAK —
the featured-member card on the public front page can quote member-only
profile text to anonymous visitors (found by Ian 8/16, gate 58 now guards
it) — and it delivers the hub canonical tags that unblock the Google
sitemap submit, plus the Guitardle legacy paint fix (live's own path: a miss
currently wears the same style as a hit).

**The ONE member-visible switch riding ON (Ian's explicit ruling, 8/16
"post-slot only"):** the logged-out mobile bottom bar — the center slot
stops implying you can post and reads "Sign in", and the person tab reads
"Account" for logged-out visitors. Members see no change.

**Arrives OFF/inert:** profile-setup step (new mu-plugin, flag false) ·
notification quick-reply (flag false) · notif type filter/clear (flag false)
· server-play + day-puzzle (false) · compose (tracked false) · everything
else in the span is gates, board tooling, and docs.

## 0. Check first (read-only)

    sudo -u ubuntu git -C /home/ubuntu/loothplatformv2-clean fetch origin
    sudo -u ubuntu git -C /home/ubuntu/loothplatformv2-clean rev-list --count HEAD..origin/main

`0` = already run, stop. Bigger = proceed.

## 1. The pull

    lg-deploy

## 2. Same window: TWO couplings (one new mu-plugin, one new webroot file)

    sudo ln -s /home/ubuntu/loothplatformv2-clean/platform/mu-plugins/lg-profile-setup.php /var/www/dev/wp-content/mu-plugins/lg-profile-setup.php
    sudo -u ubuntu bash /home/ubuntu/loothplatformv2-clean/webroot/install-symlinks.sh --new-only

(No nginx changes for live in this span — the conf edits were dev2-only files.)

## 3. Smoke, on the box

    curl -sk --resolve loothgroup.com:443:127.0.0.1 https://loothgroup.com/ -o /dev/null -w '%{http_code}\n'
    curl -sk --resolve loothgroup.com:443:127.0.0.1 https://loothgroup.com/hub/ | grep -c 'rel="canonical"'
    curl -sk --resolve loothgroup.com:443:127.0.0.1 https://loothgroup.com/ | grep -ci stripe

Expected: `200`, then `1` (the hub now names itself to Google), then `0`
(still no Stripe traces for visitors).

## 4. THEN the button this paste unblocks

Submit **https://loothgroup.com/sitemap.xml** in Google Search Console —
the hub pages now carry self-referencing canonicals, so the submit is safe
for the first time.

## Rollback

Nothing member-visible changes except the ruled sign-in slot; if anything
looks off, tell keeper what you saw before touching anything.
