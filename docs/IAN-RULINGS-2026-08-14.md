# Ian's rulings — 2026-08-14 (keeper, quotable)

1. **Loothprints: layout-v2 owns EVERY page, old and new** — "I want all the old
   posts and the new posts to be handled by layout-v2. I thought we made this
   decision." (He did — it is the standing legacy->v2 conversion direction.)
   The compose/edit form is DETAILS-ONLY (licence, type/area of work, print
   files, title — the fields layout-v2's editor cannot reach, per the compose
   lane's measured answer). The form never synthesizes or replaces a page; new
   posts get a layout-v2 page at creation. The A/B/C freeze question is
   RESOLVED BY REFRAME — freeze and drift cease to exist.

2. **Desktop mockups requested** for featured-members (1280w, both tickbox
   variants, dash, front-page card, %-complete meter) — in build.

3. Fleet: relight all 3 teams after the multi-day shutdown ("Relight all 3").

4. **V2 blocks for every form detail** — "If we do not have a block that can handle anything in the new form, like the license, we need to spin up v2 to produce the block... the lane should check. You can have the lane turn into a v2 type lane if we need it to." Lane: coverage table first (28 fields vs block catalog), gaps to Ian, then build. V2 source-of-truth question flagged (projects/ vs monorepo).

5. **V2 blocks: build all four, approved order** — licence block, print-files via download-block + picker, items editor (CAD + tip jar), work-type taxonomy block. Compose lane RE-CHARTERED as the layout-v2 lane ("Yes — build all four").

6. **Featured members, all four design rulings (decision box, after decide.html):**
   - Tickbox = OPTION B, its own block on the profile.
   - The EIGHT completeness items LOCKED as proposed (12.5% each): photo, location, what-you-do, bio, links, skills+instruments, work photos, banner.
   - Volunteering: ACCEPT ANY % — tick welcomed at any completeness, member shown their card preview, meter does the nudging. No floor.
   - Meter scope: THIS FEATURE ONLY (profile + tickbox flow + dash); site-wide push stays backlog 19.

7. **V2 MAY INSERT a missing block into a stored page** ("Yes — may insert"): first the licence block on the 3 licence-invisible posts (72155/72146/71927), same principle covers the work-type block later. Only to surface what the author already declared.
8. **4th seat = Guitardle fix** (backlog 22).

9. **Guitardle anon ruling** ("Agree — relabel only"): logged-out play stays free and untracked; the claim button becomes honest ("Sign in to claim a spot") for logged-out visitors; the member-side fix (account-tracked daily attempt, claimed at START, with RESUME) is the build.
