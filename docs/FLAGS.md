# FLAG REGISTER — every switch, where it lives, and what it's doing

Started 2026-08-09 (Ian: "how are we keeping track of all these flags?").
**Maintenance rule: any merge that adds, flips, or retires a flag updates this
file IN THE SAME COMMIT — keeper refuses the merge otherwise.** States below are
as-verified on 2026-08-09; live = commit `021ff38` unless noted.

**Issue rule (Ian 8/19): closed = live to members.** Flipping a flag ON for
live closes its GitHub issue in the same breath; unflagged work (bug fixes,
docs, tooling) closes at live deploy. An issue is never closed at mere merge —
merged is not shipped here.

## Member-facing feature flags (tracked config — flip by commit + pull)

| Flag | Lives in | Repo default | dev2 | live | State |
|---|---|---|---|---|---|
| sheet-embeds `enabled` | platform/config/sheet-embeds.php | **true** | ON | **ON** (smoke-verified on box) | ✅ DONE — Ian-verified both boxes 8/9 ("looks good") |
| social-actions `enabled` | platform/config/social-actions.php | **true** | ON | **OFF** (flip commit post-dates live's pull) | Ian-verified dev2 ("workds"); reaches live on next lg-deploy |
| `LG_HUB_TOPIC_LANDING` | bb-mirror/config.php (env/$_SERVER '0' forces OFF) | **true** | **ON** | code not on live yet | Flipped ON 8/9 (Ian). Serves the hub+modal at the permalink instead of the legacy page. Reaches live on next lg-deploy; Ian testing dev2 serve first |
| post-follow `enabled` | platform/config/post-follow.php | **false** | OFF | OFF | E3 server half built (one-mailer); gate 18 asserts ruling-6 defaults |
| follow-digest `enabled` | platform/config/follow-digest.php | **true** | ON | ON | E1 GA since 8/8 (fd0d196); the nested second `enabled=false` is §2.2 forum-items — see below |
| follow-digest forum-items (nested) | platform/config/follow-digest.php | **false** | OFF | n/a | §2.2 build in flight (one-mailer); lane refuses merge until preview-armed verification — arm previews via `LG_FD_FORUM_ITEMS` env/$_SERVER only |
| `bell_follows_bb_subscriptions` | platform/config/notify-bridge.php | **false** | OFF | OFF | ❌ CLOSED — Ian ruled leave OFF 8/9 (consent inference; ruling 6 separation stands) |
| back-pill `enabled` | platform/config/back-pill.php | **false** | OFF | OFF (code not on live yet) | 3.8 back-nav hybrid (lower-left, appear-on-scroll). Gate 22 (nav must navigate). Merged 8/9; awaiting Ian on the serve, then flip |
| `dismiss_instead_of_delete` | profile-app/config/notifications.php | **false** | OFF | OFF (code not on live yet) | E4 delete=dismiss (ruling 9). ⚠️ **HAS A SCHEMA DEPENDENCY AND AN ORDER — see below.** Gates 23 + 25 assert both states |
| weekly-front `enabled` | platform/config/weekly-front.php (box-local override: `platform/config/weekly-front.local.php`, gitignored) | **false** | OFF until merged, then flipped ON for Ian's look via `tools/preview/weekly-front-flip-dev2.sh` | OFF (code not on live yet; protected by the override file being ABSENT) | Backlog 8 — the latest weekly issue on the front page for logged-out visitors. Ian ruled Option A 8/15 ("build it and let me see it on dev2") and has NOT seen the built thing yet. Gate 54→renumber pending keeper. ⚠️ The dev2 flip is a `.local.php` file, NOT an FPM `env[]` — dev2's pool files are symlinks into the serving checkout, so an env flip dirties tracked files there and a later `pull --ff-only` can refuse |
| composer-categorize-last `enabled` | platform/config/composer-categorize-last.php (+ box-local composer-categorize-last.local.php, gitignored; preview override `LG_CCL_PREVIEW` env/$_SERVER) | **false** | ON (dev2 .local.php, 8/20) | **ON — LIVE since 8/20 ~23:35 UTC** (Ian's deploy + .local.php with live `default_forum_id` **72757** "Discussions"; forum created by Ian 8/20, mirror row announced via forum Update and verified through live-ro; the 21-pair map verified valid on live ID-by-ID — shared ancestry; Ian on the live composer: "It's good." #129 CLOSED. ⚠️ live 72757 still lacks `_bbp_forum_type`/`_bbp_status` metas — contract passes without them; tidy opportunistically) | #129 ledger 44 — write first, categorize last. Removes the composer's required Where step; untagged discussions land in the default forum; optional tag step + hierarchical picker at the end maps Content Topics -> forum. Ian ruled Option C 8/19 ("Looks good"). Gate 74 asserts all three states (absent/OFF/ON). ⚠️ **TWO THINGS MUST BE TRUE BEFORE THE FLIP, EITHER BOX:** (1) `default_forum_id` is per-box — dev2's Discussions is 73564, live's twin forum has a DIFFERENT post ID and must be set in the same breath as the flip; (2) that forum must be IN THE POSTGRES MIRROR — the picker, the hub's forum reads and the postable contract all read Postgres. On dev2 this was the blocker on 8/19 (73564 in WordPress, absent from `forums.forum`, because forum rows arrive only via bbp_new_forum/bbp_edit_forum); **resynced and re-verified 8/20 — `lg_ccl_default_forum_ok()` now true as the pool user, with 3818/3876 controls still correctly not-postable**. LIVE will hit the same state when its twin forum is created there. `lg_ccl_default_forum_ok()` keeps the Where step while it is false, so the composer refuses rather than filing posts at a forum row that does not exist. Resync: `bb_mirror_sync_dispatch('forum', <id>, 'upsert')` |
| `LG_FD_CADENCE_CONTROL_SHIPPED` | mu-plugin define | **false** | OFF | OFF | Cadence control behind it; gate 15 asserts absence when off |
| `LG_PRESERVE_FORUM_SUBSCRIPTION` | mu-plugin define | **true** | ON | ON | P0 4.5 fix, LIVE since 8/8 — never turn off (data loss) |
| `LG_AUTHOR_SOCIALS_ALL_MEMBERS` | bb-mirror/config.php define | **true** | ON | ON | The original flag-pattern exemplar; GA |
| `LG_BB_MIRROR_FOLLOW` | FPM pool env (platform/fpm/) | dev2 "1" | ON | per live pool | Thread-follow surface; ⚠️ deploy-tooling branch documents a restore sequence that silently zeroed it — see that branch's register before touching pools |
| `LG_THREAD_FOLLOW_ENABLED` | bb-mirror/config.php | see file | — | — | Same family; two-source pattern (getenv/$_SERVER) |
| frontend-compose `enabled` | platform/config/frontend-compose.php (+ **box-local** frontend-compose.local.php — see below) | **false** | **ON** (via the .local.php, verified on the serve 8/16: `lg_fc_enabled()` true, route serves 184,627B to an allowed member) | OFF (no .local.php there) | Front-end compose/edit. Ian's item-5 'Do it' 8/15, light + dark. Gates 19 (OFF is a byte-identical no-op) + 35. ⚠️ ON for **all members** once flipped — the allow-list was deleted, so the flag is the only thing narrowing it |
| hub-author-banner-swap `enabled` | platform/config/hub-author-banner-swap.php (+ box-local .local.php, reader in bb-mirror/web/_chrome.php; env `LG_HUB_AUTHOR_BANNER_SWAP` exists for gate red-first legs only — never a deploy mechanism) | **false** | **ON** (dev2-only .local.php, placed 8/16 for Ian's ordered look) | code not on live yet | Backlog 38, P0: the Adv-Search modal's in-place author pick never redraws the green author banner. ON = `_feed.php` wraps the banner loop in one addressable div + `fmodalApply` swaps it like the chip bar. OFF proven byte-identical by its gate. Gate **64** (CDP, red-first) on the featured seat's branch, riding behind this merge. Row added by keeper at merge — the lane's branch predates the flag-register discipline check |
| profile-setup `enabled` (+ `testers` allowlist) | platform/config/profile-setup.php (env/$_SERVER `LG_PROFILE_SETUP` forces ON) | **false** | OFF | code not on live yet (no config file in live's checkout, verified 8/16) | Backlog 19 arrive-alive step at /profile-setup/. With `enabled=false` AND `testers=[]` the route is never registered — a genuine absence, not an empty page; gate 51 asserts it. ⚠️ **REGISTERED RETROACTIVELY 8/16** — it merged without a row, which is the omission the flag-register gate found. ⚠️ On dev2 the mu-plugin is merged but NOT symlinked into the docroot, so the route 404s even if flipped — the `ln -s` must happen in the same window as the pull |
| welcome-activation `enabled` (+ `cutover` date) | platform/config/welcome-activation.php (**no env override on purpose** — the arbiter runs in cron sweeps and lg-wp-cron.service carries no Environment=) | **false** | OFF | code not on live yet | Rail-agnostic first-activation welcome in `Arbiter::sync`; keys on the MEMBER (`_lg_membership_activated_at`) instead of role ordering, so a Patreon and a Stripe joiner get the same welcome. OFF is byte-identical — no marker is written at all. `cutover` (2026-08-16) fences retro-fire: all 1,225 existing paid members registered before it, so arming cannot mail one of them **even if the backfill is never run**. ⚠️ The MODAL leg is what this switches; the **EMAIL leg is separately suppressed on live** by lg-poller-mail-killswitch + `Plugin::gateOutboundMail` (`lgms_poller_mail_enabled` absent on live). Gate: tools/gates/welcome-activation-gate.py — number **63** minted by keeper 8/16 at the handoff; the file may still carry the placeholder, profiles seat applies it next wake |
| hub-feed-noindex `enabled` | platform/config/hub-feed-noindex.php (box-local override: `.local.php` beside it) | **false** | ON (dev2 local, crawler-view verification) | OFF | #81 v2 (Ian 8/19: strangers see the member-identical hub — the teaser approach was RETIRED same day, its flag file deleted). ON = bare /hub/ carries crawler-only noindex,follow (emitted beside the canonical, only when canonical IS /hub/); every modal-open per-topic address stays indexable so search links migrate there. Humans see zero difference in any state. Close #81 at live flip. |
| location-address `prefer_typed_address` | profile-app/config/location-address.php (box-local override: `profile-app/config/location-address.local.php`, gitignored) | **true** | ON | ON at Ian's next lg-deploy (ruled 8/18: "Yes — flip it", after dev2 verify) | The profile's Location block printed `users.location_address` — a column whose ONLY writer is the one-time BuddyBoss import — instead of the address the member typed into `users.location_text`. Member-reported by John Wilmink (Thomas Muse Guitars), 47 days old; the map pin was always correct, only the text was stale, which is why it read as a save bug. ON = street precision prefers the typed value, AND the save path writes `location_address` in step so the column stops being a fossil for the other readers that trust it (Profile.php exact tier, api/v0/directory-members.php). ⚠️ **Both halves are gated together on purpose** — an ON that wrote the column while OFF only changed rendering would leave members who saved during ON carrying data OFF never meant to create. OFF is byte-identical on screen AND on disk. City/State precision is untouched in both states and gate **73** asserts that in all three states (absent/OFF/ON), because "print what the member typed" is one edit away from leaking a street to a City audience. Four live members mis-render under OFF: ids 190, 590, 598, 1323. ⚠️ Whoever lands `directory-location`: its new `listLocationText()` prints this same frozen column at street level, so under OFF those four would get their old HOME address into directory rows and map-pin popups — flagged to Ian 8/18, not changed by this lane |
| featured-members `enabled` | platform/config/featured-members.php (env/$_SERVER `LG_FEATURED_MEMBERS`) | **true** | ON (since 8/15, Ian: "Yes, flip when landed") | code not on live yet | Backlog 18 / #107 — the member tickbox, the admin pool dash, and the front-page band resolving a REAL member instead of the hand-typed one. OFF is byte-identical: `u.php` emits the tickbox block only when on, and `index.php` ignores any `member_uuid` in config.json outright, so a stale value cannot leak the real-member path back on. Gate **39** §C asserts markup, CSS and script are each gated, and §C4 that `me-featured.php` can really reach the file. ⚠️ **REGISTERED RETROACTIVELY 2026-08-20 by the 107-consent-followup lane** — it merged without a row, found while adding the row below |
| featured-consent `enabled` (+ `informed_copy_since`) | platform/config/featured-consent.php (env/$_SERVER `LG_FEATURED_CONSENT`) | **false** | OFF | code not on live yet | #107, Ian 8/20 (decision box): *"the tick is consent"* — the featured card may repeat an OPTED-IN member's one-line "what you do" even when their profile header is members-only, which is the platform default covering 1,917 of 1,933 members. ON also adds the sentence to the tickbox copy that says so, because consent that does not state what it covers is not consent. OFF is byte-identical, proven on the SERVED page (218,021 bytes either way; the first cut leaked 8 bytes of indentation because PHP eats the newline after `?>` but not the whitespace before `<?php`). Gate **39 §G** asserts all three states and EXECUTES the rule over a 12-case truth table. ⚠️ **TWO THINGS MUST BE TRUE BEFORE THE FLIP, EITHER BOX:** (1) `informed_copy_since` must be set to the flip moment IN THE SAME EDIT, with an explicit UTC offset — it is compared against a Postgres timestamptz, and left null it means "nobody is informed", a flag that reads as working while doing nothing (§G1 goes RED on both); (2) **the column grant must land first** — `tools/cut/featured-member-grants.sql` now grants `users.featured_opt_in_at` to the `archive-poc` role, and without it the resolver raises `permission denied for table users`, which its own try/catch turns into NO BAND for every visitor. Applied and verified on dev2 8/20; **live needs it before the flip**, and re-applying after any profile_app restore. §G2 asserts the grant is live so a blank band is never a mystery. Existing tickers are NOT silently upgraded: republication needs an informed tick or a `consent_ack` the dash records when an admin features them knowingly |
| auto-ban `enabled` | platform/config/auto-ban.php (box-local override: `auto-ban.local.php` beside it, gitignored) | **false** | OFF | OFF (code not on live yet) | #162 — the credential-stuffing detector puts the offender on a blocklist the webserver reads. Ian 8/20: *"can we just add it to a file of known offenders and nip it at our webserver?"*, narrowed the same day to *"this should only block ips that try several different logins in one block"*. ON = `lg-login-monitor` appends to `/var/lib/lg-auto-ban/state.json` at the same moment it sends the stuffing alert — same threshold, same window, no new detection. OFF writes **no file at all** (not an empty one) and leaves the alert email and the ring-buffer log byte-identical; gate **84** §A asserts all three states (absent/OFF/ON) read from the flag. ⚠️ **THIS FLAG BLOCKS NOBODY ON ITS OWN — THERE ARE TWO KEYS.** Enforcement needs the box-local nginx snippet `/etc/nginx/snippets/lg-auto-ban-doors.conf` + `conf.d/lg-auto-ban-maps.conf`, which ONLY `sudo tools/infra/install-auto-ban.sh` creates and which live is protected by the absence of. That split is the point: Ian asked to watch the ban file behave on dev2 before anything is refused, and flag-ON + snippet-absent is exactly that state. The dash (wp-admin → **Login bans**) leads with which of the two it is. ⚠️ **A NEW mu-plugin (`lg-auto-ban.php`) NEEDS ITS `ln -s` IN THE SAME WINDOW AS THE PULL** — mu-plugins are 42 individual symlinks and a pull creates none of them. ⚠️ The vhost gains one glob `include` line; it names no `$lg_ab_` variable, so a box that never runs the installer is unaffected and `nginx -t` cannot fail there (gate 84 §F2). `ban_seconds` (86400) and `max_entries` (500) live in the same file; the renderer keeps its **own** cap because a bound the untrusted writer can raise is not a bound. Close #162 at the live flip. |
| tester-unlock `enabled` + `token_sha256` | `platform/config/tester-unlock.php` (box-local override: **`platform/config/tester-unlock.local.php`**, gitignored — this is the DEPLOY MECHANISM; preview override `LG_TESTER_UNLOCK` / `LG_TESTER_UNLOCK_SHA256` env/`$_SERVER`, red-first legs and lane previews only) | **false + empty hash** | OFF (keeper places the `.local.php` once #180 merges — that flip is the whole ask) | OFF (code not on live yet) | #180. Ian 8/21: *"dev2 join goes to stripe rather than patreon with a fresh incognito. I need it to go to patreon unless the user has some kind of token url or something to unlock the whitelisted pages."* ARMED, **one shareable URL marks ONE browser** with the cookie `lg_join_unlock` (HttpOnly, Secure, SameSite=Lax, host-only, 1yr) and for that browser only the header's Join points at `/lgjoin/` — and the join-flow door admits it. `claim /lgjoin/?lgtester=<token>` → cookie, then **302 to the clean URL** so the token leaves the address bar, the history and every onward Referer, and the next request proves the COOKIE rather than the parameter. `clear /lgjoin/?lgtester=off` needs no token and works even after a rotation, so a stranded visitor can always un-mark themselves. ⚠️ **IT WIDENS `allowlist` AND ADDS NO STATE**: in `'off'` the cookie is **not consulted at all** — 'off' still means NOBODY, which is #170's ruling and live's tracked default — and in `'on'` it is redundant. So this flag only means anything on a box whose header is in `'allowlist'`; **both boxes are meant to sit there during the soft launch** (dev2 already does, verified on the serving checkout). Arming it on an `'off'` box does nothing and nothing is broken — gate 85 §F reports that pairing rather than asserting it. ⚠️ **THE STORE HOLDS A HASH, NEVER THE TOKEN**: `token_sha256` is sha256 of the token, compared with `hash_equals`; reading the config (or a backup of it) hands nobody a working link, and gate 85 §A1/§A1b assert that **no tracked file pairs `token_sha256` with a 64-hex value** and that the `.local.php` really is gitignored. **TWO INDEPENDENT KILL SWITCHES**: `enabled => false`, or an empty hash — `enabled => true` with no hash is a DEAD config, so a half-placed override fails CLOSED. Rotate by replacing the hash; every cookie already out there stops matching on the next request. **NO EXPIRY, NO EMAIL MATCH, NO PASSWORD** (Ian ruled the simple shape): a forwarded link admitting a stranger to LOOK is accepted, because **viewing is not signing up**. ⚠️ **THE REFUSAL THAT MAKES THAT SAFE IS NOT A WHITELIST — measured 8/21 on both boxes.** Nothing in the signup or checkout path consults the cohort list. What dead-ends a marked browser today is that lgjoin's own JS requires `POST /wp-json/lg-member-sync/v1/auth` to return ok before it calls checkout, and that route answers anon **401 `bb_rest_authorization_required`** — BuddyBoss's global `bb-enable-private-rest-apis`, re-armed by every DB reload. Gate 85 §E measures that refusal **as it is** and REPORTS the mechanism rather than asserting a whitelist that does not exist; the API-level gap is keeper's separate go-live issue. **NO SECOND SWITCH, unlike #165 and #170**: the admission lives in `lg_membership_testgroup_gate_or_exit` — the one gate both doors delegate to, where invites already plug in — so arming opens the button and the page together and `lgms_stripe_testgroup_pages` does not enter into it. **The claim runs BEFORE that gate's `manage_options` early-return**, or an ADMIN clicking the link marks nobody and never finds out (gate 85 §A5; Ian is an administrator). **CONFIG COUPLING**: `platform/nginx/lg-microcache.conf` bypasses the anon microcache for this cookie — a marked browser IS anonymous, so without it `/hub/` serves a 60s-cached header still pointing at patreon.com. Inert until armed. **OFF is byte-proven**: 18 cmp comparisons against `origin/main` across 3 states × 3 viewers × 2 config shapes, **on a tree already carrying the nginx change**; ARMED with a valid cookie **exactly one** cell differs (allowlist/anon). Gate **85** (116 assertions; red-first **25/25**) |
| `lgms_checkout_audience` | **wp_option** (WP admin / `wp option update lgms_checkout_audience allowlist`) — NOT a config file and NOT an FPM pool env, because the readers span the poller, WP-Cron (no environment at all) and, over the shared-secret REST channel, the Slim billing app (which cannot read wp_options — its DB user holds `ALL ON lg_membership` and `USAGE ON *.*`) | **`allowlist` (ENFORCING)** | `allowlist` (by default — the option is ABSENT on dev2, verified 8/21) | `allowlist` on the first pull (code not on live yet) | #181. Ian 8/21, decision box: ***"Fix before go-live"***. ⚠️ **THIS IS THE ONE FLAG IN THIS FILE THAT DEFAULTS TO THE ENFORCING STATE, and the exception is deliberate** (keeper ruling (a), 8/21): the enforcing state must be the state the boxes actually run, or it is never exercised before the night it has to work. A default of `off` would ship a fence nobody had ever walked into. **THE HOLE IT CLOSES, reproduced on dev2 as served, anon and cookieless, with a real price id from the PUBLIC `/billing/v1/products` list: `POST /billing/v1/checkout` -> `HTTP 200` + a live Stripe `clientSecret`.** Paying it ran `Sync::customer` -> `UserProvisioner::findOrProvision`, which creates a WP user by email and grants the tier. **THREE STATES:** `off` = nothing is consulted, today exactly; `allowlist` = the soft-launch cohort and nobody else, at BOTH the mint and the provision halves; `on` = GA, the switch Ian throws when the test phase ends. **ONE LIST, NEVER A SECOND** — `lgms_stripe_lifecycle_allowlist` read through `StripeLifecycle::inCohort()`, the same rows the webhook fence, the entitlement sweep and the header pill already key on; gate 86 §D asserts no file names a cohort option of its own. ⚠️ **NO ADMIN BYPASS** (keeper ruling (b)) — unlike the header's `$caps['stripe_testgroup']`, which is `manage_options || inCohort()` and is right for a *button*. It is wrong for a *fence*: an administrator who sails through cannot see it fail, and Ian is an administrator. He is in the dev2 cohort by list (id 1), not by privilege. **TWO HALVES, and only one can be routed around:** the MINT half refuses early and honestly at all three doors (Slim `/billing/v1/checkout`, WP `/me/checkout-session`, and lgjoin through the first); the PROVISION half lives in `UserProvisioner::findOrProvision` **one line below the existing-bridge early return**, reads the option in-process with **no network**, and therefore fails CLOSED — it is what stops a session minted *before* the list changed. ⚠️ **THE PLACEMENT IS THE DESIGN:** below the bridge return means an already-bridged member is untouched in every state, so their sweeps keep landing — **grants AND retractions**. Above it would freeze real members the moment the cohort narrowed, silently. **FAIL-SAFE IS CLOSED:** an absent option, a typo, a stray array, a value from the future all read `allowlist`; only the literal `on` opens the doors. **UNKNOWN REFUSES, with 503 and a DIFFERENT SENTENCE from the 403** — "could not verify" vs "not open for sale yet", because those need opposite fixes and one shared sentence sends whoever is debugging down the wrong one. **GIFTS ARE FENCED TOO** and the Slim guard takes no `isGift` argument, so there is no exception to forget. ⚠️ **THERE IS NO `off` VALVE ON THE PROBE URL**, unlike `LGMS_PATREON_STANDING_URL` — blanking that one stops a probe whose silence lets buyers through; blanking this one would refuse *everyone*. To stop enforcing, set the option to `off`, in WordPress, on the record. ⚠️ **CONFIG COUPLING, NONE — verified**: `/srv/lg-stripe-billing` and the poller mu-plugin are BOTH symlinks into the serving checkout, so one `pull --ff-only` deploys both halves atomically; and the probe URL DERIVES from `LGMS_SYNC_URL`, so no box needs an env edit. Gate **86** (145 assertions; red-first **20/20** mutations + 2 no-op controls) |
| `bb_exclude_endpoints_from_restriction` (filter, not a flag — recorded here because it is load-bearing for the row above) | `lg-patreon-stripe-poller/src/Wp/CheckoutAudienceRestController.php`, registered in `Plugin.php` | n/a | active | active on the first pull | #181. ⚠️ **THE SHARED-SECRET REST CHANNEL WAS DEAD, AND THIS IS THE NARROW REPAIR.** Measured on dev2 8/21 from 127.0.0.1 **with the correct secret**: every server-to-server route in the `lg-member-sync/v1` namespace answers **401 `bb_rest_authorization_required`**, because BuddyBoss's `bb_restricate_rest_api` short-circuits the REST stack *before* any route's own `permission_callback` whenever `bb-enable-private-rest-apis` is `1` — which it is on dev2 AND live, and which is **re-armed by every DB reload**. Without this the audience probe could never get an answer, every checkout would read UNKNOWN, and the fence would refuse the cohort too. The repair uses BuddyBoss's **own documented extension point** and names **exactly one route**, `/lg-member-sync/v1/checkout-audience`; the route's shared-secret check still runs, so it is not an auth bypass — it only decides *which* check does the refusing. Gate 86 §G8 asserts it adds one route, preserves what was there, is idempotent, and returns a non-array untouched. ⚠️ **`/sync-customer`, `/patreon-standing` and `/send-gift-codes` ARE STILL 401 AND WERE DELIBERATELY NOT OPENED** — that means **#150's double-pay probe has been answering UNKNOWN on every call** since the option was last re-armed, and the Slim app's post-checkout sync ping is dead (the five-minute `Sync::all()` sweep has been covering for it, which is why nothing looked broken). Reported to keeper as a separate finding with this exact one-line fix; widening an auth surface past the route this lane needs is not a decision to make in passing |
| header-join-stripe `state` | platform/config/header-join-stripe.php (box-local override: `platform/config/header-join-stripe.local.php`, gitignored; preview override `LG_HEADER_JOIN_STRIPE` env/`$_SERVER`, red-first legs and lane previews only — never a deploy mechanism) | **'off'** | 'on' (dev2's .local.php still says the legacy `enabled => true`, which the reader maps to 'on') | 'off' (code not on live yet) | #165, then **#170**. ⚠️ **THREE STATES SINCE #170, not two** — Ian 8/20: *"We need the join button in the header to still go to patreon unless a test user is there on live."* `'off'` = nobody; the patreon.com anchor of his 2026-06-12 ruling, byte-for-byte, in a new tab. `'allowlist'` = the Stripe soft-launch cohort ONLY, and only while signed in — every anonymous visitor and every unlisted member still gets patreon.com. `'on'` = everybody; our own two-tier `/lgjoin/`, same tab. **Connect Patreon is untouched at every width in all three states** — the dual-rail founding law is served by the destination page offering both rails, not by the button. ⚠️ **`enabled => true` IS STILL READ AND STILL MEANS 'on'.** dev2's hand-placed `.local.php` says exactly that and lives in the SERVING CHECKOUT, which no lane may edit; a reader that dropped the legacy key would revert dev2's header to patreon.com on the next `pull --ff-only` with nobody having flipped anything. Gate 79 asserts it against dev2's byte-exact on-box shape. ⚠️ **NEITHER 'allowlist' NOR 'on' IS SUFFICIENT ON ITS OWN, and they need DIFFERENT partners:** `'on'` pairs with the wp_option **`lgms_stripe_pages_live`** (WP admin → Settings → LG Member Sync), or an anonymous visitor is handed *"This page isn't available yet"* by `membership-pages/web/router.php` (`['lgjoin.php','testgroup','public']`). `'allowlist'` pairs with **`lgms_stripe_testgroup_pages`** instead — and the two predicates are NOT the same shape: the header PILL has ONE lock (the cohort list, via `$caps['stripe_testgroup']`), the DOOR has TWO (that flag AND the list). So a listed NON-ADMIN can be handed a pill and refused at the door, while an admin passes both and sees nothing wrong. **No second list anywhere**: one wp_option `lgms_stripe_lifecycle_allowlist`, written by `LGMS\CohortAllowlist`, reaching the header as a capability the poller already computes. **THE CACHING LAW is structural, not careful**: `'allowlist'` keys on a per-viewer capability an anonymous ctx never carries, so the logged-out page CANNOT differ between 'off' and 'allowlist' — proven by cmp at 28,028 bytes, not argued. Gate **79** (157 assertions with the browser leg, 104 without; red-first 23/23). #165's "the flag cannot reach a signed-in member in ANY state" is NARROWED to off/on — 'allowlist' exists precisely to reach one — and what it may change is pinned to exactly one added line, none removed. The blast radius is the reason for the flag: this partial renders on every page of seven independent apps (bb-mirror, archive-poc, events, profile-app, membership-pages, lg-layout-v2, a docroot script). `webroot/bottom-nav.js` needs no flag of its own — both its anon sheet and (since #170) its authed "You" sheet read the header's href — but the authed row is load-bearing: at ≤640 on the hub the entire aside is `display:none!important`, so it is the tester's ONLY door to `/lgjoin/` at a phone width |
| header-join-stripe `enabled` | platform/config/header-join-stripe.php (box-local override: `platform/config/header-join-stripe.local.php`, gitignored; preview override `LG_HEADER_JOIN_STRIPE` env/`$_SERVER`, red-first legs and lane previews only — never a deploy mechanism) | **false** | OFF (keeper places the `.local.php` once #165 merges — that flip is the whole ask) | OFF (code not on live yet) | #165. Ian 8/20: *"can you Wire the header on Dev2 to have the stripe menuing that a logged out user would see?"* ON = the ANON header's Join goes to our own two-tier `/lgjoin/`, in the same tab; OFF = the patreon.com anchor of his 2026-06-12 ruling, byte-for-byte, in a new tab. **Connect Patreon is untouched at every width in both states** — the dual-rail founding law is served by the destination page offering both rails, not by the button. ⚠️ **THIS FLAG IS NOT SUFFICIENT ON ITS OWN, and that is the whole trap:** `/lgjoin/` decides its own audience via `membership-pages/web/router.php` (`['lgjoin.php','testgroup','public']`) and the wp_option **`lgms_stripe_pages_live`** picks which column applies. While that option is off the PRE-LAUNCH column applies and an anonymous visitor is handed *"This page isn't available yet"* — measured on dev2 2026-08-20. **The two flip in the same window** (this flag on the box; `lgms_stripe_pages_live` in WP admin, Settings → LG Member Sync), or Join is wired perfectly and lands nowhere. Gate **79** holds that shut: while this flag is ON it ASSERTS anon `/lgjoin/` is not the stub; while it is OFF that leg reports and does not assert, so it never reddens an unrelated lane. OFF is **byte-proven, not argued** — the partial is rendered from the branch and from `origin/main` with the same anon ctx and compared byte for byte (28,028 both ways), for the ABSENT config too, and an AUTHED ctx is byte-identical in every state including ON. The blast radius is the reason for the flag: this partial renders on every page of seven independent apps (bb-mirror, archive-poc, events, profile-app, membership-pages, lg-layout-v2, a docroot script). `webroot/bottom-nav.js` needs no flag of its own — the PWA anon sheet reads the header's href — but its `target="_blank"` was unconditional and is now derived from the href, because an internal page opened in a new tab throws a member out of the installed PWA (`display:standalone`) to buy a membership in a browser |
| front-signup-banner-retire `enabled` | platform/config/front-signup-banner-retire.php (box-local override: `platform/config/front-signup-banner-retire.local.php`, gitignored; preview override `LG_FRONT_SIGNUP_BANNER_RETIRE` env/`$_SERVER`, red-first legs and lane previews only — never a deploy mechanism) | **false** | OFF (Ian flips it once he has seen the front page without the strip on the dev2 serve, logged out — that look IS the ask) | OFF (code not on live yet) | #169. Ian 8/20, from his logged-out funnel walk: *"The secondary join on the front page at the top banner can go away."* ON = the anon `<aside class="signup-banner">` is not emitted; OFF = today's behaviour for everybody. Logged out at 1440 there were **three** join doors above the fold — the header pill, this green strip, and the hero button — and this retires the middle one. ⚠️ **THE COLLATERAL IS THE MEMBER GREETING.** The banner is the `if` half of an if/elseif in `archive-poc/web/index.php` whose `elseif ($is_member)` renders *"Welcome back, &lt;name&gt;."* — a different feature sharing the `.signup-banner` class and the same slot. This flag gates the **ANON half only**, the shared CSS is deliberately untouched (deleting it would take the greeting's styling with it), and gate **80** asserts the greeting renders in BOTH states with the authed render **byte-identical across them**, so the flag cannot reach a signed-in member at all. OFF is **byte-proven, not argued**: the real script is rendered from this branch and from `origin/main` in the same directory (a snapshot sibling, so every `__DIR__` require resolves identically) under the same anon ctx — **72,054 bytes both ways**, and ON is 71,624, differing by exactly the nine lines of the `<aside>` and nothing else. Blast radius is unusually small and was checked rather than assumed: nginx routes exactly **two** URLs to this script (`location = /` and `location = /front-page/`), unlike the header partial's seven apps |

### ⚠️ The `*.local.php` BOX-LOCAL override — a second way these flip, added 8/15

The table above says "flip by commit + pull". That is no longer the whole truth.
Two of these flags are ON on dev2 without any commit, via an **untracked,
gitignored** per-box file next to the tracked one:

| Flag | Tracked default | Box-local file (dev2 only) | Reader |
|---|---|---|---|
| frontend-compose | `false` | `platform/config/frontend-compose.local.php` | `lg_fc_enabled()` in the mu-plugin |
| back-pill | `false` | `platform/config/back-pill.local.php` | `bb-mirror/web/_chrome.php` |

**The shape**, and copy it rather than inventing a third: the reader loads the
tracked config first, then `@include`s `<name>.local.php` and lets it win only on
`array_key_exists('enabled')` + `=== true`. Unreadable or malformed → the tracked
value stands. `.gitignore` carries the glob `platform/config/*.local.php`.

**Why it exists:** an FPM pool env reaches FPM *only*, so wp-cli, WP-cron and the
gates read the opposite state from the serve — that split is what reddened gate 35
on a healthy box. It also writes a *tracked* file inside the serving checkout via
the pool symlinks, which dirties the one checkout that must only ever pull.

**LIVE IS PROTECTED BY ABSENCE, not by a check.** No code asks which box it is on;
live simply has no `.local.php`, so it takes the tracked default. That is exactly
why these files must never be committed — one commit would switch a member-facing
surface on for everyone.

**⚠️ ORDER, learned the hard way:** the READER merges and the serve pulls BEFORE
the `.local.php` is placed. Reversed on 8/15 — the file was created while nothing
read it, the pool env was removed in the same change, and compose went dark
(`/compose/` 404 to an allowed admin) until the reader landed.

## Runtime WP-option flags (flip by wp option update — PER BOX, no deploy)

| Option | Default | dev2 | live | State |
|---|---|---|---|---|
| `lgms_retraction_sweep` | absent=OFF | OFF | OFF | Phase 1 wave 1 (29fe64e): detection-only sweep; retraction stays the explicit script |
| `lgms_null_shadow_fix` | absent=OFF | OFF | OFF | Phase 1 wave 1: NULL patreon row stops shadowing the reader; measured blast radius = members 612/1768 (fix PROTECTS them) |
| `lgms_stripe_lifecycle` | absent=OFF | OFF | OFF | Phase 1 (0ffb32f): webhook-driven single-tier membership. OFF = no route/no read/no log (keeper-verified). ⚠️ INTERLOCK: refuses while `lgms_identity_gate` dark. FLIP ORDER live: identity_gate ON → lifecycle ON → allowlist governs WHO. ⚠️ SCOPE WIDENED 2026-08-15: this flag now also arms the allowlist fence in `Sync::customer()`, i.e. the ENTITLEMENT SWEEP — the road a redeemed GIFT takes to a role (billing app → `/sync-customer` → Arbiter) and the road `Tick` pass 2 takes every five minutes. Before that, the list guarded the webhook ONLY and a gift to an unlisted member let them in anyway. Flag OFF = the sweep behaves exactly as it always has. Gate #34d: tools/gates/stripe-testgroup-sweep-gate.php (26) |
| `lgms_stripe_lifecycle_allowlist` | absent=**CLOSED for everyone** | unset | unset | Soft-launch cohort (docs/STRIPE-SOFT-LAUNCH-ALLOWLIST.md): array of THIS box's WP user ids; only listed members transition, everyone else's events are 200-acknowledged + journaled `skipped: not in soft-launch cohort (uid=N)`, no membership change EITHER direction. Empty/absent/malformed = nobody (fail-safe, gated). Edit: wp-admin → Settings → LG Member Sync → **Stripe Test Group** tab, or `wp option update lgms_stripe_lifecycle_allowlist '[101,202]' --format=json`. Gate #34: test-soft-launch-allowlist.php (39) + e2e §6 (12) |
| `lgms_stripe_testgroup_pages` | absent=**OFF** | unset | unset | Unlocks the EXISTING member pages (join / gift / redeem / my-gifts / refund / welcome / regional) for the Stripe Test Group, per Ian 2026-08-14 (docs/STRIPE-TEST-VIA-EXISTING-PAGES.md). OFF/absent = those pages stay administrator-only exactly as today — the Test Group unlocks NOTHING. TWO locks: this flag AND `lgms_stripe_lifecycle_allowlist`; either one shut refuses everyone, and an absent/empty/malformed list is nobody. An administrator is never gated behind the list (Ian must not lock himself out of the surface he is building on). Read by the standalone membership-pages app, NOT the shortcodes — an nginx regex shadows those. Gate #34b: tools/gates/stripe-testgroup-pages-gate.php (54) |
| `lgms_stripe_lifecycle_allowlist_added` | absent | unset | unset | Dash bookkeeping only (uid→date added, feeds the cohort table's "date added" column). The lifecycle gate NEVER reads it; losing it costs display data only |
| `lgms_stripe_webhook_secret` | unset | unset | unset | Stripe dashboard webhook signing secret — set on live before lifecycle flip |
| `lgms_stripe_price_month` / `lgms_stripe_price_year` (LEGACY since #148) | unset | unset | unset | **TWO CADENCES** (Ian 2026-08-15: "We need a monthly and a yearly price etc." — his Patreon shape, 5/mo + 60/yr). One option per cadence so neither can overwrite the other. **Superseded by the per-tier options below**, and kept as a READ-ONLY fallback **for the DEFAULT tier only** — see that row for why a non-default tier must never borrow them. Nothing writes these any more. Leave one unset and that cadence is simply not offered. Set from **Settings → LG Member Sync → Stripe Price**. Gate #34c §6b, gate 76 §5
| `lgms_stripe_price_<tier>_<cadence>` (e.g. `lgms_stripe_price_looth2_month`) | unset | unset | unset | **PER-TIER, PER-CADENCE PRICING** (#148). Ian 2026-08-19: *"I've decided I want to be able to have multiple tiers."* The tier is part of the KEY rather than a value inside one option, because a single option holding a map loses a tier's price on a partial write — and a lost price is a tier nobody can buy. Written only by **Settings → LG Member Sync → Stripe Price**, which creates the price in Stripe, records it in our `prices` table, repoints this option, and **retires the price it replaces** as one action. Read through `StripePrice::currentPriceId($cadence, $tier)`, whose fallback chain is `lgms_stripe_price_{tier}_{cadence}` → (**default tier only**) `lgms_stripe_price_{cadence}` → (**default tier + month only**) `lgms_stripe_price_id`. ⚠️ **THE CHAIN IS CLOSED TO NON-DEFAULT TIERS ON PURPOSE**: both legacy options were written when there was one tier, so the price they name is THAT tier's price — letting looth2 read them would sell Looth LITE at the Pro price, silently, on a box that looked correctly configured. An unset tier reads as "not offered" and nothing else. The TIER LIST comes from the catalogue (`StripePrice::tiers()` — active, non-regional, `kind='membership'`), so registering a product is the whole of adding a tier; the dash gains pricing only, no tier-builder form (Ian's 8/19 scope ruling). SANDBOX ONLY: the control refuses a live key. Gate **76** §4–§6, §8
| `lgms_multi_tier` | absent=**OFF** | unset | unset | **THE STRIPE GRANT FOLLOWS THE PRICE** (#148). Ian 2026-08-19: *"I've decided I want to be able to have multiple tiers"*, superseding his 8/08 ruling *"move to ONE tier for the stripe memberships…"*. ON = `StripeLifecycle::applyConfirmed()` resolves the subscription's price through the catalogue (`ProductRepo::tierForPrice`) instead of applying the `self::TIER` constant; the WP checkout door accepts a validated `tier` param; and `StripePrice` reads/writes per-tier options. OFF/absent = the constant `looth3` exactly as before, **and no price lookup happens at all** — gate 76 §3 proves that by running the OFF leg against a database with **no products or prices tables**, so any lookup is a hard SQL error rather than a quiet pass. ⚠️ **WHICH WAY THE OLD BEHAVIOUR FAILED IS THE OPPOSITE OF THE OBVIOUS GUESS**: nobody was under-granted — the constant is `looth3`, so a member buying **Looth LITE at $5 was granted Pro**. And it is not additive: `grantMembershipFromSubscription` revokes by source and re-inserts on any ref change, so the constant **OVERWRITES** the correctly-resolved looth2 entitlement the Slim return path writes. Turning this on is what makes the two Stripe code paths agree. **An unmapped price never retracts**: it falls back to the constant and NAMES the price in the log, because returning null would reach `applyOpinion` as a retraction — the same shape as a cancellation — and demote somebody who just paid. **Not an FPM pool env**, deliberately: the Arbiter runs inside the cron sweep and `lg-wp-cron.service` carries no `Environment=` (`trap-wp-cron-has-no-environment`), and dev2's pool files symlink into the serving checkout (`trap-pool-env-flip-dirties-the-serving-checkout`). A **second** switch inside the already-off `lgms_stripe_lifecycle` on purpose — "is the lifecycle live?" and "does it sell more than one thing?" are different questions, and folding them together would mean multi-tier could only be tested by turning the whole money path on at once. Flip: `wp option update lgms_multi_tier 1` (per box). Gate **76**: `tools/gates/stripe-multi-tier-gate.php`
| `lgms_stripe_price_id` (LEGACY) | unset | unset | unset | Superseded by the per-cadence options above; kept as a READ-ONLY fallback for the MONTHLY slot so a box configured before cadences existed keeps working. Nothing writes it any more, and a real monthly price wins over it. Originally: the single Stripe price NEW joins are sold (read by Wp\CheckoutRestController). **Set it from the dash — Settings → LG Member Sync → Stripe Price** (Ian 2026-08-15: "I'd like to be able to set the price. In the dash."). That control is the only supported writer: it creates the Stripe price, records it in our own `prices` table, and repoints this option as ONE action, because a price Stripe knows and we do not makes an existing subscriber VANISH from the join page's already-subscribed check (INNER JOIN through `prices`) and offers them a second subscription. Nothing back-fills it — the webhook never handles price.created. Changing it grandfathers everyone already subscribed. SANDBOX ONLY: the control refuses a live key. Ships UNSET on purpose — the number is Ian's. Gate #34c: tools/gates/stripe-price-control-gate.php (49) |
| `lgms_double_pay_block` | absent=**OFF** | unset | unset | **ONE PAYMENT SOURCE PER MEMBER** (#150 + #149). Ian 2026-08-19, verbatim: *"We should disallow double payment source for the same user."* Block, not warn. ON = a member with an ACTIVE PAID Patreon standing is refused at all three purchase doors: the Slim API `POST /billing/v1/checkout`, the WP route `POST /wp-json/lg-member-sync/v1/me/checkout-session`, and the `/lgjoin/` page (banner INSTEAD OF the tier picker; the catalogue fetch does not run at all). OFF/absent = today's site exactly, at every door — byte-proven by rendering /lgjoin/ and /manage-subscription/ from the branch AND the serving checkout through the same FPM socket (107,825 vs 107,825 and 47,904 vs 47,904, cmp clean). ON also adds the MEMBER-FACING half of the unblockable direction: /manage-subscription/ tells a member who is being charged on both rails that they are paying twice, leaves the choice of which to cancel with them, and links to /request-refund/. ⚠️ **ONE OPTION ROW, THREE READERS, AND THE THIRD DOES NOT READ IT.** The poller uses `get_option`; the standalone membership-pages app reads the same `wp_options` row over SQL (as it already does for `lgms_stripe_testgroup_pages`); the **Slim billing app cannot read WordPress at all** — its DB user holds `ALL ON lg_membership` + `USAGE ON *.*`, so `wp_users`/`wp_options` are closed to it (the `WP_DB_NAME`/`WP_TABLE_PREFIX` keys in its `.env` are vestigial, nothing reads them). It therefore ASKS the poller at `/wp-json/lg-member-sync/v1/patreon-standing`, a route that only EXISTS while this flag is on — so a 404 IS the off state and there is no second switch to forget. **Not an FPM pool env**, deliberately: the readers span three pools plus WP-cron, a pool env reaches FPM only, and dev2's pool files are symlinks into the serving checkout (`trap-pool-env-flip-dirties-the-serving-checkout`). **Honest cost of OFF**: one loopback request per non-gift checkout that 404s (127.0.0.1-pinned, 2s cap) on a path that already round-trips to Stripe. `LGMS_PATREON_STANDING_URL=off` in the billing `.env` disables the probe outright — an **emergency valve, not the flag**. **Fail-open on purpose**: unknown answer (flag off, WordPress silent, timeout) lets the buyer through; failing closed would stop every sale on a hiccup. Gifts are never refused and never even asked about. Keys on real Patreon standing (`lgpo_patreon_user_id` + `lgpo_tier_map` + `patron_status`), **NEVER on the one-slot `payment_source`**. Flip: `wp option update lgms_double_pay_block 1` (per box). **Measured before flipping (read-only, 8/19): LIVE has ZERO dual payers** — 0 Stripe customers, 0 live subs, 0 bridge rows, 0 `payment_source='stripe'`, 0 stripe rows in `lg_role_sources` — so enforcement can arm with nobody to reconcile. dev2 has 11, all seeded test accounts. Gate **75**: `tools/gates/double-pay-block-gate.php` |
| `lgms_stripe_frozen` | (see poller) | — | — | Stripe ingest freeze — Phase 1 proper unfreezes; stripe seat owns |
| `lgms_poller_mail_enabled` etc. | (see poller settings) | — | — | Poller operational toggles; stripe seat owns |

## Flags on UNMERGED branches (tracked here so they don't get lost)

| Flag | Branch | State |
|---|---|---|
| `dismiss_instead_of_delete` (E4) | origin/notif-bridge | Ian approved merge 8/9; bounced for gate renumber, re-minted 22-25 and re-merged. Now listed in the member-facing table above — **read the ordering note below before flipping it** |
| `bell_follows_bb_subscriptions` (E4) | origin/notif-bridge | ❌ CLOSED by Ian 8/9, ships OFF. Listed above; the code stays so reversing the ruling needs no rediscovery |
| notif-read-seen flag (P0 4.1) | origin/recap-read-timer | 35/35 green; renumbering to gate 21; needs `## Decision to arm` before ARM (not before merge) |
| digest-images flag (P0 4.0) | origin/digest-images | ❌ WRONG PATTERN — documented as wp-config define (untracked on live, breaks deploy-by-pull). Must be reworked to tracked config BEFORE merge. Unowned |
| `LG_NOTIF_QUICKREPLY_ENABLED` (env `LG_BB_MIRROR_NOTIF_QUICKREPLY`) | origin/notif-quickreply-v2 | Tap-to-reply from a notification. Defined in `bb-mirror/config.php`, **default OFF**; read from **both** `getenv()` and `$_SERVER`, because the lane preview feeds it a `fastcgi_param` and that does not reach `getenv()` — a real bug caught on 7/31, not a hypothetical. **NOT ARMED ON THIS BOX** (verified 8/16: absent from every FPM pool and from /etc/nginx). OFF ships no bytes — `pwa.js` does not even request `notif-reply.js` — and the API 404s rather than merely going uncalled. Gate 52. Merges clean into main; awaiting keeper |

## ⚠️ `dismiss_instead_of_delete` — the one flag here with a SCHEMA DEPENDENCY

Every other flag in this file is safe to flip in any order. This one is not, and the
constraint runs the opposite way to the intuition, so it is written out rather than
left to be re-derived.

**ORDER: deploy the code → apply the migration → flip the flag.**
Live pre-flight: `grep -c schemaHasDismiss /srv/profile-app/src/Notifications.php`
on the live box — **0 means STOP**, the code is not there yet.

Migration: `profile-app/sql/2026-08-08-notification-dismiss.sql` (Ian runs live SQL).

**Why the order is not free, measured not theorised.** The migration narrows
`uq_notifications_target_unread` to add `AND dismissed_at IS NULL`. Postgres infers an
ON CONFLICT arbiter whose predicate is IMPLIED BY the clause, and implication runs one
way — so code emitting the OLD two-term clause matches nothing against the new index
and throws `42P10` on every hub push. Applied to dev2 while the serving checkout was
still on `main`, this killed every notification on the box instantly and **silently**
(`lg_notify_push` swallows its errors by contract). Repaired by reverting the index.

**Per-box oddity worth knowing:** dev2 currently has the `dismissed_at` COLUMN but the
OLD two-term index. That transitional state is deliberate and safe both ways — old code
emits two-term against a two-term index; new code emits three-term against it and also
works, since `A∧B∧C` implies `A∧B`. Only the narrowed index is exclusive.

Full reasoning in the migration header and `[[trap-on-conflict-arbiter-implication-direction]]`.

## Related, not flags

- **deploy-tooling branch (7/31, unmerged)**: flag/deploy REGISTER runbooks
  (deploy-symlink-couplings.md, live-divergences.md) + restore-sequence
  preconditions + gate check [0] — born from the 04:39 outage where a restore
  silently turned `LG_FOLLOWING_ROW_TOGGLES` off. Needs a verification pass +
  merge; owner wanted.
- The house pattern itself: docs/BACKLOG lines carry flag state per item; each
  config file carries the flag's whole story as comments; gates assert shipped
  defaults by READING them (never by assuming).
