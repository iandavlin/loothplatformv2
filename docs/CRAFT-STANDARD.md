# Web-craft standard (Ian 6/12: "figure out how this gets fixed permanently")

The disease this kills: basics (image sizing, eager scripts, cache headers)
were re-discovered and re-fixed surface by surface — ~13 rounds on images
alone — because each session's lesson died with its context and "verified"
meant "the screenshot looks right." Screenshots can't see weight.

## The law

1. **Discovered twice → becomes a gate.** Any defect class found a second
   time MUST be encoded as a mechanical check in `tools/gates/` before it is
   fixed the second time. Fixes without gates are rent; gates are ownership.
2. **Gates run, not get remembered.** `tools/gates/run-all.sh` is the one
   entry point. Run it before any push that touches a user-facing surface,
   and as the cut acceptance gate (LIVE-DEPLOY-PLAN Phase D). A red gate
   blocks the push the same way a red visibility matrix does.
3. **Done = green gates**, not a screenshot.

## The craft checklist (what the craft gate enforces)

- **Images**: every same-origin content image goes through the resizer
  (`/img.php?s=…&w=…`) — never a raw `/wp-content/uploads/` original — with
  `srcset` (≥2 widths, browser picks by slot × DPR) and `width`/`height`
  attrs (layout reservation). No image ships >1.7× its rendered pixels.
- **Scripts**: no eager heavyweights a viewer can't use — editors (quill),
  composers, admin tooling load on intent (click/focus), never for anon.
- **Weight**: a page's image transfer stays under budget (gate: 1.5 MB);
  total transfer under 2.5 MB.
- **Caching**: versioned static assets (`?v=`) ship long-lived
  `Cache-Control` (nginx d0457fc pattern).
- **Page furniture**: HTML that must not cache (front page) says so; pages
  carry exactly one h1; lazy-load below-the-fold media.

## Documented exceptions (decided, not missed)

The checklist above is the default. An image that departs from it gets an entry
HERE, with the measurement that forced it — so the next reader can tell a
considered trade from an oversight, and doesn't "fix" it back into a defect.

### YouTube thumbnails on discussion cards — `mqdefault`, no `srcset`

`bb-mirror/web/forums/_feed.php` renders a YouTube facade on a discussion card
whose body leads with a YouTube link. When the discussion has no attached photo
of its own, the thumb is `https://i.ytimg.com/vi/<id>/mqdefault.jpg` — 320×180,
cross-origin, **one width, no `srcset`**. That is deliberate:

- **Sharper costs a broken card.** The higher-resolution rungs do not always
  exist. Measured over all 13 YouTube discussions on dev2 (2026-07-30), `hq720`
  and `maxresdefault` return **404 on one of them**. A 404 in a `srcset`
  candidate is a *broken image*, not a graceful fallback — the browser does not
  retry another candidate. One reliably-soft card beats an occasionally-empty one.
- **The 4:3 rungs bake in letterboxing.** `hqdefault` (480×360) and `sddefault`
  (640×480) are 4:3 canvases with black bars burned into the pixels, so they put
  bars *inside* the cover box. `mqdefault` is the only always-present rung that is
  true 16:9.
- **Weight.** Those rungs run 58–206 KB each against a 1.5 MB PAGE-IMG-BUDGET,
  and one forum page can carry 7+ facades. They *do* count: `i.ytimg.com` sends
  `timing-allow-origin: *`, so Resource Timing reports a real `transferSize` and
  the gate's budget sees the bytes. `mqdefault` is 5–19 KB.

**Known cost: it is soft on a high-DPR phone.** ~1× on desktop, ~0.3× on a 3×
phone. That is known, not missed. Do not add `srcset` to "fix" it without first
re-measuring rung availability — the reason this is one width is availability,
not laziness.

The escape hatch already works: a discussion that carries its own attached photo
uses *that* as the thumb, and that path is fully compliant — resizer, `srcset`,
`width`/`height`.

### Discussion media in the weekly digest EMAIL — one width, no resizer, no `srcset`

`lg-weekly-digest` renders a discussion card's thumb from the BuddyBoss media
attached to the topic (`LG_WD_Query::topic_media_thumb`). It ships a **single
same-origin upload URL**, no `/img.php`, no `srcset`. All three departures are
forced by the medium:

- **The resizer is unreachable from an inbox.** Real mail clients fetch images
  with no cookies and `img.php` sits behind the dev gate, so routing sent mail
  through it breaks the image for every recipient. This is already settled and
  documented at `class-lg-wd-signup-page.php:223` — the resizer is for the
  on-page preview only.
- **One URL has to serve both layouts.** `srcset` support across mail clients is
  not dependable, and the card lays the thumb out in a 240px column that stacks
  to ~448px in the `<=480px` block of `templates/email.php`. 448 is therefore the
  slot the single URL must cover.

**The rule is width-driven, not name-driven**: pick the narrowest registered rung
whose width is `>= 448`, and prefer a real rung over an oversized original.
BuddyBoss's rungs are *bounding boxes*, so one name is a different width per
orientation — measured over 393 attachments on dev2:

| rung | portrait | landscape | square |
|---|---|---|---|
| `bb-media-activity-image` | 300 | 546 | 400 |
| `…album-directory-image-medium` | 401 | 755 | 534 |
| `bb-media-photos-popup-image` | 675 | 1195 | 900 |

Forum photos are mostly portrait phone photos, so a fixed *name* that reads well
for landscape (546px) hands portrait a 300px file into a 448px slot.

**Do not "fix" this by asking for `large` or `medium`.** WP's own sizes are never
generated for `bb_medias` uploads, so core falls all the way back to the ORIGINAL
— measured at 2545×1652 / **689 KB** on the topic in Ian's 2026-08-03 report.

MEASURED: 393 attachments on dev2 → **640px median, 67 KB mean**, against 900px /
439 KB for the originals. 29 live discussions from 2026-07-01 on → 633px median,
70 KB mean, **0** narrower than the slot, **0** above the 1.7× ceiling.

**Known cost: soft on a high-DPR phone**, and above 1.7× the 240px *desktop*
column. Both are accepted because a single URL must serve both layouts and mail
is read mostly on phones. Known, not missed.

### Not an exception: `img.php?s=bb_medias/…` without dims

`/hub/<forum>/` ships a topic cover through the resizer *without* `width`/`height`.
It is a per-attachment data gap, not a missing code path: on
`/hub/share-your-repair-content/`, **4 of the 5** `bb_medias` covers carry both
attrs and one (`bb_medias/2026/06/1000024061.jpg`) carries neither, so
`lg_cover_dims()` is resolving for most rows and coming back empty for that one.
All five `<img>` tags are **byte-identical between `main` and the
`discussion-card-video` branch** (verified 2026-07-30) — pre-existing, not
introduced by the facade. That page is not in the craft gate's
`PAGES`, so the gate has never seen it. Left alone on purpose (Ian, 2026-07-30):
adding the page would turn a *shared* gate red for a defect the lane did not
introduce and block every other lane. Tracked on the backlog instead; fix the
dims and add the page in the same change.

## The class that keeps getting through: presence is not reachability

Twice now Ian has reported a control he could not use while every check we had
was green, because the check asked whether the element **existed** and he was
asking whether he could **use** it:

1. **2026-07-30, the 🔔/✉ toggles.** 24 `[data-follow]` nodes in the DOM;
   `.fc-actions` wrapped to a second line and the card's `overflow: hidden` ate
   them on 17 of 18 cards. Gated by `follow-visible-gate.py`.
2. **2026-07-31, Sign in on the anon front page.** `wp-login` ×2 in the served
   HTML; `.lg-chrome__signin` was `display:none` from 820px down while its
   stand-in only appeared from 640px down, so **641–820px had no way in at all**
   — and it was live. Gated by `anon-signin-reachable-gate.py`.

Neither was covered, moved or missing from the markup. Both were rendered and
then hidden by CSS resolved across *two* stylesheets, which no static check of
either file can see. So:

- **Assert the goal, not the element.** "A signed-out visitor can reach Sign in"
  survives a redesign that moves the control; "`.lg-chrome__signin` is visible"
  gets edited away by the change that breaks the user.
- **Measure paint and hit-test, in a real engine.** Styled + sized + in-viewport
  + `elementFromPoint` at its own centre returns it. `querySelectorAll().length`
  is not evidence and must never be reported as if it were.
- **Bracket every breakpoint the behaviour crosses.** 1440 and 390 were BOTH
  green on the day the whole band between them was dead. Sampling "desktop and
  phone" is how a responsive lockout hides.

## Existing gates

| # | Gate | What it guards | Needs |
|---|---|---|---|
| 1 | `profile-app/bin/visibility-matrix.php` | the entire visibility model (67 asserts) | — |
| 2 | `tools/gates/craft-gate.py` | the checklist above, over real pages as anon+member | **a browser on CDP :9222** |
| 3 | `tools/gates/infra-sec-gate.sh` | cookie auth / source disclosure / cdp exposure | loopback |
| 4 | `tools/gates/hub-content-paragraph-gate.sh` | `content_html` keeps its paragraph breaks | — |
| 5 | `tools/gates/looth-auth-issue-gate.sh` | non-REST mint bounce (recurs on every DB reload) | loopback |
| 6 | `tools/gates/event-date-tz-gate.sh` | a UTC "today" must not judge a site-local stored date | — |
| 7 | `tools/gates/events-tap-navigates-gate.sh` | an events tap navigates; the retired mobile modal stays retired | — |
| 8 | `tools/gates/composer-topic-meta-test.js` | forum picker cloning + tags on the composer | node |
| 9 | `tools/gates/author-socials-live-gate.sh` | bylines RESOLVE socials from the profile store, never mirror ACF | loopback |
| 10 | `tools/gates/react-types-cover-standalone-gate.sh` | a rendered react button's type is one the endpoint ACCEPTS | loopback |
| 11 | `tools/gates/shop-planner-url-gate.sh` | `/shop-layout-planner/` still answers 200 **and still mounts the planner** | loopback, or `ssh live-ro` with `--live` |
| 12 | `tools/gates/anon-signin-reachable-gate.py` | an ANON visitor can **reach** Sign in in ≤1 tap at every width — visible *and* hit-tested, not merely in the DOM | **a browser on CDP :9222** |
| 13 | `tools/gates/follow-digest-gate.py` | **PROMOTED into the sequence by this merge** (it was HELD OUT and red on purpose while the sender did not exist). The follow-digest flag's OFF state sends NOTHING: no cron armed, no cadence stored, the suppression filter proven to be the identity function, and the resolver proven to return zero recipients | wp-cli |
| 14 | `tools/gates/dev-files-anon-unreachable-gate.py` | lane/dev tooling inside a deployed plugin tree is **unreachable by an anonymous request** — 404/403, not "absent from my checkout" | starts its own **gate-free** nginx (dev2's armed gate would 403 everything and false-pass) |
| 15 | `tools/gates/cadence-control-gate.py` | the account-level email-frequency control is **PRESENT when `LG_FOLLOWING_CADENCE` is on and ABSENT when it is off** — and still hidden when the flag is on but the sender would not serve that member. Also: `hidden` actually hides (the UA rule loses to `display:flex`), and the page's only cadence write is follow-digest's transport, never usermeta and never `follow.php` | needs the lane preview up: `sudo tools/preview/lane-preview.sh up account-following` |
| 16 | `tools/gates/group-mail-dead-gate.py` | BuddyBoss's **group** mailer stays dead, and the thing keeping it dead is an **empty list nobody declared load-bearing** — `lg_discussion_group_allow()` defaults to `[]` inside `lg-discussion-group-gate.php`, written for Local Looths, and it is the only reason a new discussion in a group forum mails nobody. Post-sweep the kept groups still hold 3,735 armed subscriptions (Tri State Looths: 853), so adding one slug for entirely correct reasons turns on a mass mailer while every other gate stays green. Also holds the sole-claimant assertion on `bb_send_forums_subscribed_reply_email_notifications`, so a second hook cannot silently undo the follow roundup's suppression. Does not forbid the change — makes it impossible to make BY ACCIDENT; see `docs/ONE-MAILER-SCOPE.md` §4 | **none** — pure static analysis, so it cannot go DEAD for environmental reasons |
| 17 | `tools/gates/subscription-preserved-gate.py` | **participation must never silently unsubscribe you.** BuddyBoss reads a MISSING subscription field as "the member unticked the box" and deletes their subscription; our composer replaced BB's form in June 2026 and sends no such field, so posting a reply — or editing a discussion — destroyed the author's subscription on a save that succeeded. Same class as edit-post-parity. Runs its probe in THREE processes: repair absent (**must reproduce the loss** — the negative control, without which a green is unearned and the gate reports DEAD), flag ON (must preserve), flag OFF (must match absent exactly). Covers `/reply`, `/reply/<id>` and `/topics/<id>`, which resolve "whose subscription" differently | `wp-cli` as `looth-dev`, and a published topic on the box |
| 18 | `tools/gates/post-follow-controls-gate.py` | **ruling 6's defaults: 🔔 bell TICKED, ✉ email UNTICKED.** A post at the defaults must create the `forums.topic_follow` row and must **NOT** create the `type='topic'` roundup row — "email unticked" is the difference between a member who chose email and one signed up by posting, and it inverts silently if a composer ever passes `subscribe` by reflex. Four modes (absent / off / on-default / on-email); OFF is asserted against **absent**, not against "nothing written", because `subscribe` is BuddyBoss's own param which our flag neither gates nor should. Does not assert bell *delivery* — that is the notification bridge's contract | `wp-cli` as `looth-dev`, a published topic, and Postgres `forums.topic_follow` |
| 19 | `tools/gates/social-actions-wired-gate.py` | **a rendered control CARRIES ITS BEHAVIOUR — the UI-lies class.** Backlog 4.4 + 4.3 (Ian 8/8): DMs dead from the mobile profile tray and the 3-dots menu dead, ONE cause — profile-app renders the social widget and shipped its behaviour as an inline `<script>`, and the tray RELOCATES that markup into another page where an inline script can never run. Seven controls rendered, none wired. **Why a gate and not a test:** every natural assertion here is about PRESENCE and the defect lives entirely in ABSENCE — the buttons are in the DOM, styled and hit-testable, so a presence check is GREEN on the broken state. It asserts the PAIRING (markup present ⇒ its wiring is present and reachable) per flag state, off the rendered widget, and pairs every absence claim with a liveness claim, because the widget renders `''` for an anonymous viewer and for your own profile — against which every "no stamp" assertion is vacuously true. It also diffs the two live copies of the wiring (the OFF heredoc vs the ON asset) and goes RED on drift, which is the only thing making a flagged migration with two copies safe | *(see the gate header — owned by the social-actions lane)* |
| 20 | `tools/gates/hub-topic-landing-gate.py` | **a sitemapped discussion URL lands on THE HUB with its modal open, and carries its text in the SERVER HTML.** e9ddc28 listed 1,352 discussions in the sitemap, every URL the permalink `/hub/<forum>/<topic>/` — which rendered the legacy standalone page, so the sitemap turned a fallback into Google's front door (Ian: *"does this look like the fucking hub with a fucking modal open?"*). **TWO HALVES THAT FAIL INDEPENDENTLY**, which is the whole reason both are gated: the obvious fix — route that URL at the feed and let forums.js §4f open the modal — gives Ian the right picture and silently destroys the SEO half, because §4f's cold path fetches the body AFTER load, so a crawler reads an empty modal and **nothing looks wrong to anyone running JS**. A: OP body + reply text in `curl`, no JS, no cookies, title in `<title>` — this half also guards the REPLACEMENT, since the legacy page did server-render its content and that must not regress on the way out. B: the hub feed grid with a modal already open on it. Plus the visibility masks **differentially against the fragment API** (audit H6 was that leak on this exact permalink) and a hidden-forum topic 404ing through **both** the landing route and the fragment API — the fixture self-guards by checking the sitemap first, since a 404 assertion against a public topic is a green that measures nothing. READS the flag state rather than hardcoding it; `LG_TL_REQUIRE_ON=1` promotes "legacy layout served" to a finding (throw it when the default flips), `LG_TL_PREFIX` gates a lane preview instead | **none** — curl + the dev gate cookie, ground truth self-sourced from `/bb-mirror-api/v0/topic` and `?replies=`, so no DB role |
| 21 | `tools/gates/notif-read-seen-gate.py` | marking notifications read is scoped to **what the member actually SAW** — and, the decisive half, **rows they did NOT see are STILL UNREAD**. Also: owner scoping (a foreign id marks nothing), `markAllRead` still sweeps, the recap keeps unseen rows and is emptied by a sweep, and `applySeenRead` measured under **BOTH** values of `read_seen_only` | `sudo -u profile-app php` + the `profile_app` DB. No browser: it runs in a transaction that is **never committed** |
| 30 | `tools/gates/legacy-url-redirect-gate.py` | **a legacy forum URL reaches its OWN canonical permalink, not a generic landing.** Ian: *"are all our old links going to work in legacy posts etc?"* The obvious check is GREEN ON THE BROKEN STATE — every legacy shape already 301s *somewhere*, so "does it redirect?" proves nothing. Google reads a **many-to-one** 301 as a soft 404: no authority transferred, no consolidation, and the old URL keeps its own index entry, which is the reported symptom. (A blanket `Disallow` makes it worse — a disallowed URL cannot be recrawled, so it stays indexed with no content.) So the gate asserts the **destination, per URL**, resolved from Postgres rather than hardcoded. Also asserts the two failures a redirect map makes in opposite directions: a reply in a **non-public** forum must NOT resolve (a redirect target is an existence oracle), and an **unknown** id must still land somewhere rather than 404 a crawler. `topic-tag` and `page/<n>` deliberately keep landing on bare `/hub/` and are not asserted — a tag has no indexable per-URL target, because the hub's tag view is `/hub/?…` and live robots.txt `Disallow: /hub/?`, so a 301 into it would aim Google at a URL it is forbidden to fetch. That is a recorded decision, not an oversight (`docs/LEGACY-URL-REDIRECT-STATE.md`) | `sudo -u postgres psql` for ground truth; curl + the dev gate cookie |
| 31 | `tools/gates/hub-category-page-gate.py` | **`/hub/<category>/` renders THE HUB and declares which URL it is.** Ian, 2026-08-11: the category pages were never rebuilt. One premise corrected before the gate was written — the page is **not** a wholesale legacy view, it already renders the new feed cards; what it *also* renders, and neither `/hub/` nor a topic landing does, is the legacy **left category tree** (`nav-tree` 84 on `/hub/general/`, 0 on `/hub/`, 0 on a landing). Same condition as the rail: `_chrome.php` shows the legacy nav only when `__bb_hub_rail` is empty and `_feed.php` sets it **only on the unscoped branch**. **Presence AND absence** — "no nav-tree" alone is satisfied by a blank page, a 500 or a redirect, so every rail check is paired with "the hub rail is really there". Two halves with different lifecycles, gated differently on purpose: the **canonical** is not member-visible so it ships unflagged and is asserted unconditionally; the **rail** is member-visible so it ships behind `LG_HUB_CATEGORY_RAIL` (OFF default) and the gate READS which rail is served, reporting OFF as a state rather than a finding. `LG_HC_REQUIRE_RAIL=1` demands it | `sudo -u postgres psql` for the category list; curl + the dev gate cookie |
| 32 | `tools/gates/stale-page-redirect-gate.py` | **retired pages 301 per-URL to a destination that ANSWERS.** Ian's 2026-08-11 rulings: `/shop-organisation/` → `/hub/shop-organisation/`, `/featured-content/` → `/hub/`, `/merch/` → loothtool.com, **`/shop/` left alone** (alive, linked from homepage/front/hub/events). Per-URL and never blanket, because Google reads many-to-one as a soft 404. **It asserts the destination RESPONDS, not merely that a Location header is emitted** — and that is the whole reason it exists rather than the conf being "obviously fine". `/merch/` points at a domain we do **not** control, and measuring before shipping found `https://loothtool.com/` = 200 but `https://www.loothtool.com/` = **522** (Cloudflare origin timeout). The www form is the one most people would type into a conf, and it would have 301'd every visitor and Googlebot into a dead end. An external target can also rot later with nobody touching our code — a conf comment cannot notice that; this can | curl + the dev gate cookie, plus one unauthenticated external fetch |

> **Rows 19 and 22 are now filled in** (hub-seo-landing, 2026-08-12). This note previously said both were missing and "left for their owners to describe"; they had been open long enough that later lanes numbered past them, which is exactly how a table with holes becomes the thing people mint duplicate numbers from — three collisions had already merged cleanly by the time it was noticed. Both rows now TRANSCRIBE each gate's own header rather than guessing at its rationale, so they are the owner's words even though the owner did not type them here. `run-all.sh` remains the authoritative roster, and every row number in this table is now aligned to the `=== GATE n/N ===` banner of the gate it describes.
| 26 | `tools/gates/notif-renderer-parity-gate.sh` | every type the store can HOLD has a **sentence on both bells**. The required set is read from `Notifications.php` (TYPES + HUB_TYPES), not by diffing the two renderers — that would go green the day a type is added to neither. `forum.followed_topic` shipped 2026-07-28 with a desktop sentence and none on mobile, whose `default:` renders a bare actor name; eleven days on live, and both rows it ever produced went to real members, one of them Ian | none (source parity); `--prove` red-firsts against the real pre-fix file at `862feb9` |
| 27 | `tools/gates/notif-dismiss-gate.sh` | the bell's **delete/dismiss contract** and **leg 4's follow stores**, per flag state. Today's `deleteAll` really destroys; dismissal keeps the row, hides it, and still lets the NEXT reply through; the `ON CONFLICT` arbiter matches the index that actually exists in all four pairings *including the failing one*; and leg 4 honours `status=0` so the 8/8 group-sub sweep cannot be undone through the bell | passwordless sudo to `profile-app` and `looth-dev` (peer auth — neither database is reachable as `ubuntu`); reports **CANNOT RUN**, never RED, without it |
| 28 | `tools/gates/notif-bell-delivery-gate.sh` | **a FOLLOW actually produces a BELL ROW**, end to end. IAN-RULINGS §6 assigns this contract to the bridge, not the composer, and Ian made the bell the DEFAULT follow channel — so gate 18 (composer writes `topic_follow`), the followers proof (leg 4 reads it) and gate 20 (a row renders) were three green gates either side of a seam nobody crossed. Crosses two Postgres databases, two OS users and an HTTP hop, so it cannot be a unit test. Writes real rows on dev2, cleans up under a `trap`, and **asserts the cleanup**; refuses to run anywhere but dev2 (keyed on `LG_PUBLIC_HOST`, since `LG_ENV` says "dev2" on live too). Found a real defect on its first run — see the Host note below | passwordless sudo to `looth-dev` **and** `profile-app`; dev2 only |
| 29 | `tools/gates/notif-endpoint-gate.sh` | the bell's **DELETE endpoint honours the flag, per state** — the model proof covers `delete()`/`dismiss()`, but what runs when Ian flips `dismiss_instead_of_delete` is the ENDPOINT, and a mis-wired branch there means the old destructive behaviour (or a 500) on a live bell. Issues REAL authenticated requests against the **working tree's** copy, because curl would test the serving checkout i.e. `main`. Asserts the response is **byte-identical in both states**, so no surface breaks on the flip, and that a second dismiss still reads `not_found` rather than 500. Phase 0 is its liveness control: a GET must return the payload AND a bogus token must be refused. Flips a tracked file by **snapshot-and-restore with the md5 asserted**, never `git checkout --` | passwordless sudo to `profile-app`; the migration applied (else phase 2 reports CANNOT RUN) |

> **Two rows both said "13" until 2026-08-01** — dev-files-anon and follow-digest,
> minted by different lanes in the same window. Same collision as the "9/9" one in
> `run-all.sh`. **Mint a gate number from `origin/main`'s count, never your
> branch's**, and re-check it immediately before you push.

**Gate 15 asserts an ABSENCE, which is the half this codebase kept missing.** Gates
assert what should be PRESENT; all six defects that reached Ian's phone through a
green suite were things that should have been absent and were not. A control that is
meant to be invisible is precisely what nobody writes an assertion for. It proves
both directions by diffing two surfaces that really exist — the live page and the
lane preview — and its `--prove` pass runs every predicate against the surface it was
written to REJECT. That pass immediately caught two of the gate's own assertions
being worthless, which is the argument for having it.

**Gate 21 is the second time the SAME absent-half class cost a member their digest**,
which is exactly the trigger this document defines for minting a gate. The class is
"a read that was never a read": on 2026-07-29 the recap's two registers disagreed
about what "still outstanding" meant, and on 2026-08-07 the mobile sheet's 700ms
timer marked a member's whole store read after rendering eight rows. In both cases
"the rows the member saw are read" was true and green throughout; nobody had written
down "the rows the member did NOT see are still unread". Its red-first pass
(`lib/notif-read-seen-redfirst.sh`, ten inversions) caught the gate matching its OWN
COMMENT PROSE — `limit=200` appears in both the fetch and the comment explaining it —
so the check now strips comments before reading code. Full account:
`docs/RECAP-READ-TIMER.md`.

**Gate 13 is written BEFORE the feature it guards, and that is the point.** Every
other gate here encodes a defect class found twice. Email is unrecallable, so the
second occurrence is not survivable — this one is gated before the first. It was held
out of the runner only because a deliberately-red gate in the shared sequence would
block every other lane's push for a feature that does not exist; **the commit that
defines `LG_FOLLOW_DIGEST_ENABLED` must promote it into the sequence in the same
commit.** A gate guarding an unrecallable channel that is never promoted is worse
than no gate, because it reads as covered.

It also carries the answer to the trap that makes most OFF-state gates worthless:
**an absence assertion passes vacuously whenever the machinery that would produce the
presence is itself missing.** "No cron event is scheduled" is true on a box with no
WordPress at all. So every absence assertion in gate 13 is paired with a *liveness*
assertion proving the mechanism exists and is reachable first. Absence without
liveness is not evidence.

All thirteen run from `tools/gates/run-all.sh`. Two more are deliberately HELD OUT of
the runner because they pass standalone but flake red in sequence (CDP under load,
and loopback `/whoami` tripping infra's `limit_req` zone) — see the note at the
foot of `run-all.sh` for how to run `forum-visibility-gate.sh` and
`editor-rail-reachable-gate.sh` by hand.

### A synthetic click cannot perform the gesture that breaks

`tools/gates/messages-longpress-react-gate.py` (HELD OUT — it needs a proxy with member
cookies and a member who is a participant in a thread that has messages; red/green-proven
both ways on 2026-07-31) encodes the class that has now reached Ian **three** times:

1. `mobile-hub.js` `holdTargetFrom()` claiming the 🔔/✉ follow toggles (`8405055`);
2. the same shape again on the consolidated Follow pill;
3. the messages action sheet closing on **its own trailing click** — which made
   reacting to a message on a phone impossible, not merely awkward.

The common cause is not carelessness, it is the harness: **a CDP/Playwright `.click()`
dispatches in single-digit milliseconds and can never cross a 380/480ms hold threshold.**
Every automated tap ever run against these controls took a path a human finger cannot
take, so the defect was not missed — it was *unreachable*. Press with `touchStart` → a
real wall-clock sleep → `touchEnd`, and assert the STORE, not the pixel.

It also carries the absence lesson in its sharpest form: on the broken build the sheet
DID open, and a presence check said so (that assertion passes in the red run). The
load-bearing assertion is that it is **still** open once the finger lifts.

### "Bad z-index" is usually a CLIP, and both look identical from outside

`tools/gates/react-controls-reachable-gate.py` (HELD OUT; red/green-proven 2026-07-31)
guards two live defects that were each **present, correctly styled, and impossible to
use** — the same class as the Sign-in lockout above, found twice more in one day:

* **The card react palette** was `position:absolute; bottom:calc(100% - 2px)`, i.e. it
  opened *upward, entirely outside* `.fcr` — and `765dbc3` had put `overflow:hidden` on
  `.fcr` to let the action row shrink. Its own offset parent clipped it away. Ian
  reported it as *"the react button modal on all cards has a bad z"*, and that reading is
  completely reasonable: **a clipped popover and one painted behind something look the
  same.** `z-index: 20` was being honoured the entire time. When a popover "has a bad z",
  check `overflow` on every ancestor up to the stacking context *before* touching
  `z-index` — and fix it by moving the clip onto the in-flow child that actually needed
  clipping, so the layout guarantee the `overflow:hidden` was bought for survives.
* **The messages React control** was revealed by `:hover` alone, so on a touchscreen at
  ≥641px — where the *desktop* modal renders — no hover event ever fires and the only
  route to reacting was unreachable. Gate the **hover capability** (`hover: none`), never
  a width breakpoint: absence of hover is the cause, and a large touchscreen is exactly
  what a width query misses.

In both RED runs a presence-style assertion **passes** ("the palette is open and has a
real box"). `elementFromPoint` is the only check that separates *painted and reachable*
from *present*.

**A gate that CANNOT RUN is not a gate that passed, and not one that failed.**
Gate 2 drives a real Chrome; with no engine on :9222 it reports one `GATE-ERROR`
per page and exits 1, which is indistinguishable from finding real violations —
it spent weeks looking red while it was in fact dead. Treat "no engine" as *no
verdict* and say so, rather than reporting a pass or a failure it never reached.
`origin/events-fix-verify` carries the three-state fix (exit 2 = CANNOT RUN);
until that lands, check for the engine before you believe gate 2 either way.

## Why this works when 13 fixes didn't

The visibility model stopped leaking the week it became ONE function plus a
test that fails. Nothing else in this project has ever stopped a recurrence.
This document exists to make that the default move, not the last resort.
| 23 | `tools/gates/sw-fetch-bounded-gate.py` | a service-worker navigation **always SETTLES** — the decisive input is a `fetch` that neither resolves nor rejects, which is what Ian's spinning tab actually was. Also: the one retry is not traded away for the deadline, dev-gated paths are **not intercepted at all**, a gate 403 becomes a **claim prompt** instead of raw nginx output, and a 403 shell asset no longer kills the whole install. Asserts the harness's OWN fidelity first — a `vm` context lacks `setTimeout`/`URL`, and without them cases pass down paths no browser takes | node. No browser, no nginx, no DB |
| 25 | `tools/gates/mirror-reconcile-poison-gate.php` | **one bad row must not wedge the mirror sweep.** `bin/reconcile.php` is the only safety net under a fire-and-forget realtime sync, and it walked its delta with a bare `foreach` while writing the `last_reconcile_at` bookmark *after* the walk. A rewound bookmark pulled reply 71720 (its `_bbp_topic_id` points at an **attachment**) into the window on 2026-07-29 23:20 UTC; the FK threw, nothing caught it, and because the bookmark write was never reached the next run rewalked the same window and died the same way — 3,084 times over 11 days. Cost: 16% of replies posted in that window never reached the hub, each rescued only when its author happened to **edit** the post, worst case 2d22h. Asserts the walk survives a throwing row *and records it*, that reconcile goes through `bb_mirror_walk_ids` (lexed, not grepped — a comment quoting the old shape must not satisfy it), and that no tail step can skip the bookmark. Paired with the tripwire `tools/mirror-sync/watch-mirror-sync.sh`, because the gate proves the code is right and only the watch proves live is | **none** — static + in-process, so it cannot go DEAD for environmental reasons |
| 22 | `tools/gates/back-pill-navigates-gate.py` | **a rendered NAV control must actually navigate** (backlog 3.8, Ian ruled option D 2026-08-09). Same class as row 19 one step further along: 19 guards markup that arrives without its behaviour, this guards a control that is present, IS wired, and still takes you nowhere — a dead href, a wrong target, a listener that `preventDefault`s and forgets to navigate. Ian's 3.8 complaint was a back button that existed and could not be reached; shipping one that is reachable and inert would be the same sentence with a new subject. Also asserts the flag's OFF state is a real no-op | *(see the gate header — owned by the mobile-bugs lane)* |
| 24 | `tools/gates/sw-no-offline-shell-gate.py` | **while the origin is UP, a navigation through the service worker must render THE REAL PAGE, never the offline shell.** Encoded because the class bit three times (Ian 2026-06-25 / 08-09 / 08-11): blank spin, then "You're offline", on a URL that answers 200 server-side. Deliberately split from `sw-fetch-bounded-gate`, which uses a stubbed worker in node to ask "does the handler SETTLE when the network never answers?" — a hung fetch is something a browser cannot stage. This one is the opposite half: a REAL browser, a REAL registered service worker, a REAL page | a browser on CDP |
| 37 | `tools/gates/guitardle-claim-gate.py` | **the daily Guitardle attempt is ONE allowance per MEMBER ACCOUNT, claimed at the START of a game.** Ian 2026-08-14: *"fixing the guitardle giving more chances on different devices"* — the Weekly Top 5 has claimable spots, so this is fairness, not a quirk. **The obvious gate would have been GREEN on the defect.** "A member cannot record two results in one day" was ALREADY TRUE: `UNIQUE (wp_user_id, play_date)` + `ON CONFLICT DO NOTHING` has held since June, and 7 days of live traffic produced 93 successful POSTs and exactly 93 rows — zero duplicate writes. The leak was never the RECORDING, it was **ABANDONING**: nothing was written until `handleWin`/`handleLoss`, and the mid-game snapshot lived in `localStorage`, i.e. per DEVICE. So a player could reveal letters until the phrase was readable, close the tab (no row, no lock, **no trace**), reopen in incognito and solve it cold in one move for 10 points — 20 with hardcore. One POST, one row, indistinguishable from honest play. Live evidence: WP 197 was 27 plays / 27 wins / every win ≤4 moves / **7 of them in a single move**, against a field whose best average is 4.1. So the assertion that bites is *a SECOND start-claim for the same (member, day) must claim NOTHING* — proven red against `origin/main`'s endpoint, which `LG_GDLE_ENDPOINT` re-runs. Drives the **working tree's** endpoint via `guitardle-claim-probe.php` with a real WP session cookie, because curl would reach `/srv` i.e. the serving checkout and test `main`. Phase 0 proves the door answers as a known member before any "no row was written" is trusted; the flag-OFF promo render is compared **byte-for-byte against `origin/main`** (rendered from the same directory so `__DIR__` and the `?v=` cache-bust match). Also gates the two logged-out copy rulings and the How-to-Play overlay, which had to be **built** — the served game had no rules surface at all and Hardcore's only explanation was a `title=` tooltip, invisible on touch | passwordless sudo to `looth-dev` **and** `postgres`; wp-cli; the migration applied (else CANNOT RUN); creates/reuses a `gdle_gate_probe` subscriber and deletes every row it writes, asserted |
| 40 | `tools/gates/guitardle-nonce-retry-gate.py` + `guitardle-retry-harness.js` | **a finished Guitardle result must not be LOST to an expired nonce.** Backlog 24. Measured on live over 7 days: 101 finished games POSTed, **8 came back 403**, from 8 distinct IPs across 6 days. A WP nonce lives ~12h and the game sits in a front-page iframe people leave open, so a tab opened last night and played this morning carries a dead one — and every call ended in `.catch(() => {})`, so the player saw their win card, believed it counted, and it never reached the board. Roughly **1 game in 12**, landing hardest on the members who play most: the opposite fairness failure to the device-hopping one gate 37 covers. The fix routes **all three** nonce-bearing calls through one `postWithNonce()`, which mattered more than the ticket said — a **start-claim** lost to a stale nonce means the day is never claimed, so gate 37's allowance fix silently does not apply to that game, an invisible hole *inside* the previous fix. **TWO HALVES, because neither proves anything alone**: the SERVER half shows a stale nonce really is answered `bad_csrf` and records nothing, and that the same result resent with a fresh nonce records *with its real score*; the CLIENT half **slices the shipped `refreshNonce`/`postWithNonce` straight out of `game.js`** and evaluates that source against a stubbed network. Deliberately **not** a browser test — a browser dependency would flake on a 2-core box and a DEAD gate blocks every lane, which is worse than the coverage it buys; and slicing the real source rather than re-implementing it means the harness cannot drift from what ships, reporting **CANNOT RUN** if those functions are ever renamed instead of passing vacuously. Red-first with two *valid, still-parsing* mutations — retry-without-refreshing (resends the dead nonce) and retry-in-a-loop — each caught by the assertion written for it. Also verified end-to-end in a real browser through nginx → FPM → WP → Postgres against a genuinely poisoned nonce: `POST 403 → GET 200 → POST 200`, row landed with the right score | passwordless sudo to `looth-dev` **and** `postgres`; wp-cli; **node** (the retry harness); reuses the `gdle_gate_probe` subscriber and deletes every row it writes, asserted |
| 41 | `tools/gates/guitardle-serverplay-gate.py` | **the Guitardle leaderboard's inputs are what the SERVER watched, and the answer never reaches the client.** Backlog 25, option A. `moves`/`won`/`hardcore` all came out of the POST body and hardcore **doubles** points, so anyone holding their own nonce could post a 20-point day — but server-side scoring **alone would not have fixed it**, because the answer was public. Not merely "the CSV is on a public URL": measured in a browser, the legacy board put the phrase **in the DOM** — 18 tiles, all 18 carrying `data-letter`, so `"POLYURETHANEFINISH"` read straight off the *blank* tiles. Open the inspector, read the answer. A player who does that solves in one move and an honest server scores it 20; quantified on live, that is **140 points a week against an actual weekly leader on 62** — permanent first place, on a board with claimable spots. **The assertion most likely to save someone is not about the exploit.** Phase 2: the server now carries its own copy of `loadPhrase()`, and if it ever drifts from `game.js` the server judges a **different puzzle than the player saw** — *every honest player loses*, which is worse than the hole being closed. So the phrase id and letters are recomputed **independently in Python** straight from the raw assets and compared against the PHP resolver, on **both** audience tracks: two implementations, one answer, every run. The rest asserts OFF is a no-op with the legacy finish still working; that the server counts the moves and judges the guess (a body claiming `moves:1, won:true, hardcore:true` is simply ignored); and that **both** doors which would make all of it pointless are shut — the legacy finish, and `save`, which let a client write the very `revealed`/`purchased` sets moves are counted from. Red-first three ways, each restored from snapshot with the md5 asserted: against `origin/main`'s endpoint (six ON-half assertions fail), re-opening the legacy finish door, and making the server-driven renderer write `data-letter`. Takes a **per-run probe identity** — see the note on gate 37 | passwordless sudo to `looth-dev` **and** `postgres`; wp-cli; the migration applied; creates and **deletes** its own per-run probe account, asserted |
| 42 | `tools/gates/guitardle-daypuzzle-gate.py` | **the puzzle LIBRARY and the SEQUENCE never reach a browser.** Backlog 26. Gate 41 stopped the phrase reaching server-driven *members*, but could not remove `assets/guitardle_phrases.csv` and `assets/sequence.json`, because the **logged-out** game still fetches them to draw its board and judge its own guess. Those two files are 285 phrases plus the **fixed order**, so today, every future day, *and the member track* are computable by anyone who opens them — quantified on live at **~140 points a week against a real weekly leader on 62**. It deliberately does **not** claim a logged-out board stops holding its own day's phrase: it must, it judges its own guess, and an anon result is never recorded (`uid<=0` is rejected). What goes is the library and the order. **Two assertions earn this gate.** There is deliberately **no `aud=` parameter** on the day endpoint, and the gate goes hunting for one anyway (`?aud=m`, `?aud=member`, `?member=1`, `?track=member`, `?is_member=true`) — serving the member track there would restore gate 41's hole through a door needing **no login at all**. And phase 5 asserts the **assets are STILL PRESENT**: this is stage ONE of a two-stage deploy, and pulling them before both flags are on everywhere is a *blank* board for legacy members, not a degraded one — so "tidying them away" fails here rather than on Ian's phone. **Two guardrails keeper added on review, both red-first proven.** (a) The endpoint accepts **no parameter of any kind** — not a date, not an index, not an audience; the *server's own clock* picks the day. An earlier draft accepted `?local_date` with the same ±1 clamp the score API uses, which felt consistent and was still an **oracle**: a read-only endpoint that answers for a day you name rebuilds the key on a delay, one query at a time, so there is no window small enough to be safe and therefore no window. The gate probes thirteen shapes (`local_date`, `date`, `day`, `d`, `index`, `i`, `offset`, `n`, `phrase_id`, `id`, `seq`, …) and asserts the endpoint reads no superglobal at all. Stated cost, not hidden: the logged-out day now turns over at the **server's** midnight, so a US player sees it change in the evening — if that ever matters the fix is a site-timezone constant *in the endpoint*, never a parameter. (b) **No cache may outlive the day**: on live this sits behind Cloudflare, and an unauthenticated GET cached across midnight either serves yesterday's phrase to everyone or pre-bakes today's for anyone who asks early — so `Cache-Control: no-store` plus `CDN-Cache-Control` (which Cloudflare honours in preference) are both asserted from the live response headers. Also asserts that the `.php` is in the **explicit** nginx location list, since a `.php` with no location of its own falls through the parent alias and is served as readable source. Serves the endpoint over real HTTP via `php -S` rather than `include`ing it — an endpoint that works as an include and 500s over HTTP is a class this would otherwise miss. Red-first three ways: adding an `aud` parameter, having the day loader grab the library, and dropping the `.php` from the location list | `php` only — no WordPress, no Postgres, no browser |
| 43 | `tools/gates/sw-admin-bypass-gate.py` | **wp-admin must never render the PWA offline shell.** Backlog 28. Ian, 2026-08-15: a slow admin click showed him the game-like *"You're offline"* page on a **dashboard** URL. He was not offline and it is not our surface — wp-admin should fail the way wp-admin fails, with the browser's own network error, so the symptom names the real problem instead of blaming the network. **Why the worker sees admin URLs at all**, which the ticket did not say: `pwa.js` registers with `scope: '/'`, so it controls **every** same-origin navigation; wp-admin never loads `pwa.js` and does not need to, because a registration made on a member page covers the whole origin. Both navigation branches in `sw.js` end at `caches.match('/offline.html')`. **The assertion that earns this gate runs every check on the LIVE hostname as well as dev2.** `sw.js` already had a bypass list and the obvious fix was to add `/wp-admin` to it — a fix that is **inert on live**, because `isBypassed()` opens with `if (!RESILIENT || !IS_DEV2) return false`, and inert again whenever the pwa-sw flag is off. A dev2-only gate would have blessed a change that does nothing where the members are. It is paired with the opposite half: a **member page under the same failure must still get the shell**, or "intercept nothing" scores full marks on phase 1 by breaking offline support for everybody. Also asserts precision — `/wp-admin`, `/wp-admin/`, `/wp-admin/index.php`, a query variant and `/wp-login.php` bypass, while `/wp-adminfoo`, `/wp-admin-ish/`, `/hub/wp-admin/`, `/hub/` and `/members/` must not; the first two were **measured** as swallowed by a bare `indexOf` prefix before the matcher was tightened — and that cross-origin is left alone. Driven through `lib/sw-handler-harness.js`, which loads the **real** `sw.js` under a stubbed worker global, so it tests the shipped file and needs no browser (a service worker is its own target, and a CDP network stall lands on the page's) | `node` only — no WordPress, no Postgres, no browser |
| 45 | `tools/gates/theme-independent-contrast-gate.py` | **a contrast defect that fails in BOTH themes, which neither a light pass nor a dark pass will ever be blamed for.** Number assigned by keeper 2026-08-15 and verified free on `origin/main` before use — lanes do not mint gate numbers. Grew out of gate 36's sweep, which kept surfacing findings that were not dark defects at all: a near-white foreground on a **hardcoded** mid-tone fill with no dark variant, rendering identically whichever theme is picked. Three instances, same instrument, same eyes: `bb_mirror_avatar()`'s `crc32($slug)` palette written as an INLINE style (white text fails on 3 of 8 — `#c66845` 3.84:1, `#87986a` 3.12:1, `#a0714f` 4.23:1); `loothalong.js:170`'s crew avatars under a near-white icon (`#b98a3e` = **2.85:1** against the 3:1 icon bar, and its selector prefix is `'#' + EL_ID` — an id, *not* a theme scope, which is what proves it theme-independent); and the PWA install button's LIGHT half (3.12:1) whose dark half gate 36's wave fixed. By the law above a class found twice becomes a gate — this reached three, and fixing any of it behind a flag named "dark" would mislabel it permanently for the next reader, which is why it is a second gate and not a wider first one. **Mechanically**: every surface is measured TWICE, resolved light and resolved dark, with the SAME probe gate 36 and the sweep use, and an element counts only when it fails in BOTH, matched by selector — deliberately the weaker of the two available definitions, because a defect failing in both themes for *different* reasons is still one no theme pass catches. Where the colours are identical across both passes it reports `HARDCODED`, the stronger signal. Owns the six anon surfaces gate 36 does not, so the two never both own a page. Liveness is asserted on BOTH sides: a page stuck in the wrong theme would make the intersection one theme measured twice and green for a reason that isn't true. A **ratchet**, same as 36. Its intersection logic is red-fired by `_both_theme_selftest()` before any browser opens, and that self-test was itself mutation-tested against three realistic failure modes — including the silent-green one where the intersection always returns `[]` | chrome-dev on 127.0.0.1:9222, `tools/gates/gate-env.sh` |
| 46 | `tools/gates/compose-media-gate.py` | **an abandoned compose leaves ZERO orphans, and each post keeps its own library.** Ian 2026-08-15. **Measured before it existed:** uploading through the real picker and abandoning left an attachment with `post_parent 0`, the file on disk and nothing referencing it — neither this build nor WordPress core sweeps unattached media, and a subscriber has `upload_files`. Fix is draft-first: a hidden `auto-draft` from compose-open, so uploads parent from birth and `uploadedTo` scoping has something real to scope to. **Assertion 4 counts the SITE-WIDE unattached total**, not just this gate's rows — a gate that counts only its own rows cannot see an orphan from a path it never modelled. **6** exists because `wp_delete_auto_drafts()` removes the post and LEAVES its attachments. **7b** plants two cron entries and proves the flag-off heal takes the whole HOOK: `wp_unschedule_event()` removes one occurrence, so two entries meant every load disarmed one and left the other force-deleting drafts daily for a feature nobody could reach. **Its cleanup is scoped to the probe account** — it once force-deleted every compose draft site-wide *while printing GREEN*, proven with a draft planted under another member | wp-cli; the mu-plugin present in the docroot, else **CANNOT RUN (exit 2, no-verdict — never a pass and never a false red)** |
| 47 | `tools/frontend-compose/dark-contrast-sweep.py` | **the compose form meets WCAG AA in dark and carries no bright surfaces.** Ian 2026-08-15: *"compose works well. Needs some dark mode love."* Reads the **RENDERED** page rather than a hex-pair list, because the form's colours are ACF/WP stylesheets meeting the site tokens and cannot be written down in advance. Run at **both widths (1280 and 390)** — the last two visual defects on this surface were both on the phone. **REPOINTED**: it measured the hub's injected modal until Ian ruled the form leaves the modal (main `4dbb192` killed the embed); the contrast concern moved with the surface rather than going away, and a gate still aimed at `#lpm-overlay` would have gone vacuous | headless Chrome + the dev gate; the compose route reachable (flag on for the lane path) |
| 49 | `tools/gates/paired-flag-agreement-gate.py` | **every copy of a paired feature flag holds the same value.** Number assigned by keeper 2026-08-15, verified free on `origin/main` first. **A defect I designed in.** The dark-anon wave ships flags as per-file module-local vars rather than one shared `window.LG_*` — correct, and it stays, because `pwa.js` documents that a dynamically-injected `defer` script has no guaranteed order against the sync-injected ones, so a shared global would be read before it was written on some loads. What local copies do have is this failure: I defended the pattern in a comment calling it *"one grep away from flipping every copy at once"*, and on 2026-08-15 the flip happened without the grep — `LG_DARK_BORDER_FIX` went `true` in `app-settings.js` and stayed `false` in `hub-polish.js`, `hub-infinite.js`, `privacy-sheet.js` and `sponsor-sheet.js`, so dark mode served **two different border colours at once**, side by side on hub surfaces where both files style the same page. Worse than either whole state. **A design whose safety depends on a comment being obeyed is not safe** — this is that comment turned into a mechanical check, which is what the law at the top of this document asks for. Asserts only that copies AGREE, never that a flag is on or off, so it stays correct as flags flip in either direction and nobody has to edit the gate to ship a feature. Static source read — no browser, no network, cannot flake, free in every suite. **Red-fired against the REAL live half-state before the fix**, naming both broken flags and all seven sites; `--selftest` additionally red-fires the predicate on synthetic input | — (reads the tree) |
| 53 | `tools/gates/dark-onboard-subtext-gate.py` | **`.lgpo-subtext` keeps a dark-theme ink, and its flag's OFF path stays a byte-identical no-op.** Number assigned by keeper 2026-08-15 (52 is frontend-compose's). **The defect the charter did not name, and the reason that matters.** The re-chartered dark-anon-sweep seat was sent to fix *"the lg-acc accordion summary labels are INVISIBLE — 1.21:1"* on bpnoaccess, described as dark ink stranded on light cards and live right now. Measured on the real served page with gate 36's own probe, in all four dark states, **that defect does not exist**: 86.php's dark block moves the summary ink AND the `.lg-acc` background together, so the accordions render `#e5e7e1` on `#1b1e21` (~11:1) and are correct. What *was* on that surface was the **mirror image**, and critically in a **different plugin**: `.lgpo-subtext`, hardcoded `color:#666`, on the dark card `#1b1e21` = **2.92:1**. The card follows the theme because 86.php owns it; that one piece of ink did not, because 86.php's block never reaches `lg-patreon-stripe-poller`'s markup — so a fix in 86.php was *structurally incapable* of touching it. A wrong root-cause story does not merely mislabel a bug, **it names the wrong file**. **Mechanically**: a static render of the shortcode's template region lifted verbatim from the file under test — no browser, no network, cannot flake. Asserts BOTH flag states independently (OFF adds nothing; ON emits an `html[data-lguser-theme='dark']` rule whose colour is *computed* against `#1b1e21` and must clear 4.5:1), asserts the LIGHT `#666` was left alone (5.57:1, a passing surface), and only **REPORTS** the shipped default — asserting it is what would redden the gate the moment someone legitimately flips it. Red-first (`dark-onboard-subtext-redfirst.sh`) breaks all five assertions and requires each to redden; it snapshots and restores rather than `git checkout --`, which would wipe uncommitted work under test | — (reads the tree, needs `php` CLI) |
| 33 | `tools/gates/buck-surface-guard.sh` | **THE FENCE (Ian, 2026-08-11): our work must never modify Buck's files.** Buck's surfaces are hands-off — not ours to fix and not ours to report — *unless* one is leaking data or eating resources, which is a talk-to-Ian-first situation. Fails if the changes about to land touch any path containing `buck`, excluding the guard and its own companion notes. Runs in the suite against a lane's branch (`origin/main...HEAD`) **and should be run by keeper before any merge** — it caught this lane editing `strangler-bb-mirror-buck.conf`, which was reverted to `origin/main` verbatim. Note it is a GUARD, not a numbered gate, in `run-all.sh`: it deliberately carries no `=== GATE n/N ===` banner | **none** — pure `git diff`, so it cannot go DEAD for environmental reasons |
| 50 | `tools/gates/work-board-gate.php` | **the work board renders EVERY item, and phase 1 cannot write.** Backlog 29, built on Ian's nod (*"It's good enough to start building though"*). A dev-facing READ-ONLY page that renders `docs/BACKLOG.md` as its own PRIORITY INDEX with derived badges, plus lane lights and a capacity strip off the sentinel stamp. **The headline assertion is "nothing is dropped", not "it parses"** — because the first cut silently lost every COMPLETED item: `preg_split('/\R/', …)` without `/u` also matches the byte **0x85**, which is the **third byte of `✅`**, so it cut every ticked line in half; `preg_match` with `/u` then returns **false** on the invalid fragment *silently*, not as an error. Five P0 items rendered as three, nothing logged. Two stacked silent failures. So §1 checks every index line in the FILE reaches the RENDER, against an **independent parse** — and asserts the backlog really contains ticked items first, so it cannot pass vacuously. A second real loss was caught the same way (numeric-only ids dropped `E1`–`E5`/`S1`–`S3`, one of them a security item awaiting Ian). Also gates that a missing or malformed sentinel **degrades honestly** rather than drawing comforting zeros, that thresholds are **conditional** (91% warns, 74% does not — a warning that always fires is decoration), and that phase 1 **makes no write or shell call and reads no input**, which is the property that lets a dev-facing surface ship without a flag. One mutation found a hole in the GATE not the page: the threshold checks matched the bare class name, which is also in the stylesheet, so two of them passed on pages with no red bar at all — they now count the usage form. Number allocated by keeper; the ledger was self-contradicting at the time and a lane must not guess from that | **none** — no network, no browser, no DB |
| 63 | `tools/gates/welcome-activation-gate.py` | **the welcome one-shot reaches BOTH rails, fires once, and never retro-fires.** Number assigned by keeper 2026-08-16 and **verified free before being written** — no hit on `origin/main`, none in any worktree, unregistered in main's `run-all.sh`. Two lanes were both told *57* in one window and both wrote it down, so *"keeper said so"* is not proof a number is free. **The defect was a live dual-rail violation, not a dormant feature.** `Arbiter::sync` stamped the one-shot only on a TRANSITION into a paid tier — which is really a question about ROLE ORDERING, and the two production creation paths sit on opposite sides of it: `lg-patreon-onboard.php` creates the account WITH the paid role (so `$oldTier === $winning`, never fires), `UserLifecycle.php` creates with none (so `null → looth3`, fires). Same money, same tier, different product by rail. Measured live 2026-08-15: 16 welcome emails EVER, newest 21 June, 0 of the 33 joiners since 1 July. **The headline is asserted as SYMMETRY, not as "the welcome fires"** — the two creation SHAPES must end in the same outcome and the gate says nothing about which, so it survives the mechanism moving and is red exactly while the defect exists. **The dangerous half is retro-fire:** 1,109 paying members carry no welcome marker, so a fix keyed on *paid and never welcomed* mails eleven hundred people on the first sweep, unrecallably — §D asserts the cutover fence BEHAVIOURALLY against a member shaped like those 1,109, and deliberately WITHOUT running any backfill, because a backfill is something a person has to remember to run, which makes it a plan and not a guard. **The gate READS THE FLAG** rather than hardcoding a state, so it stays correct the day the default flips, and OFF is asserted as a CONTRACT (today's behaviour exactly, and not one byte of new state — the marker write sits inside the enabled check, because *the old behaviour plus a harmless write* is still a write). **What the build earned, twice over:** the first verification run reported the flag ON changing nothing — it was testing MAIN, because `wp eval-file` reaches the poller through an mu-plugin symlink into the serving checkout, so `\LGMS\Arbiter` is always the deployed copy whatever the branch says; the probe now clones THIS TREE's arbiter under a unique class name into this tree's own `src/`, since the config resolves by `__DIR__`. And the probe originally never RETURNED a `marker` field, so every absence assertion read empty-string and *"OFF writes no marker"* passed **vacuously on the exact property it existed to protect** — there is now a schema-liveness check that refuses to run if the probe omits any field the assertions read. Mail assertions read STAMP INTENT, never delivery: `pre_wp_mail` is filtered on dev2, and the poller's mail is **separately suppressed on live** by a killswitch whose own docblock says *DO NOT DEPLOY TO PROD*. Companion `welcome-activation-redfirst.py` mutates 10 ways and requires a RED from each, plus a no-op control and a PROSE control (a comment merely naming the forbidden Stripe call) that must stay GREEN | **none** — no network, no browser; needs `wp` + WordPress |
| 51 | `tools/gates/profile-setup-gate.py` | **new members arrive alive — the profile-setup step, its OFF no-op, and the nudge that must never exist.** Backlog 19 (Ian 8/12 from the empty-directory screenshot; ruled 8/15, Option A + four sharpenings). Number assigned by keeper 2026-08-15 and verified free on `origin/main` after a rebase — lanes do not mint gate numbers. **The headline assertion is an ABSENCE.** Ian: *"No nudging on that matter."* No banner, no dismissible card, no percentage chase — and an absence is exactly what returns later dressed as an improvement, so the gate scans five member-facing trees and pairs that scan with a **liveness self-test**: the detector must first fire on a synthetic nudge, because an absence assertion with a broken detector passes on an empty repo and proves nothing. **Dual rail is asserted behaviourally**, not by string presence: the Stripe rail's switch must be DERIVED from the shared config's `enabled` key — the loose version stayed green against a hardcoded Patreon-only build, which is precisely the regression Ian's dual-wield ruling forbids. The flag is exercised in three states by **running** the reader — config ABSENT (a real temp-dir copy with no sibling), present-and-OFF, overridden ON — because "config deleted" and "config says false" are different states a source-read conflates. OFF is a no-op by construction: the route registers INSIDE the enabled check, so OFF means *not a route*, not *a route that renders nothing*. **What the red-first run earned:** a false RED from the gate's own detector (an `id="…"[^>]*` window stops at the `>` inside a `<?php ?>` tag and misses attributes written before the id), then THREE decoration assertions that tested for strings rather than behaviour — two of them matching text in this gate's own docblock, and one matching an unrelated `$kind !== 'gift'` elsewhere in the same file. Companion `profile-setup-redfirst.py` mutates the source 12 ways, snapshots rather than `git checkout --` (which would wipe uncommitted work under test), and fails loud on a no-op mutation so a stale search string cannot read as a pass. **The absence needed a second guard the liveness probe could not give it:** the walk skipped a missing tree with a bare `continue`, while the pass line counted `len(SCAN_DIRS)` — a constant — so renaming one of the five trees would have printed *"no nudge surface anywhere in 5 member-facing trees"* having read four. Liveness proves the DETECTOR fires; it says nothing about the GROUND COVERED. Now a missing tree, or a walk that reads zero files, is CANNOT RUN rather than a green, and the pass line reports the real file count (462). **§H gates the PICTURE, because Ian rules from pictures.** The built screens are published dev-gated at `footer-mockups/profiles-alive/built/` so he can see the real thing before it reaches the serve — and a render is a DERIVED artifact that goes stale in silence: the published skip screen read *"Choose My profile"* for hours after the source was corrected to *"My Profile"*, because the snapshot was built from a pre-fix capture. Every static check stayed green; only the picture was wrong, and the picture is what he looks at. So §H pulls the member-visible literals OUT of the source (a hardcoded list would drift the same way) and requires each in the published page, plus asserts the snapshot stays INERT — the captured page carries the live JS that POSTs to the real `profile-api` write endpoints, so publishing it raw puts a working writer on a page presented as a screenshot. **Both of its own first cuts were wrong and the red-first caught both:** the agreement check searched whole files and went GREEN against the very staleness it was written for, because *"My Profile"* also appears in the site header's account menu — it matched the CHROME of the other page while the text under test was still wrong; and its coverage guard raised CANNOT RUN on a source that had lost its wording, which **overwrote a real RED with exit 2** and re-reported a defect as a missing environment | **none** — no network, no browser, no DB |
| 52 | `tools/gates/notif-quickreply-gate.py` | **tapping a notification opens a reply modal quoting the reply that rang you — and with the flag OFF that path is UNREACHABLE, not merely uncalled.** Backlog 5, Ian's layout A (2026-07-30): quote THEIR reply, never the member's own post, with a full-post link beside the composer. Number assigned by keeper 2026-08-15 — 50 was already registered and 51 belongs to an unmerged lane, so the highest number visible in `run-all.sh` is NOT the next free one. **Written against a specific failure:** three defects reached Ian on 7/30 through gates that asserted only what should be PRESENT — a composer that kept the previous reply's text, a reaction row that grew a composer it should never have had, and a flag whose OFF state quietly did something. So the suite runs the SHIPPED state and asserts the response is **byte-identical to origin/main with `?reply_context=` actually sent**. **The assertion that makes the rest worth anything is the counter-proof:** `--expect-off --force-flag-on` MUST exit 1, and it does — failing on exactly the two OFF assertions. An absence check that has never been seen to fail is not evidence. The ON mode additionally proves a reply from ANOTHER topic is refused, which is what stops the visibility gate being walked around by pairing a public topic's slugs with a hidden forum's reply id | the bb-mirror app + its Postgres; three states (pass/fail/**skip**) so a check that cannot run is counted separately rather than silently degrading to "nothing to check" |
| 54 | `tools/gates/weekly-front-gate.py` | **the weekly email on the LOGGED-OUT front page, and the padlock that fails silently without it.** Backlog 8 (Ian 2026-07-30; ruled 2026-08-15 after the mock — *"build it and let me see it on dev2"*, Option A). Number assigned by keeper; the charter's "next 52" was already stale (51 profiles-alive, 52 notif-quickreply-v2, 53 dark-anon-sweep), which is the collision the number-from-keeper rule exists to stop. **The assertion that earns it**: the `tier` taxonomy stores `looth-lite`/`looth-pro`, the front page's CSS is `.rcard--gated-lite`. An unmapped slug produces `.rcard--gated-looth-lite`, which matches no rule in archive.css — so a **member-only card renders with no padlock and reads as free content**. Nothing throws, and "a badge is present" passes on the broken output, so the gate asserts the gate ELEMENT and the exact tier token, and that an unknown tier fails **closed**. Also: the flag in all three states (absent/OFF/ON) read the way the code reads it, so flipping the default needs no gate edit; OFF emits **zero bytes**; no gated prose in the rendered output even when the payload wrongly carries some; archived posts never appear (the email's resolver deliberately returns `post_status=archived` — right for a record of what was sent, wrong for a shop window, and live's August issue contains one); the events section is not replayed (an issue's events are what was upcoming AT SEND TIME); and the masthead carries the issue's OWN date, not the render clock — the defect the email builder still has. **Static**: renders the partial in-process, calls the feed's predicates by reflection. No browser, no network, no DB. **Red-first proven on eleven mutations**, and two assertions were caught being decoration first: `[E]` and `[F]` grepped the source for words the file's own docblock satisfied (behaviour extracted into callable predicates instead), and `[D]` tested a card type that renders no prose at all, so it passed however the guard was broken | **none** — no network, no browser, no DB |
| 34 | `tools/gates/stripe-testgroup-pages-gate.php` (34b) + `lg-patreon-stripe-poller/deploy/remediation/test-soft-launch-allowlist.php` (34a) | **the Stripe soft launch reaches a hand-picked list and NOBODY else.** Ian 2026-08-14: the launch runs through the EXISTING member pages unlocked for a list, not a bespoke page. Two halves because the feature has two: 34a covers the GRANT (which webhook events may move a membership; 39 assertions, merged 8/11), 34b covers the PAGES (who can reach join/gift/refund/regional at all; 54). They address the SAME option, so they cannot drift. **34a was merged on 8/11 and never wired into `run-all.sh` — it went un-run in every nightly until 8/15; a merged gate absent from that file does not exist.** 34b asserts PER STATE and reads the flag off the box rather than assuming one: flag off/absent (the default) refuses a LISTED member exactly as today, which is what makes the off state byte-identical; flag on with the list absent/empty/malformed still refuses everyone; and with both armed the list is the ONLY discriminator. An administrator is never gated behind the list — Ian builds the Stripe op privately and must not lock himself out. Also asserts no page's GO-LIVE audience moved, and that the QA surface stays admin-only in both columns. **Two mutations found holes in the GATE rather than the code:** a fail-open list mutation stayed green because the section only proved one viewer was refused ("nobody got in" can be true by accident; "the list is empty" cannot — fixed by asserting the reader's output directly), and an admin-lockout mutation stayed green because it was a **no-op** (the gate falls through to another that also admits admins). **34c** (`tools/gates/stripe-price-control-gate.php`, 49) gates the dash price control: setting a price is THREE writes — create it in Stripe, record it in our own `prices` table, repoint new joins — and only the middle one looks optional. Skip it and `lgjoin.php`'s already-subscribed INNER JOIN loses an existing member, offering them a second subscription; so §3 runs the JOIN PAGE'S OWN QUERY rather than checking a row exists. Also asserts the charter's sandbox-only rule IN CODE (a live key refuses), that a failed local write leaves new joins pointing at the previous working price, that existing subscribers are grandfathered and their old price stays resolvable, and that it ships with NO price set because the number is Ian's. Two of its nine mutations found a hole in the GATE not the code, both the same one: an unexpected throw killed the run with no FAIL line (the gate-2 failure mode), fixed with `mustSet()`. **34d** (`tools/gates/stripe-testgroup-sweep-gate.php`, 26) closes the hole the other three left: the design says *every* grant path keeps the list check, and only the webhook ever did. A redeemed gift arrives by a different road — the separate Slim billing app writes an entitlement, pings the unconditionally-registered `/sync-customer`, and `Sync::customer()` reports a stripe opinion and runs the Arbiter — and `Tick` pass 2 sweeps every customer every five minutes to the same place, ungated by `lgms_stripe_frozen` (that guards the POLL, pass 1). So a gift to somebody NOT on the Test Group granted them membership anyway, within minutes; the charter had predicted the opposite failure. Its S1 mutation reproduces the defect exactly as it shipped (14 RED). The assertion that earns its keep is measuring "granted" from the COLLABORATORS rather than the return value — mutations S4/S5 return "skipped" while still writing the opinion or running the Arbiter, which a `$res['tier'] === null` check would have passed while the member was demoted. Gates the standalone `membership-pages` app, NOT the shortcodes — an nginx regex shadows those, so the obvious file is dead code | **none** — no network, no browser, no DB; every option is stubbed through the app's own `function_exists` seams |
| 35 | `tools/gates/compose-gate.py` | **front-end compose + edit reaches the right people and nobody else — and OFF is inert.** Number ALLOCATED BY KEEPER (34 is the stripe seat's). I had minted 34 from `run-all.sh` myself and it was already spoken for — lanes do not mint gate numbers, and this table cannot be the source either: it carries duplicate row numbers after the 2026-08-11 merges. Asserts PER STATE, reading the feature's own `lg_fc_enabled()` off the box rather than assuming one — the flag lives in the shared `platform/config/frontend-compose.php` so bb-mirror's type toggle and the WordPress form cannot disagree, which is the same UI-lies class gate 19 guards. **Flag OFF/absent:** the route is byte-identical to a before-state recorded while the feature did not exist, checked for an **ALLOWED** user — anon 404s in both states, so an anon-only probe cannot tell them apart, and that hole made the first version pass against a flag that was ON. **Flag ON:** the allowed member gets a real form; a refused member and an anon get none; a refused POST is non-2xx **and writes no row**; the **OWNER** gets the edit form and a **STRANGER** does not (IDOR); a stranger's edit POST does not move `post_modified` — the store is the witness, not the status code; and `embed=1` serves the furniture-free variant the hub composer frames, with `X-Frame-Options` proven not DENY. Every assertion falsified by mutation before being trusted — including letting a stranger genuinely hijack a throwaway post once the ownership check was disabled, because the store-witness half had been passing for the wrong reason (no valid nonce) and was not yet earning its place | wp-cli, and the dev gate token via `gate-env.sh` |
| 35b | `tools/gates/loothprint-paywall-gate.py` | **the Loothprint form's member-facing controls: the paywall toggle, and an editor that is actually initialised.** Under 35 rather than a new number — it is a control ON that form, and two lanes have already collided by both minting the next free one. Ian 8/21: *"a toggle for the user to decide if behind the paywall. Default to behind the paywall."* **P1** EXECUTES the four-case rule's truth table — `lg_fc_paywall_target()` is pure on purpose (no post, no WordPress) so it can be tested on a box where the feature is not deployed; the case that matters is `looth-pro`, which the obvious two-way mapping silently downgrades to lite. **P2** the control agrees with the flag per state (absent/OFF/ON), read from the serving checkout, never hardcoded. **P3** a choice reaches the **SERVED BYTES** of the article page, not the term store — the store was right and the page was wrong, which is the defect this feature nearly shipped with. **P4** the rich-text editor is not left un-initialised: Ian's 8/21 screenshot showed *"Click to initialize TinyMCE"* above his write-up as literal `<p>test</p>`, which is ACF's `delay` placeholder — **the same element as gate 47's bright-surface red**. ⚠️ P2/P4 are **DEPLOY-COUPLED and say so**: that mu-plugin is symlinked out of the serving checkout, so until a merge AND a pull they print NOT DEPLOYED — never a red (the box being behind is not a defect) and never a green (that would be a vacuous pass on the claim under test). Probe post is **per-run and PID-keyed**, deleted with its blob | the dev gate; `looth-dev` wp-cli; the loopback `_materialize` endpoint; `archive-poc` psql |
| 35c | `tools/gates/materializer-queue-harness.php` | **a blob re-bake must see the whole request, not its first hook.** ACF calls `wp_update_post` BEFORE the `acf/save_post` chain, so `wp_after_insert_post` fired the bake first and the dispatcher's per-request de-dupe then SWALLOWED the one the tier write triggered — the member's paywall choice was written and the page kept rendering the old gating. On the member path `lg_fc_promote_draft` sends mail at priority 25 before the write at 26, so it was deterministic, not a race. Drives the real hook order against stubs (no box state, because that mu-plugin is ALSO symlinked out of the serving checkout) and asserts: nothing is sent mid-request, five triggers collapse to ONE POST, a first publish is baked as **published** not auto-draft, a delete **survives its own row disappearing** (the post type is resolved at QUEUE time for exactly this reason), and an unmanaged type is never queued | `php`, nothing else |
| 39 | `tools/gates/featured-member-gate.py` | **backlog 18 (Ian 8/11), design rulings 2026-08-14.** Two schema constraints proven by trying to VIOLATE them rather than reading the DDL: `featured_opt_in_at` must be NULL unless `featured_opt_in` is true, and `discovery.featured_history` may never hold two OPEN rows (Ian's ONE AT A TIME ruling — a partial unique index on `ended_at IS NULL`). Re-runs `Completeness.php` against **every** public member and diffs it row-by-row against `tools/profile-completeness-report.sql` — the two are separate implementations of the same eight-item score with no shared source, and building the class found a real divergence (32 members' `business_name` carries a stray backslash from a double-escaping artifact; Postgres's `LIKE` silently absorbs it as an escape char so the SQL side was accidentally right and a naive PHP comparison was not) — this keeps that fix from silently regressing. Flag-off is read from the SHIPPED SOURCE (same style as row 22): the card markup in `u.php` and the `member_uuid` resolution ternary in `index.php` must each be textually reachable only through the flag check — **the first cut of the index.php check was too loose** (it only confirmed `$lg_fm_on` appeared *somewhere* after the `if`, which stayed true even after the ternary gating the actual resolution was deleted, since the variable is still computed a few lines above regardless) and was only caught by red-firing that exact mutation, not by inspection. Also asserts `me-featured.php` never lands in `Auth::ADMIN_EDIT_AS_ENDPOINTS` — Ian's consent ruling is "never inferred... no override, not even for admins," and that allowlist is the one place it could quietly regress. **§F added for #107** (Ian 8/19: *"the dash should allow me to select anyone"*, clarified to *"opted in only"*): completion may never block a selection, proven by EXECUTING the shipped rule (`FeaturedMemberDash::selection_block_reason`) against the real pool rather than grepping for a refusal that could simply have moved — and reporting CANNOT RUN on an empty pool, since "nobody is blocked" is trivially true of nobody. §F2 holds the pillar the change must not take with it (a Private profile is still refused; Ian rejected the "literally anyone" reading). **§F4 exists because removing the wall alone would have made the failure SILENT rather than fixing it**: the front-page resolver keeps its own avatar+role guard, so a pick it cannot draw must be warned about at selection time. Measured through the real `admin-post.php` path 2026-08-20 — featuring Rick Liftig, whom the dash then called "Ready" with an enabled button, answered "Saved and pushed to archive-poc" and removed the band from the front page entirely (74,456 → 72,838 bytes, zero `lg-fm__` markers); **four of eight opted-in members were in that state**, invisible to `card_ready`, which tests a location the card does not need and cannot see the header visibility the card's role depends on. §F3 is the drift alarm for the predictor that fixes it, since it reproduces a rule living in ANOTHER PROCESS (archive-poc) that profile-app cannot call. **§G added for #107's follow-up** (Ian 8/20: *"the tick is consent"*): the featured card — and only it — may repeat an opted-in member's members-only one-liner. A deliberate hole in the 8/16 never-republish rule needs a fence, so §G3 EXECUTES the rule rather than reading it, lifting `lg_fm_card_role()` out of `index.php` by name (the file is a whole rendered page and cannot be `require`d) and running it over a 12-case truth table: every uncertain input — flag off, cutover unset, cutover unparseable, no opt-in stamp — must fall back to the pre-#107 answer, and a tick made before the copy said so must NOT publish without a recorded admin acknowledgement. Red-fired by mutating that one branch to publish anyway, which is the silent upgrade of old consent the ruling forbids. §G2 is a LIVENESS check on a grant, not a config read: the resolver runs as the `archive-poc` role under COLUMN-SCOPED grants and now needs `users.featured_opt_in_at`, which was not granted until 8/20 — an ungranted column raises `permission denied for table users`, and the call site's try/catch turns that into NO BAND for every visitor, so the symptom of a missed deploy step is a blank front page with nothing to say why. §G4 resolves all three callers' `@include __DIR__` paths for real (three apps at three directory depths; `api/v0` needs THREE dots where `u.php` needs two — the exact miss §C4 was written for, one flag over). §G6 asserts the dash stays SILENT on an absent `glance_needs_ack` — an endpoint older than the dash means *unknown*, and warning about picks that are fine trains an admin to click through every warning. §G7 measures the CONFINEMENT against the real served page: a members-only one-liner must still not reach the rendered profile body of a logged-out viewer. ⚠️ §G7 deliberately asserts the BODY only. Measured 2026-08-20: a profile's `<meta name="description">`, `og:description` and `twitter:description` carry `at_a_glance` verbatim to logged-out visitors and crawlers even when the header block withholds it — 28 public members, pre-existing, and reddening main for it would block every lane. It is a ruling for Ian, recorded in `docs/domains/PROFILE.md`, not a gate leg | passwordless sudo to `profile-app`/`postgres` for the DB + PHP-harness checks; the live-route check (`§E`) needs `LG_GATE_HOST`/`LG_GATE_COOKIE` and reports CANNOT RUN, never RED, without them — expected pre-merge |
> mirror-sync lane sat unmerged and main gained two more gates. Both times the
| 36 | `tools/gates/anon-dark-contrast-gate.py` | **a logged-out visitor in DARK MODE gets no MORE unreadable text/fields than today's recorded baseline on the sign-in/join/sign-up path.** Backlog 21, Ian 2026-08-14: *"the dark mode needs some love for the login stuff"* + *"a ton of instructions and fields not ready for primetime in dark for logged out."* Measures WCAG contrast (text 4.5:1, large text/field borders/icon controls 3:1) on `/wp-login.php`, `/join`, `/lgjoin` and the front page, in BOTH dark paths — **app-dark** and **os-dark** — at desktop and mobile. **A RATCHET, not a zero-findings gate** (keeper caught this before merge, 2026-08-15): this backlog names a genuinely large surface that will take several more waves to clear — the front page alone still carries 10 findings that are a different lane's surface (a Guitardle leaderboard) entirely. A zero-findings assertion merged before the wave finished would BLOCK EVERY OTHER LANE'S TRAIN for debt this lane is still working through in separate commits, so the gate asserts **no regression past `BASELINE`** per surface instead — still catches a NEW dark-contrast defect being introduced, stops blocking on debt already disclosed. The ratchet only tightens: fixing more must lower `BASELINE` in the same commit, never just leave the number stale. The comparison logic itself (`ratchet_verdict`) is red-fired via `_ratchet_selftest()` on every run, no browser needed, before any real measurement is trusted. **`BASELINE` is a noise-safe floor, not a tight one**: two independent live captures of the identical post-merge state, ~40 minutes apart under this box's real 2026-08-15 contention (load 0.4–7.1, other lanes' gates running concurrently), disagreed by 2–8x on several surfaces — wall-clock settle delays, not an event-based wait, so CPU contention delays the dark repaint past the probe's read. `BASELINE` is the per-surface max of both captures for exactly this reason; a single-run baseline would flap the gate red on measurement jitter, not real regressions, which is worse than no gate (it teaches everyone to ignore gate 36). **That timer was replaced with a real signal on 2026-08-15, and the "CPU contention" diagnosis above is now known to be at most half the story.** `measure()` now asserts the theme has RESOLVED — polling for `<html data-lguser-theme='dark'>` — as a **precondition** before the probe walks anything, on BOTH dark paths rather than only app-dark, and re-navigates once if it has not. The old ordering probed on a wall-clock settle and only noticed afterwards, which is the bug: an unresolved page is still showing its LIGHT paint, so the probe reads light ink against light backgrounds and reports enormous ratios-of-nothing — **1.2:1 findings against hex backgrounds that only exist in the light palette**. Those were never defects on the page; they were photographs of a page mid-boot, and they are the signature of the burst false-reds seen across several surfaces this day (a `join` surface reported 21 findings against a recorded baseline of 0; measured properly, `join` reads 0 in all four states and its footer is `#101214` with zero sub-AA items). Measured while verifying the gate-53 fix: two of four app-dark runs came back `theme='default'`, and one app-dark/desktop needed **four** attempts to resolve. A surface that still will not resolve now has its findings **DISCARDED rather than ratcheted** — a zero from it is unearned and a non-zero is a phantom — and is called out by name instead of silently passing. Re-baseline tighter now that this holds, ideally on a quiet box. Shares its contrast math (`tools/gates/lib/contrast-probe.js`) with the sweep that ranked these surfaces (`tools/preview/dark-anon-sweep.py`). Confirmed live before any fix: the login page's three-card skin (`lg-snippets/snippets/86.php`) predated dark mode entirely; the mobile "+" compose icon measured 1.85:1 (`--lg-sage-d` repoints to a LIGHT colour under dark while its icon's hardcoded-white stroke does not follow); `/join`'s intro paragraph inherited the boot script's forced body ink while its own card background never moved. The login-page, `/join`, and the shared pre-launch-stub fixes have since landed unflagged (none of the three is member-visible); the icon, border-token, and search-wrapper fixes are built and gate-verified but held OFF behind their own flags pending Ian's phone pass on the dev2 serve | chrome-dev on 127.0.0.1:9222, `tools/gates/gate-env.sh` resolving a token |
| 62 | `tools/gates/flag-register-gate.py` | **a branch that ADDS a flag must REGISTER it in `docs/FLAGS.md`.** That register has stated in its own header since 2026-08-09 that *"any merge that adds, flips, or retires a flag updates this file IN THE SAME COMMIT — keeper refuses the merge otherwise"* — and **nothing enforced it**, so it decayed: one sweep on 8/16 found SIX unregistered flags across FIVE branches. Six lanes breaking the same rule independently means the rule was unenforced, not disobeyed. Encoded per this document's own law after the class appeared FOUR times in one lane in one session. **RED-FIRST FROM REAL HISTORY**, not a mutation — `notif-quickreply-v2` at `8b988b5` RED, at `51cb578` GREEN. ⚠️ **Catches prose that is MISSING, never prose that is WRONG** — a docblock naming a deleted mechanism sails past, which is exactly how the compose flag taught a dead pool-env mechanism for a day. "Flag" is deliberately narrow (getenv/`$_SERVER`, a BOOLEAN `define()`, or a new tracked `platform/config/*.php` with `'enabled'`) because the first draft RED-ed on two constants, and a gate that reds on non-defects blocks every lane | git only — no browser, network or DB, so it cannot flake under load |
| 59 | `tools/gates/guitardle-letter-state-gate.py` | **a guessed letter is a HIT or a MISS, and a resumed board PAINTS.** Ian, playing on dev2 2026-08-16: *"it's keeping track of letters that are in the puzzle, but not the guesses that were misses. The letter only stays lit for a correct letter"*, then *"refreshing the page on dt lights all letters, but the correct letter just selected from mobile isn't there."* **NOTHING WAS EVER LOST, and the gate asserts that too** — a lost move would be a *fairness* bug, not a paint one. Measured with two devices interleaving: the row read `{"moves":3,"revealed":["C","K","B"]}` **on main as well as on the fix**; the reveal endpoint appends under `SELECT … FOR UPDATE` and `save` is refused `409 server_owns_state`, so the client cannot overwrite it. Every symptom was the client drawing a correct record wrongly, in three ways: (a) `positions=[]` means **two different things** — a vowel's first tap *and* a miss — so branching on `positions.length` filed every miss as a purchase, left the key **enabled**, and `.purchased` is styled for **vowels only**, so a missed *consonant* got a class with no rule anywhere and rendered untouched; (b) server play draws tiles with `data-i` (the client is never told the phrase — backlog 25) while legacy uses `data-letter`, and **both** restores called the `data-letter` painter, so a resumed board matched zero tiles and painted nothing while the same loop lit every letter *including misses* — verbatim *"lights all letters, but the correct letter isn't there"*; (c) even on **legacy**, which is what **live runs today**, a miss wore the same `.used` as a hit, so live is not correct either — it just fails less visibly. **KEYED TO STATE, NOT WIDTH, and that is the point**: the obvious desktop-vs-mobile gate would be the wrong axis and would *pass* — there is no width branching in this code and both widths measure identically. What differs is **state** (live play / after a refresh / across two devices), so those are the phases; Ian met one defect on each device and reasonably read it as a device difference. Two devices are two **isolated browser contexts**, never two tabs — tabs share cookies *and* localStorage, which hides the very bug under test. **Asserts the pixels, not just the class names**: on main a miss got `purchased`, which differs from `used` as a *string* while rendering identically to an untouched key, so the class-diff assertion passed on the defect until a `getComputedStyle` check was added. Asserts **this tree's** client, substituted over CDP, because dev2 serves `main`; `LG_GDLE_SERVED=1` measures the served code instead — the red-first direction, which reddens **24** assertions across all six phases (re-measured 2026-08-16; this row said 14 and `run-all.sh` said 4 for the same measurement, and neither was ever right). Found and fixed a regression in its own fix: under server play a stale `localStorage` snapshot carries letters but no positions, so replaying it marked *every* letter a miss. **PHASE 5 (vowels)** closes a gap the first cut left: the fix also changed `serverReveal`/`handleVowel`, and none of it was asserted — a vowel is the only letter tapped **twice**, so it is the only one that can sit half-resolved. Bought on tap 1 (purchased, *not* resolved, and still **tappable** — a disabled key could never be revealed), resolved on tap 2 (hit or miss, purchase spent, disabled). **PHASE 6** is a FOURTH defect, found by reading the guard meant to prevent it and reproduced on an ordinary member path: play the game page directly (LEGACY — `?sp=1` is added *only* by the front-page block, and legacy writes a local snapshot because it knows the phrase), then enter via the front page. `restoreSavedGame()` guarded with `!serverPositionMap()`, but the handshake builds that map by looping over `revealed[]`, so a claimed row with nothing revealed encodes as PHP's `[]` — a JSON **array** — and an empty array is **truthy** in JS. The guard could never fire in the state it was written for; every lookup then fell back to `PHRASE_LETTERS`, which is *always* `''` under server play, so `hit=false` for everything. Measured: a genuine HIT rendered `miss.used` with nothing painted — the board **asserting something false** rather than merely failing to show it. ⚠️ **The first version of phase 6 passed on this defect**: it built the state with a DB rewind and asserted an absence, but under server play the client writes **no** `localStorage` at all (`serverReveal` calls no `saveGame`), so it ran against an empty board — and a key with *no* class satisfies "not a miss" exactly as well as a correct one does. It now asserts the snapshot **exists and names the hit** before judging any paint; an absence assertion without a liveness assertion is vacuous | chrome-dev on 127.0.0.1:9222, passwordless sudo to `looth-dev`/`postgres`, the dev-gate token from `/etc/nginx/conf.d/loothdev-auth.conf`; per-run probe member, swept and deleted |
| 60 | `tools/gates/guitardle-phrase-uniqueness-gate.py` | **no player is served the same puzzle twice in a cycle.** Backlog 35. Ian caught it on LIVE 2026-08-15: members got DAN ERLEWINE on day 12 (23 June) and again on day 65 (15 August). **The obvious diagnosis is wrong, and the wrong fix here is destructive.** `sequence.json` is a clean no-repeat permutation of all 285 ids and the no-repeat-until-all-played mechanism works exactly as designed — *reshuffling it would re-serve phrases players have already had.* What defeated it was one phrase TEXT living under TWO IDS: 180 and 233 were both `Dan Erlewine`. The sequence never repeated an id; it served two different ids that read the same to a player. So the gated property is deliberately **not** "the sequence has no repeated id" — *that was true the whole time this bug was live.* It is the thing a player experiences: **walk the full cycle on BOTH tracks and see no phrase twice.** **Both tracks earn their place.** Logged-out runs half a sequence ahead (+142, Ian's 6/11 ruling), so a duplicate can be a fairness bug on one track years before it surfaces on the other — this very pair had already bitten members twice while both entries were still in the logged-out track's future (days 155 and 208). A gate that checked one track would have called this library clean. **The ASCII assertion is not housekeeping either.** `_guitardle-puzzle.php` splits the CSV with `preg_split('/\R/')` and **no `/u`**, so any character PCRE's `\R` treats as a line break but JavaScript's `split('\n')` does not — bare CR, `\v`, `\f`, U+0085, U+2028/9 — cuts a row in half for the **server** while the **client** parses it whole, and the server then judges a different puzzle than the player's board was drawn from, silently. `\v` and `\f` are plain ASCII, so a non-ASCII check alone cannot catch them; both assertions are carried. The fix this gate landed with retexts **id 233 only** — not 180, which was **that day's live puzzle**, and whose saved-game snapshot keys on phrase id rather than text, so changing it would have swapped the answer under members mid-game. Zero index changes, zero sequence changes, no row removed, both assets still served (gate 42 phase 5 untouched). Red-first (`guitardle-phrase-uniqueness-redfirst.py`) breaks **thirteen** behaviours on a snapshot copy — never `git checkout --` — with an unmutated control that must stay green and a loud failure on any no-op mutation | — (static file read: no browser, no network, no WordPress) |
| 68 | `tools/gates/compose-chrome-gate.py` | **the standalone compose page wears the site header and footer, in BOTH themes, and the embed variant does not.** Ian 2026-08-16, mid-test: *"can we get the header and footer so it looks like a normal page?"* — with keeper's ruling that standalone pages MIMIC the chrome and the presence is gated in both themes from birth. **Liveness first**, because a locked-out browser serves a styled 403 that looks identical in every theme — a chrome assertion against it fails for the wrong reason and a presence-only one could PASS on it. Judges **visible, not present** (computed display + a real layout box, never a class name in the HTML): chrome in the DOM at height 0 is the *presence is not reachability* shape. **Both themes** because a token-coloured chrome can be present and invisible in one — this same form produced exactly that this week. The embed leg is an **absence assertion paired with a liveness one** ("no chrome" is trivially true of a 404) and SKIPS rather than scores if that route is unreachable. RED-FIRST from real state: 4 of 5, exit 1, before the chrome shipped | headless Chrome + the dev gate; compose reachable for an entitled member (flag OFF ⇒ exit 2, no verdict) |
| 69 | `tools/gates/loothprint-edit-door-gate.py` | **ONE Edit pill in the Loothprint's sticky dock, going straight to the prefilled form — for the entitled and for nobody else.** ⚠️ **RE-AIMED 2026-08-21 (#179), NOT RENUMBERED** — same control, new shape, so the gate moved with it rather than a second number being minted. It previously asserted a two-line choice (*Details & files* vs *Page text*, Ian 8/16); Ian superseded that from the running page: *"The text is handled on the form. I don't know how we got to the modal thing."* There is now ONE door for **everyone, cap-holders included**, and the gate asserts the second door's **ABSENCE**. **It runs THREE viewers** — the post's own author *without* the cap (exactly 1), an `edit_archive_poc` holder (exactly 1, and NO page-text door), a stranger (0). The cap-holder leg is the point: they are the only viewer for whom a second door could have survived, so dropping that leg is what would make *"not even for admins"* untestable. **Signed-in liveness**, not just page liveness: identity comes from a `/whoami` loopback that is INTERMITTENT for a minted cookie — measured, the same tree gave items=2 then items=0, so without it "no pill" and "not recognised" are one observation (signed-out ⇒ exit 2, never a fail). Asserts a **real post id** (the first build would have shipped `id=0`), that the **old corner control is gone**, that the link **agrees with the compose flag either way**, and #179's **pill-family** rules: one height, one radius, a shared edge on the axis the dock is laid out on, a phone row that fits and does not wrap, and dark pills in dark. **HK-027 is asserted against the leftmost RENDERED BODY TEXT**, scrolled to where the dock and the text share a band — not the article wrapper box, which carries padding and would have passed a dock sitting on the words. ⚠️ Deliberately does **not** assert 641–700px, where MAIN IS ALREADY RED (body text at x=41.6/44.0 against a dock right edge of 82): a gate that goes red for another open defect blocks every lane. ⚠️ Reads the EFFECTIVE flag from the **serving checkout**, not the repo — a repo-relative read always answers the tracked default because the box-local override exists only beside the deployed app. `--path` points it at a lane preview, because `/srv/archive-poc` is a symlink into the serving checkout and the default path always measures MAIN | headless Chrome + the dev gate; an author-without-cap, a cap-holder and a stranger (defaults patreon_77159883 / claude_admin / erin.vogel) |
| 70 | `tools/gates/mirror-sync-loud-gate.py` | **the mirror pipe announces its own failures, and reconcile can reach backwards.** Backlog 3.9. A skip is not a success: `bb_mirror_upsert_reply()` returns without writing for an unmirrorable row — correctly, since throwing there wedged live's reconcile for **11 days** — but `_sync` answered 200 either way, so the 8/9 analysis read 290 POSTs, saw all 200, and could not separate the 11 replies that vanished from the 61 that landed. ⚠️ Asserts **202 specifically, not "not 200"**: a 4xx/5xx looks stricter and is worse, because the WP hook is fire-and-forget and an error status retry-storms a row retrying cannot fix. Also asserts the upsert **still never throws** — loud is not fatal. Backwards reach: the delta walk is forward-only (`post_modified_gmt >= bookmark - 60`) and on 2026-08-16 all five diverged replies were 60–73 days older than it, so the deep sweep must be interval-bounded on its own key, repair on a **modified-time difference** (not just absence, or stale edits stay permanent), and **refuse an empty WP read**. Mutation-proven: 202→200, skip→500, guard removed, upsert made to throw, note deleted — all RED | source-read only: no browser, network or DB, so it cannot flake under load |
| 72 | `lg-weekly-digest/dev/verify-discussion-media-thumbs.php` (+ `.sh`) | **the weekly digest's discussion cards keep their images.** Ian 2026-08-03: *"images from discussions are now sometimes not making it into the weekly digest"*, narrowed 08-05 to *"mostly the discussion section"*. A topic resolved a thumb from a featured image (bbPress topics never have one) and the first inline `<img>` in `post_content` — **nothing else**. The hub composer *strips* inline previews and stores images as BuddyBoss media, so every composer-made discussion lost its image; the last inline-`<img>` topic was 2026-06-17, which is the "now". **Measured on live before choosing an approach**, which is the substance: of 68 image-bearing discussions in 90 days, 13 already work, **54 are fixed by `bp_media`**, and **1** (71454) is reachable only from the mirror — so resolving from the mirror instead buys *one topic in ninety days* in exchange for a cross-DB dependency inside a **cron-driven mailer**, where `lg-wp-cron.service` carries no `Environment=`. Built on `bp_media`; 71454 recorded as knowingly uncovered. **DATA-SHAPED, SO IT RUNS ON THE REAL CORPUS, NOT A FIXTURE** — a seeded row proves the code can read a row we wrote; 40 real failing-class topics prove it reads what the composer produces. Reports the corpus it measured and exits **CANNOT RUN (2)** if the failing class vanishes, so it cannot pass vacuously. Asserts **both** flag states — 0/40 resolve OFF (the bug, reproduced), 40/40 ON — and that 40 passing-class cards are **byte-identical** between them, so the fix cannot reach a card that already worked. Proven able to fail: deleting the resolver reddens it (exit 1), restored from a snapshot copy, never `git checkout --`. **Reply-only images stay excluded ON PURPOSE** — *a topic card showing a photo that is not in the topic misrepresents the click* — recorded as Ian's call, not silently closed. ⚠️ **dev2 holds 2825 `bp_media` rows and ZERO media files**, so this gate proves the URL is resolved and emitted and can **never** prove the image *loads*; only live can | — (static: `wp eval-file`, no browser, no network) |
| 73 | `tools/gates/location-address-gate.py` | **the profile prints the address the MEMBER TYPED, and the privacy ladder does not move when it does.** John Wilmink (Thomas Muse Guitars) retyped his location to his shop and dragged the pin; the pin moved and was correct, the address TEXT kept showing his HOME address, for **47 days**. **The obvious diagnosis is wrong** — nothing failed to save. His typed value was in `users.location_text` the whole time, correct on both boxes. The block printed a *different column*: at street precision `Block::locationDisplay()` read `users.location_address` first, and that column's **only writer in the entire repo** is the one-time BuddyBoss import (`bin/snapshot-location-from-bb.php`). No editor endpoint maintained it, so it froze on the pre-import address the moment a member edited and, being read first, beat what they had just typed. The map was unaffected because it reads lat/lng and the reverse-geocoded city/region — which is exactly why the pin was right and only the text was wrong, and why this looked like a save bug for seven weeks. Measured by running the real render function against the real DB (user 190, dev2 AND live); **four live members were mis-rendering** (190, 590, 598, 1323), twelve more had diverged columns but sat at City/State precision where the column is never read. ⚠️ **THE LEG THAT EARNS THIS GATE IS NOT THE FIX, IT IS THE LADDER.** *"Print what the member typed"* sits one careless edit away from printing a street address to an audience who chose City — the same field is where members hand-type addresses, and on live two text-only rows already hold full street addresses. So **every** state asserts city/state still print the STRUCTURED label **and** that neither address string appears in their output: a leak assertion paired with a liveness one, since *"contains no street address"* is trivially true of an empty string (`feedback-absence-assertion-needs-liveness`). **Reads the flag rather than hardcoding a state** (`feedback-gate-reads-the-flag-not-a-hardcoded-state`) — it reports the tracked default and exercises absent/OFF/ON regardless, so flipping the default needs no edit here. OFF is asserted as an **equality against the historical string**, not as *"not the new value"*, which an empty return would have passed. **The write half is gated with the read half on purpose**: an ON that wrote `location_address` while OFF only changed rendering would strand members who saved during ON with data the OFF path never meant to create — so OFF is a no-op on disk as well as on screen, and the gate counts writes against guards to prove no write escapes. Pleasant consequence recorded rather than relied on: once a member saves under ON the two columns agree, so they render correctly even if it is later switched OFF. **Red-first, three mutations, each into a COPY of the tree** (never `git checkout --`, `feedback-mutation-harness-must-snapshot-not-checkout`): reverting the `Block.php` fix → RED naming the home address it printed; ungating one write → RED *"3 writes but 2 flag guards"*; making `coarseText` fall through → RED with 12 findings, ladder leaking in all three states | **none** — pure function under test: no DB, no browser, no network, so it cannot flake under load and cannot go vacuously green behind a locked-out browser |
| 74 | `tools/gates/categorize-last-gate.py` (+ `categorize-last-redfirst.sh`) | **write first, categorize last — all THREE flag states, and the five couplings that would make a green flag a lie.** #129 ledger 44. Ian 8/16: the composer's required *Where* step is *"a pain point for posting"*… *"maybe we add it at the end as optional. bonus points"*; refined 8/19 to Option C — *"add in the tags and maybe popping up a new modal with a decent heirarchical layout"*. ⚠️ **THE CLASS THIS GUARDS IS NOT THE ON PATH.** It is that *"flag OFF is a no-op"* is asserted by nobody and drifts — the recurring failure here. So it **reads the flag rather than hardcoding a state** (`feedback-gate-reads-the-flag-not-a-hardcoded-state`) and asserts per state: **absent** — both readers fail CLOSED when the tracked config is unreadable, because a reader that defaults ON when its config vanishes turns a deploy hiccup into a member launch; **OFF** — the original Where-step markup is present **VERBATIM** (the diff removes ZERO lines; the old path must be *untouched*, not merely unreached) **and** `register_taxonomy_for_object_type` is behind the flag, since an ungated attach is a **schema change**, not a no-op; **ON** — the pre-made forum choice, the tag field, the Skip control and the on-intent endpoint all exist. Five couplings, each with a scar: **(1)** the two non-postable forum lists agree — when only the picker knew, a hand-built request could file into a forum merely *"not offered"*; **(2)** the landing forum is **CONFIG, never a constant** — dev2 and live are different WordPress installs so the same forum has a different post ID, and comments are **stripped before** the hardcode scan (the first version read the measurement note about 73564 inside a `/* */` block as a hardcode — a false RED on a correct file costs what a miss costs); **(3)** the applier's reply loop bumps `post_modified_gmt` **and** dispatches — **a change that does not bump it never reaches the forum mirror, confirmed before for exactly this operation** (the replies of a topic moved between forums), so MySQL would look perfect while the Hub showed the old forum indefinitely; **(4)** both flag readers consult `getenv()` **and** `$_SERVER`, or a lane-preview `fastcgi_param` serves the OFF path on the very URL built for Ian to click; **(5)** the **MOBILE** composer is guarded — *"mobile is the flat form served unchanged"* is a comment that is true about `forums.js` and **false about what a phone renders**: `hub-polish.js` `fbStyleComposer()` builds its own 4-step wizard whose step 1 was *"Title & forum"*, so unguarded the removed pain point survives on the platform most members post from. Also pins the picker's `z-index` between the mobile composer sheet (2147483560) and `.lg-lightbox` (2147483646) — the @-mention dropdown in this same composer was invisible on phones at 100000 (Ian, 2026-07-23). Exits **2** for CANNOT RUN, not 3: `run-all.sh` reads anything-else-non-zero as RED, so a wrong code reports a missing environment as a finding and blocks every lane. **Red-first, 11 checks, mutating SNAPSHOT copies** (never `git checkout --`, `feedback-mutation-harness-must-snapshot-not-checkout`): 9 mutations each RED **for their own stated reason** — ungated taxonomy attach, reply loop stops bumping, lists disagree, mobile guard removed, forum id hardcoded, z-index lowered, OFF markup edited, Skip orphaned, FLAGS row deleted — and **both the baseline and a no-op whitespace mutation stay GREEN**, so it is neither always-red nor always-green | **none** — pure source + config analysis: no DB, no browser, no network, so it cannot flake under load and cannot go vacuously green behind a locked-out browser |
| 75 | `tools/gates/double-pay-block-gate.php` | **one payment source per member — nobody is charged twice for the same membership.** Ian, 2026-08-19, verbatim: *"We should disallow double payment source for the same user."* Block, not warn. Before this, a member paying $5 or $11 on Patreon **right now** could walk through checkout and pay a second time with no warning anywhere. The Stripe half of that rule already existed — `CheckoutController` refuses a second subscription with *"starting a second subscription would bill you twice"* — and had been **Patreon-blind since the day it was written**, as had lgjoin's active-sub redirect. **THREE doors, not the two the plan named.** Re-verification found a third: `POST /wp-json/lg-member-sync/v1/me/checkout-session` minted subscription sessions for logged-in members with no Patreon check at all; it is asserted here so it cannot drift back to being the unwatched one. **The verdict keys on real Patreon standing and NEVER on `payment_source`** — one slot the two rails overwrite each other in — and the gate proves that twice over: the helper's comment-stripped source may not contain the string, and mutating the field alone must move no verdict. **ONE SWITCH, three readers.** The wp_option `lgms_double_pay_block` is read by the poller with `get_option`, by the standalone membership-pages app over SQL, and by the Slim billing app **not at all** — its DB user holds `lg_membership` and nothing else, so it asks the poller at a route that only EXISTS while the flag is on. OFF is therefore a real ABSENCE at every door rather than a branch answering "no", and the ON leg beside it proves that absence was not a dead call. Reads the flag rather than hardcoding a state (`feedback-gate-reads-the-flag-not-a-hardcoded-state`). **Fail-open is asserted, not assumed**: an unknown answer lets the buyer through, because failing closed would stop every sale on a WordPress hiccup. **A gift is never refused and never even asked about** — buying for somebody else is not double-paying. ⚠️ **IT ASSERTS BY REFLECTION THAT IT IS TESTING THIS BRANCH**: `lg-stripe-billing`'s composer autoloader maps `LGSB\` into the SERVING CHECKOUT, so without that leg a lane running this on dev2 measures main and calls it verified (`trap-harness-and-serve-answer-from-main`). Source assertions run on comment-stripped tokens so prose can never satisfy one (`feedback-red-first-that-stays-green`). Three traps are asserted directly because each has cost this repo before: an **ancient `synced_at` must not unblock a patron** (it is last-CHANGED, not last-checked, so the steadiest patrons carry the oldest rows), a **NULL `patron_status` must not read as PAYING** (`trap-null-source-row-shadows-live-reader`), and the member-facing copy is compared **word for word between its two homes** rather than keyword-matched, since the join page cannot call the poller and the copy therefore exists twice. **Red-first, twelve mutations**, listed at the foot of the gate file, each applied against a file SNAPSHOT (never `git checkout --`, `feedback-mutation-harness-must-snapshot-not-checkout`) and each reddening its NAMED assertion; **two no-op mutations** (renaming a local, reflowing the message whitespace) confirmed to redden nothing | **the `/lgjoin/` HTTP leg** — it REPORTS rather than asserts while every membership-pages surface on dev2 404s at the nginx door (unrelated infra defect, attributed on issue #150). A dead surface must not pass as a green one (`trap-locked-out-browser-goes-vacuously-green`). Every asserting leg is pure: no DB, no browser, no network, so it cannot flake under load |
| 76 | `tools/gates/stripe-multi-tier-gate.php` | **the Stripe rail grants the tier that was PAID for.** Ian, 2026-08-19: *"I've decided I want to be able to have multiple tiers."* That ruling REPLACES the 8/08 one — *"move to ONE tier for the stripe memberships and have ALL tiered content open to the one tier through stripe"* — which was implemented faithfully as a constant, not carelessly. ⚠️ **THE OBVIOUS ASSERTION IS A VACUOUS GREEN, and it is the reason this gate is shaped the way it is.** `StripeLifecycle::TIER` was already the literal `'looth3'`, so *"a PRO purchase grants looth3"* **passed on the very defect** — it cannot tell a RESOLVED looth3 from a CONSTANT one. Every tier assertion here is written against the tier the constant is NOT; the one that bites is **"a LITE purchase grants looth2"** (`feedback-red-first-that-stays-green`). **The defect direction was the opposite of the expected one**: nobody was under-granted — a member buying Looth LITE at $5 was granted **Pro**. And it is not additive, because `grantMembershipFromSubscription` revokes by source and re-inserts on any ref change, so the constant **OVERWRITES** the correctly-resolved looth2 entitlement the Slim return path writes — which is why this is the change that makes the two Stripe code paths agree, the founding law of the domain (*"they both need to work together to produce a logical result"*). **OFF is proven, not asserted**: the `lgms_multi_tier` OFF leg runs against a database with **no products or prices tables**, so any price lookup on that path is a hard SQL error rather than a quiet pass — an absence assertion with a liveness assertion beside it (`feedback-absence-assertion-needs-liveness`). **An unmapped price must never RETRACT**: it falls back to the documented constant and NAMES the price in the log, because returning null would reach `applyOpinion` as a retraction — the same shape as a cancellation — and demote somebody who has just paid. The two failure modes are not symmetrical. **The fallback chain is closed to non-default tiers**: both legacy options were written when there was one tier, so letting looth2 read them would sell Looth LITE at the Pro price, silently, on a box that looked configured. **Tiers come from the CATALOGUE** — the gate adds a product mid-run and watches the dash offer it with no code change, which is Ian's 8/19 scope ruling (the dash gains pricing only, no tier-builder form). **§8 asserts the join-page consequence, not a row count**: `setPrice` retires the price it supersedes because `lgjoin`'s `renderTiers` draws one button per active recurring price with no dedup by cadence — dev2's Pro card was showing **$11/mo AND $9/mo, $132/yr AND $99/yr**, four buttons for one membership — and the retirement must NOT make a member still billing on the old price vanish from lgjoin's already-subscribed lookup, which is the double-charge shape. Both checked rather than assumed: that lookup joins `prices` with **no active filter**, and both `tierForPrice` implementations filter on the **product's** active flag, never the price row's. **Red-first**, recorded at the foot of the gate file: the LITE legs failed with `got 'looth3'`, `tiers()` did not exist, and *"a PRO subscription still grants looth3"* passed throughout — the vacuous green, caught in the act | **none** — SQLite in memory, no DB, no browser, no network, so it cannot flake under load and cannot go vacuously green behind a locked-out browser |
| 77 | `tools/gates/lanes-page-truth-gate.py` (+ `lanes-page-truth-redfirst.sh`) | **the lanes page cannot lie about a lane.** #151/#155/#156/#159/#160/#164/#172. The page's entire job is telling Ian the truth about what is running, and on 2026-08-19 it told him a lane that had been building for **25 minutes** was *'finished & freeable'* **and** *'APPROVED, NOT STARTED'* at the same time. **The obvious diagnosis — three bugs — is wrong: all three were ONE line.** `unique == 0` meant `done`, so a branch cut minutes ago read as finished, LEFT the seats table, and its issue then had no seat to be found at; the third lie was the second lie's shadow. A page that is wrong is worse than no page, because he acts on it. ⚠️ **ASSERTIONS MATCH MARKUP, NEVER PROSE** — grepping the rendered page for `APPROVED, NOT STARTED` during this very build reported a hit, and the hit was **the lane's own plan comment quoted inside a `<details>` block**: a page that RENDERS a plan mentioning a defect is not a page EXHIBITING it (`feedback-red-first-that-stays-green`). **EVERY ABSENCE IS PAIRED WITH A LIVENESS ASSERTION** (`feedback-absence-assertion-needs-liveness`) — *'the running lane is not freeable'* is trivially true if nothing is EVER freeable, so a finished seat sits in the fixture and must still be offered; *'no issue reads NOT STARTED'* is trivially true if that block never renders, so an issue with no seat anywhere sits in the fixture and must still be flagged. **The red-first run found two assertions that were decoration** and both are fixed: the parked-branch rule was never under test because a `merged` label was what actually spared that issue, and *'not flagged AT RISK'* could not fail because the fixture branch was pushed. **The tmux leg is the one that earns its keep**: the working-detector drifted TWICE in a single day — the morning the CLI dropped *'esc to interrupt'*, and the afternoon a raised thinking effort began appending `· thinking with xhigh effort` INSIDE the parens, so anchoring on `tokens\)` read every deep-thinking lane as idle **and**, because a no-match `grep` exits 1 under `set -e` + `pipefail`, killed the whole script so `lanes --json` printed NOTHING and the page failed for a reason nowhere near the pane it was reading. Also asserts **four chips and only four** (Ian's ruling; six states were rejected as wrongness surface), each carrying its **verbatim** reason; that a rider and a parked branch each COUNT as work started; that the checklist is **ABSENT** when nothing waits on him but **LOUD** when GitHub is unreadable, since *'nothing waits on you'* and *'I could not look'* must never render alike; and #156's endpoint refusals, per-seat nonce and 10-minute debounce, with `msg` shimmed so the gate never posts to the real board. **Red-first, 26 mutations**, each against a file SNAPSHOT (never `git checkout --`, `feedback-mutation-harness-must-snapshot-not-checkout`), each reddening its NAMED assertion, plus **two no-op mutations** that must stay green **#172 EXTENDED IT TO 128 CHECKS — no new number minted, and `run-all.sh` deliberately untouched** because two other lanes held that file. Ian 2026-08-20: *"the list for me isn't super useful… This is hard for me to parse or get started with."* Measured before building: **12 bullets, 11 of them the word "Try" followed by a raw issue title**, one control each. The leg guards two things nothing else can. ⚠️ **PROSE MUST NOT COUNTERFEIT A RECORD** — the new TEST-URL parser reads issue comments and commit bodies, text that *discusses* the convention as often as it uses it, and the very commit that added the feature quoted an example record in its own message: under a first-token reading that line handed **#172** a door pointing at the join page. So the whole remainder after the colon is the value and `safe_url()` rejects any value containing a space — a record is a **structured line**, and the validator does that work rather than the regex (`feedback-red-first-that-stays-green`, arriving from the *input* side). ⚠️ **THE LOUD LAYER MUST NOT BE COLLAPSIBLE** — a collapsed AT RISK is a hidden AT RISK, asserted by **walking `<details>` depth** rather than matching a string, because the plan blocks inside cards are themselves `<details>` and a nesting-blind regex reports whatever it likes; paired with its liveness half (an approved-with-no-seat issue sits in the fixture so something IS loud). The href is treated as **untrusted input** — it arrives from a comment or a commit body and lands in an anchor. **The first red-first run returned four of the new assertions UNPROVED and every one was the gate's fault, not the code's**: the poisoned-href check could not see its own poison (`rstrip` ate the closing paren, so `javascript:alert(1)` arrived as `javascript:alert(1` — still poisoned, merely unrecognisable to the assertion named after it); two checks shared a host, so `//evil.example/x` is a substring of `https://evil.example/x` and they measured each other; **one rule was UNFALSIFIABLE and is no longer claimed as proved**, replaced by the falsifiable rule at the same seam (an unattributable record is DROPPED, never guessed) with an honest note in place of the old claim — a gate that overclaims is worse than one that claims less; and one mutation opened an empty accordion beside the block it meant to swallow. **Three PRE-EXISTING mutations were repaired** in the same pass: rewriting `build_todo` invalidated their needles, and a stale needle reports UNPROVED — the same shape as a decoration assertion — so the lane had silently removed three assertions' worth of #160/#155 proof while adding its own. **Four assertions were re-aimed from WORDING to SHAPE** so a future wording pass cannot redden a gate that is about behaviour (`feedback-gate-reads-the-flag-not-a-hardcoded-state`). Red-first: **50 of 50** (47 mutations, 3 no-ops). ⚠️ **THE CLIPBOARD BUTTON IS DRIVEN FOR REAL, BUT NOT HERE** — it was clicked once during the build over CDP on loopback (clipboard **poisoned first**, so a no-op click could not pass on a stale value; hit-tested with `elementFromPoint` before dispatch; read back with `clipboard.readText()`) and **reported rather than gated**, because putting a headless Chrome in this file would buy that one check at the price of the property that makes the other 127 trustworthy | **none** — fixtures through the real renderer, a per-run tmux session and a throwaway git repo under a per-run path (`feedback-gate-probe-must-be-per-run`): no browser, no network, no DB, no writes to `/var/www` and no worktrees on the real box, so it cannot flake under load and cannot go vacuously green behind a locked-out browser |
| 79 | `tools/gates/header-join-gate.py` (+ `header-join-redfirst.sh`) | **WHO the header's Join sends to OUR join page — and can they get there.** #165, extended by **#170** to three states. ⚠️ **THE STATE THE EXTENSION EXISTS FOR IS THE ONE THAT NEARLY SHIPPED AS A NO-OP.** Ian: *"We need the join button in the header to still go to patreon unless a test user is there on live."* Measured before designing: `.lg-chrome__join` sat in the ANON branch of the aside, so a signed-in viewer got no Join pill at any width — 1 pill for anon, **0 for a member, 0 for a tester, 0 for an admin**. Implemented literally, `allowlist` would have rendered **byte-identically to `off` for every viewer**: the one control it changes did not exist for the one viewer it changes it for. That version passes every assertion about anon, about unlisted members, about hrefs and tabs and config shapes — which is why **mutation 16 ("the tester pill never renders")** is this gate's mutation 7, and why the assertion that bites is *"allowlist ACTUALLY DIFFERS from off for a tester"*. **THE CACHING LAW is asserted structurally**: `allowlist` keys on `$caps['stripe_testgroup']`, a per-viewer capability an anonymous ctx never carries, so the logged-out render cannot differ from `off` — cmp'd at 28,028 bytes, for the ABSENT config too. **#165's ratchet is NARROWED, not deleted**: *"the flag cannot reach a signed-in member in ANY state"* now holds for off/on only, and what `allowlist` may change is pinned by a real difflib diff to exactly one line ADDED and none removed (a positional compare called that inserted line 459 changes). **THE MIGRATION IS A GATED FACT**: dev2's hand-placed `.local.php` says the legacy `enabled => true` and lives in the serving checkout no lane may edit, so a tidy-up dropping the old key would revert dev2's header on the next pull with nothing in any diff to explain it — asserted against that byte-exact on-box shape. **THE SECOND COUPLING**: `on` pairs with `lgms_stripe_pages_live`, `allowlist` pairs with `lgms_stripe_testgroup_pages`, and the predicates differ — the PILL has one lock (the cohort list), the DOOR has two (flag AND list) — so a listed NON-ADMIN can be handed a pill and refused at the door while an admin passes both and notices nothing. Reported rather than asserted, because this leg is anonymous and `allowlist` is invisible to an anonymous observer, which is the caching law observed from outside. **Same-class is not evidence**: §A asserts no stylesheet scopes `.lg-chrome__join` to the anon cluster, which is what lets §D's in-browser measurement of the anon pill transfer to the tester's copy (`trap-class-name-assertion-passes-on-the-defect`). Red-first **23 of 23** (21 mutations, 2 no-ops, both no-ops now asserting their own target exists before mutating — the old no-op A had been retargeted into changing nothing at all). Ian 2026-08-20: *"can you Wire the header on Dev2 to have the stripe menuing that a logged out user would see?"* The anon header's Join went straight to patreon.com — his own 2026-06-12 ruling, and right for a Patreon-only world. ⚠️ **THE OBVIOUS ASSERTION IS NEARLY WORTHLESS, and that is the shape of this whole gate.** *"Flag ON means href=/lgjoin/"* is **TRUE of a build that hands an anonymous visitor "This page isn't available yet"** — the MEASURED state of dev2 the day this landed, because `/lgjoin/` picks its audience from a **different switch**: `router.php` lists it as `['lgjoin.php','testgroup','public']` and the wp_option **`lgms_stripe_pages_live`** chooses the column, so while that is off the PRE-LAUNCH column applies and anon gets the stub. §E is the assertion that bites: while the deployed flag is **ON** it asserts anon `/lgjoin/` is not the stub; while it is **OFF** it reports and does not assert, so it cannot redden a lane with nothing to do with it. The two switches **flip in the same window** or Join is wired perfectly and lands nowhere — presence-is-not-reachability in the one place it costs a sale. **OFF IS BYTE-PROVEN, NOT ARGUED**: the partial is rendered from the branch and from `git show origin/main:lg-shared/site-header.php` with the same anon ctx and compared **byte for byte** (28,028 both ways) — for the **ABSENT** config too, since a deploy can land code before config, and for an **AUTHED** ctx in every state **including ON**, because the flag must not be able to reach a signed-in member at all. Reads the flag rather than hardcoding a state (`feedback-gate-reads-the-flag-not-a-hardcoded-state`); source legs run on **comment-stripped tokens** via PHP's own tokenizer so prose can never satisfy one (`feedback-red-first-that-stays-green`). **THE CONTRACT IS ROUTE-AGNOSTIC**, like gate 12's: *"an anon visitor can reach Join"*, never *"this pill is visible"* — at ≤640 on the hub `bb-mirror/web/forums.css` hides the entire header aside with `display:none!important` **by design**, and the **PWA account sheet IS the anon door there**, so that sheet's Join is asserted instead, including that an internal href must **not** open a new tab (an unconditional `target="_blank"` throws a member out of the installed PWA — `display:standalone` — to buy a membership in a browser tab; that hardcode was harmless only while Join could just be patreon.com). **The state the origin cannot serve is MEASURED, not assumed**: dev2 serves main, so §D swaps the href in the live document and re-measures — equal computed style and equal hit-testability means presentation does not depend on the href, which would also catch a `[href^="http"]` rule in any *other* stylesheet that no grep of `site-header.css` could see. Reachability is asserted as a **DELTA** (*"the OTHER href is exactly as reachable as this one"*), never as an absolute, because at a width where main already hides the control the absolute answers about main and not about the flag. ⚠️ **`KNOWN_MAIN_GAPS` — 821px is REPORTED, NOT SCORED**: measured on main, the pill sits at x=845 in an 821px viewport, entirely past the right edge (`scrollWidth` 905); the nav collapses at ≤820 so **821–904 is a dead band** where the full nav and the anon cluster cannot share a row — **the same class as gate 12's 641–820 sign-in dead band, one band over**, and pre-existing, since a one-href change cannot move a layout. The allowance **self-expires**: if 821 ever passes, the gate **FAILS** telling you to delete the entry, and every other width stays a hard assertion so the list cannot grow silently. **Red-first, 12 mutations against file SNAPSHOTS** (never `git checkout --`, `feedback-mutation-harness-must-snapshot-not-checkout`), each reddening its **OWN** named assertion, plus **2 no-ops proven inert** — 14 of 14. The one to keep is mutation 7: a **pure whitespace** edit changing no behaviour at all, invisible to every other leg and caught only by the byte-identity comparison, which is precisely the defect lane 129 found in its own OFF path | **§D only** — one browser, one HTTP GET. §A–C are pure source + `php`: no DB, no network, no browser, so they cannot flake under load and cannot go vacuously green behind a locked-out browser |
| 79 | `tools/gates/header-join-gate.py` (+ `header-join-redfirst.sh`) | **a logged-out visitor's Join goes to OUR join page — and can actually get there.** #165. Ian 2026-08-20: *"can you Wire the header on Dev2 to have the stripe menuing that a logged out user would see?"* The anon header's Join went straight to patreon.com — his own 2026-06-12 ruling, and right for a Patreon-only world. ⚠️ **THE OBVIOUS ASSERTION IS NEARLY WORTHLESS, and that is the shape of this whole gate.** *"Flag ON means href=/lgjoin/"* is **TRUE of a build that hands an anonymous visitor "This page isn't available yet"** — the MEASURED state of dev2 the day this landed, because `/lgjoin/` picks its audience from a **different switch**: `router.php` lists it as `['lgjoin.php','testgroup','public']` and the wp_option **`lgms_stripe_pages_live`** chooses the column, so while that is off the PRE-LAUNCH column applies and anon gets the stub. §E is the assertion that bites: while the deployed flag is **ON** it asserts anon `/lgjoin/` is not the stub; while it is **OFF** it reports and does not assert, so it cannot redden a lane with nothing to do with it. The two switches **flip in the same window** or Join is wired perfectly and lands nowhere — presence-is-not-reachability in the one place it costs a sale. **OFF IS BYTE-PROVEN, NOT ARGUED**: the partial is rendered from the branch and from `git show origin/main:lg-shared/site-header.php` with the same anon ctx and compared **byte for byte** (28,028 both ways) — for the **ABSENT** config too, since a deploy can land code before config, and for an **AUTHED** ctx in every state **including ON**, because the flag must not be able to reach a signed-in member at all. Reads the flag rather than hardcoding a state (`feedback-gate-reads-the-flag-not-a-hardcoded-state`); source legs run on **comment-stripped tokens** via PHP's own tokenizer so prose can never satisfy one (`feedback-red-first-that-stays-green`). **THE CONTRACT IS ROUTE-AGNOSTIC**, like gate 12's: *"an anon visitor can reach Join"*, never *"this pill is visible"* — at ≤640 on the hub `bb-mirror/web/forums.css` hides the entire header aside with `display:none!important` **by design**, and the **PWA account sheet IS the anon door there**, so that sheet's Join is asserted instead, including that an internal href must **not** open a new tab (an unconditional `target="_blank"` throws a member out of the installed PWA — `display:standalone` — to buy a membership in a browser tab; that hardcode was harmless only while Join could just be patreon.com). **The state the origin cannot serve is MEASURED, not assumed**: dev2 serves main, so §D swaps the href in the live document and re-measures — equal computed style and equal hit-testability means presentation does not depend on the href, which would also catch a `[href^="http"]` rule in any *other* stylesheet that no grep of `site-header.css` could see. Reachability is asserted as a **DELTA** (*"the OTHER href is exactly as reachable as this one"*), never as an absolute, because at a width where main already hides the control the absolute answers about main and not about the flag. ⚠️ **`KNOWN_MAIN_GAPS` — 821px is REPORTED, NOT SCORED**: measured on main, the pill sits at x=845 in an 821px viewport, entirely past the right edge (`scrollWidth` 905); the nav collapses at ≤820 so **821–904 is a dead band** where the full nav and the anon cluster cannot share a row — **the same class as gate 12's 641–820 sign-in dead band, one band over**, and pre-existing, since a one-href change cannot move a layout. The allowance **self-expires**: if 821 ever passes, the gate **FAILS** telling you to delete the entry, and every other width stays a hard assertion so the list cannot grow silently. **Red-first, 12 mutations against file SNAPSHOTS** (never `git checkout --`, `feedback-mutation-harness-must-snapshot-not-checkout`), each reddening its **OWN** named assertion, plus **2 no-ops proven inert** — 14 of 14. The one to keep is mutation 7: a **pure whitespace** edit changing no behaviour at all, invisible to every other leg and caught only by the byte-identity comparison, which is precisely the defect lane 129 found in its own OFF path | **§D only** — one browser, one HTTP GET. §A–C are pure source + `php`: no DB, no network, no browser, so they cannot flake under load and cannot go vacuously green behind a locked-out browser |
| 80 | `tools/gates/front-banner-patreon-dark-gate.py` (+ `front-banner-patreon-dark-redfirst.sh`) | **the logged-out front page drops its SECOND join, and the join funnel is readable in dark.** #169 + #171, both from Ian's logged-out walk of 2026-08-20: *"The secondary join on the front page at the top banner can go away"* (there were THREE join doors above the fold at 1440 — header pill, this strip, hero button) and *"dark mode is sucking on the patreon stuff"*. ⚠️ **THE ISSUE NAMED THE WRONG CONTROL, AND THE GATE IS SHAPED AROUND THAT.** #171 names the header Connect Patreon pill in dark; measured, that pill is **11.34:1 in dark** with a ~9:1 outline and has no dark defect at all. There IS a real defect on it — its outline at **2.72:1 in LIGHT**, under WCAG 1.4.11's 3:1, where an outline IS the control because there is no fill. A gate built on the charter's hypothesis would have gone green on the phantom and missed the real thing one theme over, so **both themes are graded on every surface** (`feedback-measure-the-named-defect-before-fixing-it`). **What was actually broken was `/lgjoin`** — the page #165 wires anon Join to. `membership-pages/web/lg-shortcodes.css` had **ZERO** dark rules (measured: `data-lguser-theme` appeared 0 times), so all four Subscribe buttons rendered `#e5e7e1` on `#ffffff` = **1.25:1, invisible**. The mechanism is not the obvious one: `:root` sets `--lg-ink:#292929`, but app-settings.js sets it as an **inline style on `<html>`**, which beats a stylesheet rule on that same element — so the ink flips and every hardcoded `background:#fff` stays put. **The same defect class one stylesheet over, for the third time**: `join.css` already carries a dark block whose comment describes this exact bug, missed here because `/connect-your-patreon/` and `/lgjoin` render the same `.lg-join__*` names from **two different stylesheets** — which is why §C asserts **both** copies. **It also means gate 36 was RED on main**, not by any lane's doing: its lgjoin baseline of 0 was captured while every membership-pages surface 404'd, that infra defect was fixed 8/20, and the real page's debt appeared against a baseline recorded from a dead one — **fixed rather than absorbed, so the baseline is honoured, not raised**. **Two of the eight findings were PHANTOMS OF THE INSTRUMENT** and are recorded rather than "fixed": the AMEX and Google Pay marks carry explicit `fill=` in the markup, so what they PAINT is brand-correct (5.03:1, 6.05:1) — only the inherited `color` was near-white, and `color` is what a contrast reader sees. **Three more defects were found while in there**: `.lg-join__buy.is-selected` at **1.89:1 in LIGHT TOO** (no anon gate can see it — the class only lands after a click); `.lg-join__cta` on the Patreon page at a **1.05:1 BOUNDARY** against its own card, whose old comment said the pairing was fine because it checked the TEXT and never asked what the fill sits on; and `.lg-join__feature`/`.lg-join__tier-tagline`, which **only became defects because darkening the card exposed them** — caught by re-measuring before the commit, not by Ian after it. §B **renders the real script** rather than grepping it (a grep passes on a file that reads the flag into a variable nothing consumes) and proves OFF **byte-identical to origin/main, 72,054 both ways**, against a SNAPSHOT SIBLING and never `git checkout --`. Every absence is paired with a **liveness** assertion, and the **MEMBER greeting** sharing that `if`/`elseif` is asserted present in both states with the authed render byte-identical across them. Reads the flag rather than hardcoding a state. §D **adapts instead of assuming** — it fetches the served stylesheet, compares it to this worktree's, and says whether it is grading the box or injecting the branch's REAL FILE BYTES, because a browser gate that silently graded main would be worse than none (`trap-harness-and-serve-answer-from-main`). ⚠️ **MOTION IS KILLED AS CSS BEFORE THE PROBE WALKS**: `transition: background 0.15s` means `getComputedStyle` can return an **interpolated** mid-fade value that is nearly `#ffffff` — indistinguishable from the real defect, and the first version of this gate stopped motion *after* probing and reported four false 1.25:1 findings on a working branch. **The boundary leg caught a real regression in its own author's fix** (a `#3a4148` border at 1.56:1 against the card), which is the best evidence it asserts something. **Red-first, 11 mutations each reddening its OWN named assertion, plus 2 no-ops proven inert**, every one against a file SNAPSHOT restored by an EXIT trap | **§D only** — one browser over 12 surfaces. §A–C are pure source + `php` render: no network, no DB beyond the page's own read, so they cannot go vacuously green behind a locked-out browser |
| 82 | `tools/gates/approved-autospin-gate.py` (+ `approved-autospin-redfirst.sh`) | **APPROVAL STARTS WORK BY ITSELF — for PRE-STAGED work, and nothing else.** #138 phase B. Ian 8/20: *"If a lane goes Idle waiting for me to make a decision, I want the next work in line to start up while I'm screwing around."* ⚠️ **THE FAILURE MODE IS THE REVERSE OF THE OBVIOUS ONE.** A lane that fails to start is visible in five minutes; a watcher that starts one it should not is not. Nine parked branches on this box wear open+approved issues whose charters are still on disk, so a watcher missing its one-spin-ever backstop re-spins the lot, on two cores, at once, with nobody watching — which is why the backstop is a *branch-exists* check and not just a log the watcher writes itself. Asserts the four holds (keeper-quiet, fleet-down/reboot, load, the working cap read through lanes-status's OWN detector) and the five per-issue guards (charter required · the charter names the seat · one spin per issue ever · already-running · folder==branch, and a cut seat whose upstream IS `origin/<lane>`, never `origin/main` — 75a0fb6). **EVERY ABSENCE IS PAIRED WITH A LIVENESS**: "it did not spin" is equally true of a broken script and an empty fixture, so each refusal runs a control in the same harness where the same issue DOES spin, one condition apart. Leg 7 applies that to the script's own PRODUCTION defaults, which no other leg executes — every other leg drives it through `LG_AW_*` overrides, and a default that is wrong while the overrides work is invisible to all of them. **NO NETWORK, NO REAL SEAT, NO CLAUDE PROCESS** — issues are a fixture, the spin command is a recorder, and state/prompts/worktrees/manifest/bell/quiet redirect into a per-run temp dir; the quiet path especially, since touching the real `/tmp/keeper-quiet` would hold the whole fleet. Red-first 21/21 (19 mutations each red for their own named reason, 2 no-ops inert) — it found a cap checked twice where the second copy made the first unprovable. | git + tmux |
| 83 | `tools/gates/meta-glance-leak-gate.py` (+ `meta-glance-leak-redfirst.sh`) | **a members-only one-liner does not reach the crawler-visible head.** #166, Ian 8/20: *"Fix meta leak."* A profile's `<meta name="description">`, `og:description` and `twitter:description` carried `users.at_a_glance` **verbatim** to logged-out visitors, crawlers and link unfurls while the rendered body correctly withheld it behind the members gate — **42 members on LIVE, 28 on dev2**. ⚠️ **NOT ONE OF THE 42 HAD CHOSEN members-only**: `header_vis_explicit` is false for all of them, so every one is the untouched platform default that **1,917 of 1,933** members have never opened. The head and the body simply disagreed about what that default meant — which is what makes it a defect rather than a policy question, and why it shipped **unflagged** (the OFF state of a flag here IS the leaking state; precedent 789b480, the contrast fix). The same class lived on `/p/` with practice `tagline` **and** `about` under `PRACTICE_HEADER_DEFAULT` — **found twice, so gated**, one gate over four surfaces. ⚠️ **§B IS NOT PADDING AND IS THE REASON THIS GATE MEANS ANYTHING**: deleting all three meta tags outright satisfies the leak check perfectly, and **14 live members** have a legitimately public one-liner that is good SEO — the fix is a **CEILING, not a deletion**, and only the public-header side of the bracket can tell those apart (mutation M2 reddens **only** §B). Every absence is paired with a **liveness** assertion (`feedback-absence-assertion-needs-liveness`): ≥2 description tags **and** the generic fallback still naming the member, so *"the leak is gone"* and *"the page is gone"* cannot look the same. **§E is the head-side half of gate 39 §G7's fence** — #107's consent lets the **featured card** republish a members-only one-liner; a tick is not permission to put the line in Google. §G7 asserts the body and deliberately does not fail on the head; §E asserts the head. **§F asserts the rule is the renderer's OWN** (`Block::headerCeiling()` / `practiceHeaderCeiling()`), because a second copy of "is the header public" is precisely how these two surfaces drifted apart. ⚠️ **IT AUDITS WHAT `LG_GATE_HOST` SERVES, WHICH IS MAIN** — `/u/` and `/p/` are symlinked out of the serving checkout, so a lane must run `tools/preview/lane-preview.sh up <lane>` and set `LG_MGL_PREFIX=/preview/<lane>` or be told about main (`trap-harness-and-serve-answer-from-main`). **Proven RED on main (A, D, E), GREEN on branch (6).** **Two measurement bugs found in its own first runs, both silent**: `REPO` two dirnames deep instead of three left every §F source read returning `None` and reporting DEAD forever; and **§F matched its own warning** — the fix's comment explains at length that this is *not* the #107 exception, so it necessarily contains the words `featured_opt_in` and `consent_ack`, and a raw-file regex reported the **warning against** the defect **as** the defect (`feedback-red-first-that-stays-green`). §F now strips comments with PHP's own tokenizer — not a regex, since `u.php` contains `'https://'` a naive `//.*$` strip would eat. **Red-first + 4 mutations on file SNAPSHOTS** (never `git checkout --`): revert-the-ceiling → A/E/F, withhold-from-everyone → B, launder-the-consent → F, and a **whitespace no-op proven inert**. ⚠️ **The consent-laundering mutation was BEHAVIOURALLY INERT** — `u.php`'s `$SEL` never selects `featured_opt_in`, so only §F's static check caught it; §E's behavioural coverage comes from the revert mutation instead. A mutation that does nothing is not evidence an assertion works. | psql + one HTTP GET per surface, all loopback — no browser, so it cannot go vacuously green behind a locked-out one |
| 84 | `tools/gates/auto-ban-gate.py` (+ `auto-ban-redfirst.py`, `auto-ban-harness.php`) | **the stuffing detector bans at the LOGIN DOOR, and nothing else does.** #162. Ian 8/20: *"can we just add it to a file of known offenders and nip it at our webserver?"*, narrowed the same day to *"this should only block ips that try several different logins in one block"*. ⚠️ **THE OBVIOUS IMPLEMENTATION TAKES THE SITE OFF THE INTERNET.** Neither box restores real client IPs (no `set_real_ip_from` anywhere in `/etc/nginx`, verified 8/20), so `$remote_addr` is a **Cloudflare edge node** and a deny keyed on it bans the edge — every visitor behind it, not the attacker. The real client is in `CF-Connecting-IP`, a header anyone can forge by connecting straight to the world-knockable origin, which would let an attacker put **an address of their choosing** behind our own deny rule after five failed logins. §C and §G8 are those two facts asserted: the banned address is what the connection PROVES — the header is believed only from a Cloudflare range, and off a direct connection the forger's own peer is what gets banned. Also asserted: only the multi-distinct-account signal bans (50 attempts at ONE account does not, four accounts does not, a spray at accounts that do not exist does not); `logout`/`lostpassword`/`rp`/`resetpass` stay reachable to a blocked address and so does the rest of the site, because a door is not a wall; bans expire; the WP allowlist and the root-only allowlist are both immune; the renderer re-prints every address from a parsed object so a map-breakout string reaches nginx as nothing; the cap holds at 5,000-in; Remove and Never-ban work and fail without a nonce or the capability; **all three flag states read from the flag**; and the tracked vhost names no `$lg_ab_` variable in any directive, so a pull onto a box that never armed this cannot fail `nginx -t`. **§H drives the flip kit itself** — the only piece a human runs on LIVE by hand, and the last one to have no coverage: inside a redirected root it arms, proves nginx accepts what the INSTALLER wrote (not what the gate substituted), and asserts the two refusals `nginx -t` structurally cannot see — a vhost missing the include line (arming there installs a blocklist that blocks nobody) and a polite page not yet pulled (blocked members get a bare 404). Plus: a rejected config rolls BOTH halves out rather than leaving the pair half-installed, `--uninstall` keeps the hand-built allowlist, and §H8 proves the test hooks did not become the shipped behaviour. **Found four real defects:** an env value read as unset so the nginx test could not be switched off and every offline render rolled itself back; a generated conf carrying its own render time, whose moving bytes would have reloaded nginx **every 5 minutes forever on a box with no bans**; a double-encoded admin notice; and §C3 passing for the wrong reason over a rule that could be deleted outright. Mutation-proven: 41 breaks each reddening its own named assertion, 4 no-ops proven inert | php + curl + nginx binaries; no network, no root, no DB, no WordPress, no FPM — per-run temp dir and per-run port, so concurrent suites cannot collide |
| 85 | `tools/gates/tester-unlock-gate.py` (+ `tester-unlock-redfirst.py`) | **an anonymous browser gets patreon.com UNLESS it is carrying the unlock mark.** #180. Ian 8/21: *"dev2 join goes to stripe rather than patreon with a fresh incognito. I need it to go to patreon unless the user has some kind of token url or something to unlock the whitelisted pages."* ⚠️ **THE ASSERTION THAT BITES IS THE REFUSAL, NOT THE GRANT.** *"A browser holding the token sees /lgjoin/"* is satisfied by a build that shows `/lgjoin/` to **EVERYBODY** — which is precisely the state Ian was complaining about when he opened the issue. So the grant is ONE assertion and the refusals are SEVEN, each holding a **valid** cookie: no cookie, a WRONG cookie, and five wrong config shapes (disabled, empty hash, malformed hash, non-array, absent). ⚠️ **AND THE SECOND HALF IS EASY TO MISS: `'off'` must keep meaning NOBODY.** An unlock that quietly worked in `'off'` would pass every test written about `'allowlist'` and silently overrule #170's ruling on the state LIVE ships in — §B4. **OFF IS BYTE-PROVEN, NOT ARGUED**: 18 `cmp` comparisons against `origin/main` across 3 states × 3 viewers × 2 config shapes, and — this is the part that is easy to get wrong — **on a tree that ALREADY carries the nginx microcache change**, because an OFF state proven against a tree nobody will deploy proves nothing. ARMED with a valid cookie, **EXACTLY ONE** viewer × state may differ, asserted as a list rather than a count. **§E MEASURES THE SAFETY NET AS IT ACTUALLY IS AND REFUSES TO CLAIM MORE**: Ian's posture is *"no one can sign up unless they are white listed"*, and measured on both boxes **there is no whitelist in that path at all** — what dead-ends a marked browser is lgjoin's own JS refusing to reach checkout until the auth call returns ok, plus that route answering anon 401 (**BuddyBoss's global `bb-enable-private-rest-apis`**, re-armed by every DB reload). So §E asserts the ordering and the live refusal and **REPORTS the mechanism**, so a green §E can never be mistaken for a whitelist; §E4 records the *"Sign-in failed"* dead-end as EXPECTED so a tester does not read it as a defect. ⚠️ **§E1 PASSED ON ITS OWN DEFECT, TWICE, and that is the lesson of this gate**: *"the auth check appears before the checkout call"* stays true of code that checks the answer and **ignores** it; and the first fix — requiring a `return` between the two — was **also** vacuous, because the surrounding `catch` block has its own bare `return`, so deleting the failure branch's return left the window satisfied (`trap-class-name-assertion-passes-on-the-defect`). Now bounded to the failure block itself. **CANNOT GO VACUOUSLY GREEN**: §A–§D use no browser, no DB, no WordPress, no FPM and no network — the header is EXECUTED in isolated trees, the claim runs against php's own server on a **per-run port** (so concurrent suites cannot collide), the door runs against hermetic stubs, and every absence is paired with a **mode-aware** liveness assertion (the first version demanded a Join pill from signed-in viewers who correctly have none — #170 measured that the pill is anon-only, so it failed on correct output). **§F READS THE SERVING CHECKOUT, NOT THE WORKTREE** — the first version required the worktree's partial, whose `__DIR__` finds the TRACKED default, and reported *"this box is 'off'"* on a box keeper had set to `'allowlist'`. **Red-first 25/25** (22 mutations each reddening its OWN named assertion, 3 no-ops proven inert), and building it found: a mutation **absorbed by a duplicate guard** (the armed check exists in both `marked()` and `token_matches()`, so removing either alone changes nothing — recorded as *"the assertion is decoration"* when the MUTATION was the problem); a *"whitespace"* mutation that was **also inert**, since PHP-code indentation emits nothing (demoted to no-op N03, replaced by one stray byte in the RENDERED markup — #170's own 9-byte leak class, visible only to the byte-identity leg); and a harness that **deleted its own snapshot directory** after the first leg, so leg two had nowhere to snapshot, its restore failed, and a mutation stayed on disk while later legs were measured against an unverified tree | php only — no browser, no DB, no FPM, per-run port; §E is the one leg that touches the box and it CANNOT RUN rather than passing when it cannot reach it |
| 86 | `tools/gates/checkout-audience-gate.php` (+ `checkout-audience-redfirst.py`) | **the soft-launch cohort is real in the CHECKOUT PATH, not just at the page layer.** #181, Ian 8/21: *"Fix before go-live"*. Lane 180 measured that **nothing** in the signup or checkout path consulted the cohort; three unrelated accidents did the refusing, and the load-bearing one on live — an **empty Stripe catalogue** — is removed on purpose at go-live. Reproduced on dev2 as served, anon and cookieless, with a real price id from the **public** `/billing/v1/products` list: `POST /billing/v1/checkout` → **200 + a live Stripe `clientSecret`**. ⚠️ **THE OBVIOUS ASSERTION MEASURES NOTHING** — *"a cohort member can buy"* was true of the broken code too, because **everybody** could buy. The assertions that bite are the refusals (§B2 a real member outside the cohort, §B3 an unknown email, §C1 a pre-minted session cannot provision). Same family as #148's *"a PRO purchase grants looth3"* passing on a constant. ⚠️ **A SECOND VACUOUS GREEN, subtler**: `off` and `on` both answer yes to everyone, so any assertion phrased *"this person may buy"* passes in two of three states without distinguishing them — every permission assertion here names the state it was measured in. **THE REAL ARBITER IS NOT STUBBED**, because it is the only writer of `wp_capabilities` and therefore the only thing that could strip a comp member's **looth4** (Ian 8/21: *"looth4 is the everything bypass the stripe side of membeship needs to respect"*); §I runs it over a comp holder with no Stripe customer and asserts the role survives, **paired with a liveness leg proving the same sweep DOES demote a non-comp member** — without which §I would pass on an Arbiter that demotes nobody. **§H drives the probe over real HTTP** and asserts that **every** way WordPress can fail to answer refuses: 404, 500, the measured `bb_rest_authorization_required` 401, non-JSON, an unrecognised state, and a body with no `allowed` key. ⚠️ **404 REFUSES HERE AND PROCEEDS IN THE SIBLING PROBE**, deliberately: `/patreon-standing` uses *the flag is the registration*, so its absence IS its off state; this route is registered **unconditionally** and *answers* `off`, because a flushed rewrite or a deactivated plugin must never read as permission. **RED-FIRST 20/20 + 2 no-op controls**, and the run earned its keep twice: **§B8c was a genuine blind spot** (it compared a WordPress-supplied message against the unknown one, so the guard's own two constants could be made identical and it still passed — §B8d now drives the fallback path), and one "mutation" turned out to **change no decision at all** (`if ( ! $user )` → `if ( false )` still refuses, because `$user->ID` on a bool is null and `allowsUser(0)` is false — the code was right twice over). ⚠️ **THIS GATE'S FENCE REDDENED FOUR NEIGHBOURS AND ALL FOUR ARE FIXED IN THE SAME COMMIT** — gates 75 and 76 plus `test-identity-gate.php` and `test-checkout-session-metadata.php` all drive the WP checkout door or the provisioner, and the enforcing default 403'd their fixtures (two of them **fataled at exit 255 with no FAIL line**, the third time that file has died on an unnoticed new dependency). Each now loads the real `CheckoutAudience` and pins it `off` **at every `$GLOBALS['OPTS']` reset**, not once at module scope — a pin set once is silently gone by the first assertion. |
| 87 | `tools/gates/header-name-clamp-gate.py` (+ `header-name-clamp-redfirst.py`) | **the header's account chip is ONE LINE, and the full name survives it.** #173. Ian 8/20, signed in as *"Massimiliano Monterosso Maxmonte Guitars"*: *"Verbose names in the profile icon in the header? Maybe do a ....."* — and again 8/21 as *"Ian Davlin The Looth Group"*: *"Something changed in the header. We are stacking words that used to be inline."* ⚠️ **SECOND DISCOVERY OF THE CLASS, WHICH IS WHY IT IS A GATE**: site-header.css's own ≤820 rule already carries the comment *"the display name is the first thing to drop (it's what tips a busy admin aside into a two-line wrap)"* — that was #1, answered by hiding the name on tablets; this is the same defect at desktop widths. ⚠️ **THE OBVIOUS ASSERTION IS GREEN ON THE BROKEN STATE.** The header BAR never changed height — `.lg-chrome__inner` is `height:60px` **fixed** — so *"the header is still 60px tall"* is true of the screenshot Ian sent. What grew was the **button inside it**, 40px → 49 → 62 → 88, spilling out; the gate measures the button and the **line count**. **§D is what earns it its keep**: a 71-character name must add **no horizontal scroll over a 3-character one**. The `max-width` is derived from the logo (138.3), the nav's min-content (419.2) and the aside (269.5) — none of which a gate can pin down forever — so the **consequence** is asserted rather than the number, and a seventh nav item reddens a gate instead of silently invalidating a constant. The **#170 Join pill is the second aggravator** (worth ~76px of headroom, which is why keeper adding Ian to the cohort at 13:30 tipped a row whose name had not changed), so every cell runs **with and without it**, and **Ian's own name with the pill is asserted by itself** — the exact combination that produced the report. Fixture names are **real**, read off `wp_users` (1,933 rows, longest 71 chars); a longer one needs no new fixture, since every name wider than the cap is the same case. **Both sides of both breakpoints** (1000 and 820), both themes, 240 browser cells. ⚠️ **THE VIEWPORT META IS LOAD-BEARING**: without it Chrome lays the page out in a 980px fallback under emulation, every media query answers for 980, and the ≤820 rules never fire — which reads as *"the phone rule is gone"* on a stylesheet that is fine. Measured, not assumed. ⚠️ **§B's anon byte-identity renders BOTH sides from hermetic temp trees with normalised mtimes**, because the partial embeds an `@filemtime(social-modals.js)` cache-buster and an un-normalised copy differs by that number — a byte leak that is not one. That leg **caught a real one**: #173's first draft put the menu's CSS in `site-header.php`'s inline critical block, which is emitted on **every** render including the anonymous one, and grew the anon response by **745 bytes**. Review did not catch it; `cmp` did. **Proven RED on main (17 failures, 9 passes) and green on branch (29/0).** **Red-first 10 mutations + 2 no-ops**, on file **snapshots**, never `git checkout --`; **M10 is labelled SOURCE-ONLY** rather than counted as behavioural coverage, because the ≤820 rule is subsumed by the ≤1000 one and no browser cell can see it go — a mutation the instrument cannot see is not evidence the assertion works. | one browser over the loopback CDP port — no WordPress, no DB, no login, no FPM, no network. Opens its OWN target and closes it, never navigates to dev2, never touches cookies, and sets the theme as an attribute on its own document, so it cannot poison the shared chrome profile. Every cell asserts liveness first (a blank page satisfies *"nothing wrapped"* perfectly) and it exits **2 — CANNOT RUN**, never 0, if Chrome is down |
| 89 | `tools/gates/comp-expiry-gate.php` (+ `comp-expiry-redfirst.py`) | **a comp timer actually runs out — and the accounts whose timers were already past are HELD.** #183. Ian 8/21: *"comp timers need to work."* They had not for at least 41 days, and the cause was an absence rather than a bug: `lg-looth4-expiry 1.0.0` enforced looth4 expiry before the cutover and did not survive it. Measured both sides 8/21 — no file under live's wp-content, absent from `active_plugins`, `recently_activated` empty, no cron event in the 13,182-byte `cron` option, no ACF field (there is no `_looth4_expires_at` companion row), no snippet, no option naming the key. Two live timers lapsed in July with nothing watching, and — the half that is easy to miss — **nothing could SET a comp end-date either**, so the fix ships the Comp Timers tab alongside the enforcement. ⚠️ **THE TWO HALVES MUST PASS TOGETHER.** §E runs a REAL armed sweep over the two genuinely overdue accounts (1829 `2026-07-28`, 1865 `2026-07-11`) and requires their roles byte-identical with **no role operation even attempted** — Ian ruled they are left alone until he decides case by case — while §F requires a timer at or after the cutover to actually demote. Either section alone is satisfied by a sweep broken in one direction, which is why they are asserted as a pair in one run. ⚠️ **THE ASSERTION THAT MEASURES NOTHING** is named in the header so nobody re-adds it as evidence: *"an unexpired comp keeps looth4"* passed on the broken code, because before this every comp kept looth4 forever. Same family as #148's *"a PRO purchase grants looth3"*, which passed on a constant. **The timezone is asserted against a HOSTILE process zone**: the gate sets PHP's default to `America/New_York` — what both boxes actually run — so a reader that dropped its explicit UTC zone reddens §B. The values *are* UTC on two independent proofs (the old plugin's own source note, and both live rows' minute-of-day matching their UTC registration), which means the previous site-zone read placed every expiry **four hours late**. §G keeps `Arbiter::sync` the only writer of `wp_capabilities` by asserting that `CompExpiry` and the admin screen contain no `add_role`/`remove_role`/`set_role` at all — the split the old plugin lacked, and the reason it is not being resurrected. Reads the flag per state rather than hardcoding one. Red-first 32/32 (30 mutations + 2 no-op controls); **three holes it found in this gate are recorded in the commit** — the flag-off cases were masked by the cutover, no case reached the stripe coexistence guard, and "UTC appears somewhere" passed on the table header after the visible field label was removed. |

## Both themes from birth (keeper, 8/16)

Dark contrast has now bitten on FIVE surfaces (sign-in, search fields, the
compose modal, the work board, the profile setup step) — each shipped with a
gate that asserted content and behaviour but only one theme. THE RULE: a new
member-facing surface enrolls dark-mode contrast assertions in its OWN gate on
the day the gate is born — AA against the actual rendered background, both
themes, both widths. "Looks fine in light" is half a verdict.
