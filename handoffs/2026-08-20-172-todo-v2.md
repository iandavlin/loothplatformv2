# 172-todo-v2 — BUILD DONE

Branch `172-todo-v2`. Issue **#172**, labels `approved` `page`.
Worktree `~/worktrees/172-todo-v2`, left in place.

## What Ian asked for, and what he now has

Both asks verbatim on the issue, 8/20: *"the list for me isn't super useful. Can
we get links and copy and paste so I can talk to you about them?… This is hard
for me to parse or get started with."* and *"I'd also like to collapse the
sections into accordions so I can get a snapshot easier."*

**Measured before designing** (feedback-measure-the-named-defect): fetched the
live page through the dev gate. **12 bullets, 11 of them the word "Try" followed
by a raw issue title**, one control each (`on GitHub`), no door, no starter.

Now, from the real render:

    📱 Look at the join page — three tiers, and check the prices
       merged; your look is the last thing left.
       the lane said: "merged to main as c879589, dev2 flag ON pending Ian's…"
       [ Do it ↗ ] /lgjoin/     [ Copy for keeper ]
       say: "148 good" · "148 not right"                    #148 on GitHub ↗

and the whole page closed is seven lines:

    Your list — 12 items          (open)
    Agents — 2 agents · 2 working
    Seats — 4 seats
    Old desks — 2 desks
    Parked — branch kept, seat freed — 9 branches
    Cleanup — 3 merged branches deletable
    Landed on main, last 7 days — 20 merges in 7 days

## The TEST-URL convention — NEW, and keeper owns it from here

**It did not exist.** Grepped every branch and every commit body on main before
building: zero hits. So this lane defined it, in one comment above `build_todo`
in `tools/lanes-page.py`, and it is documented in `docs/domains/PAGE.md`.

A record is one line a lane or keeper already writes:

    TEST-URL: /lgjoin/            TEST-URL #148: /lgjoin/
    ACTION #148: Look at the join page — three tiers and their prices

Read first-hit-wins from: the issue's **comments** then its **body** (live — a
correction lands on the next 5-minute redraw with nothing to merge) → **commit
bodies on main** (deliberately *not* `--first-parent`, so a lane can write the
record at build time and not only keeper at merge) → the **park reason**.

**⚠ KEEPER: this is the thing to carry forward.** One `TEST-URL:` line in a
merge body and that bullet gets a working door, with no page edit ever. An
`ACTION:` line beside it and the bullet gets Ian's own words instead of a verb
plus a title.

### Seeded — four doors, each verified against SOURCE

Two of the four answer an anonymous request with a 404/302 **by design**, so a
naive 200-check reports them broken (trap: verify the thing, not the thing next
to it):

| issue | door | why the status code lies |
|---|---|---|
| #148 | `/lgjoin/` | 200 anon, nothing to explain |
| #129 | `/hub/?compose=1` | 200; `bottom-nav.js` reads `?compose=1` to open it |
| #93 | `/compose/?type=loothprint` | 404 anon **by design** — `lg-frontend-compose.php` returns early for a logged-out visitor. The path is `compose`, the type is the query arg. `/compose/loothprint/` is **not** the composer; it is the CPT archive and 301s to `/loothprint/` |
| #107 | `/wp-admin/admin.php?page=lg-featured-member` | 302 anon is the wp-admin login bounce; slug from `FeaturedMemberDash.php:39` |

### NOT seeded, and deliberately — 8 items keeper should record

I know these only by their titles, and a confidently wrong action name is worse
than an honest generic verb. Each renders `Look at <plainised title>` /
`Say GO to switch on <…>` plus *"no test link yet — ask keeper for one"*:

**#170** header Join · **#150** double-pay block · **#149** dual holders ·
**#104** mail-containment · **#88** logged-out bottom dash · **#87** recap
mark-all-read · **#84** mobile back-nav · **#81** SEO/sitemap

## Scratch render vs the live page, same data, same minute

|  | live (main — what he sees now) | this branch |
|---|---|---|
| bullets | 13 | 12 (one moved to the quiet line) |
| Do-it doors | **0** | **4** |
| reply lines | **0** | **12** |
| `on GitHub` as the card's control | 13 | **0** |
| `on GitHub` as fine print | 0 | **13** |
| accordions | **0** | **7** |
| sections shown | 5 | 7 (Cleanup and Landed became sections) |

Nothing was written to `/var/www/dev` to produce this: the branch column is a
scratch-dir render of the same `lanes --json` and the same GitHub state.

## Four defects found by the work, not by a fixture

1. **The list led with a sentence nobody said.** `main()` upgrades a seat to
   *needs-you* when its issue wears `merged`/`built` and writes the reason
   itself; the checklist's first family — which exists for *a lane that raised
   its hand and named him* — could not tell that from a real hand-raise. It
   printed `Answer 138-phase-b — it asked: "merged — waiting on your check"`.
   Two defects from one line: a sentence attributed to a lane that never said
   it, **and** the issue was swallowed before the merged/built families ran, so
   #138 — the single item Ian's ruling sends to the quiet line — never reached
   it. The upgrade is now tagged at its source; **the seat card is unchanged**
   (that state and reason are #159's design), only the checklist looks past it.

2. **A park reason is ONE line**, so the line-start rule made that whole source
   unusable — a lane writes *"merged as c879589, dev2 flag ON pending Ian's
   join-page look"* and has nowhere to put a record. The record may now ride at
   the **end** of a park-reason line, and there only. The two rules differ
   because their sources differ, not for convenience. **Gate 77 found this**, not
   a reader.

3. **The accordion marker was a Python octal escape.** `content:"\25B8"` is read
   by Python before the browser ever sees it, and `\25` is octal there — so every
   closed section wore `chr(21)` followed by the literal text `B8`. **Only
   looking at the rendered page could catch it**; every markup assertion passed
   on it. A literal `▸` now.

4. **The verb was still swallowing a raw title.** The first cut wrote
   `Look at ` + the title, and eight of twelve bullets came out as *"Look at
   Checkout is Patreon-blind: a live Patreon member can…"* — Ian's own complaint
   wearing a verb. The instruction is now complete **before** an em dash and the
   title is a label after it (`Take a look — <title>`, `Say GO to switch it
   on — <title>`), a form that cannot come out ungrammatical whatever the title
   is. Also caught by looking, not by testing.

## Ian's rulings taken in-lane (8/20)

- **No-Ian-action items get one quiet line, not a disappearance.** The rule is
  `merged` + `infra` + NOT `built`; today it matches exactly **#138** and
  nothing else. He was offered both and chose the quiet line: a wrong quiet line
  is recoverable, a wrong disappearance is not.
- **No preview** — merge it and he looks at the real page.

## Verification

- **Gate 77 EXTENDED to 127 checks** (was 80). **No new number minted**, and
  therefore **`tools/gates/run-all.sh` was not touched** — it is held right now
  by `169-front-polish` and `emoji-picker-build` and a comment-only conflict is
  a needless one.
- **Red-first: 22 new mutations + 3 no-ops**, each reddening its own named
  assertion, all against snapshots and never `git checkout --`. **The first run
  came back with four of my own assertions unproved and every one was the gate's
  fault, not the code's** — the poisoned-href check could not see its own poison
  (`rstrip` was eating the closing paren, so `javascript:alert(1)` arrived as
  `javascript:alert(1` — still poisoned, merely unrecognisable to the assertion
  named after it); two checks shared a host, so `//evil.example/x` is a substring
  of `https://evil.example/x` and they measured each other; one rule was
  **unfalsifiable** and is no longer claimed as proved; and one mutation opened
  an empty accordion beside the block it meant to swallow.
- **Three PRE-EXISTING mutations were repaired.** Rewriting `build_todo` and the
  section rendering invalidated the needles for *"the Building strip comes
  back"*, *"the list renders even when nothing waits on him"* and *"a question
  stops being mirrored as a bullet"*. A stale needle reports UNPROVED — the same
  shape as a decoration assertion — so this lane had silently removed three
  assertions' worth of #160/#155 proof while adding its own.
- **⚠ The harness's own exit code was swallowed by a `| sed` at the call site**:
  it printed *7 unproved* and *exited with code 0*, because a pipeline reports
  the last command's status. Read the harness's own tally, never the pipe's.
- **The clipboard button was driven for real** (charter's ask), not asserted as
  markup: CDP over loopback (a secure context, so no dev2, no gate cookie, no
  injected app chrome), clipboard permission granted, the clipboard **poisoned
  first** so a no-op click could not pass on a stale value, hit-tested with
  `elementFromPoint` before dispatching, then read back with
  `clipboard.readText()`. It copied
  `Re #170 Look at LIVE header Join… — [170 good / 170 not right]` exactly.
  The accordion state was driven the same way and survives a reload.
  **Reported, not gated** — gate 77 stays browser-free so it cannot flake under
  load or go vacuously green behind a locked-out session.
- **Nothing was written to `/var/www/dev`.** Every render went to a scratch dir.
- No deploy coupling changes: `lanes-page.service` passes no arguments, so
  `--repo`/`--history-ref` keep their defaults. Keeper's clone pulling is the
  whole deploy, as before.

## Noticed, not fixed

- **A truncated title reaches the clipboard.** For an un-recorded item the
  action is `Look at <title plainised to 62 chars>`, so the copied text can end
  in an ellipsis: `Re #170 Look at LIVE header Join: Patreon for the world, our
  join page for… — [170 good / 170 not right]`. It still identifies the issue by
  number and is a direct argument for writing `ACTION:` records. Left alone
  rather than special-cased.
- **`In motion (no seat)` is an accordion but had no fixture** — no
  `investigating` issue existed during this lane, on the box or in gate 77's
  fixtures. Its count line is untested.
- The 821–904px header dead band and the three anon Patreon join CTAs (#165) are
  still open; unrelated to this lane, repeated from 170's handoff.

## Branch state

- **6 behind / 9 ahead of `origin/main`.** Main moved under the lane
  (`672fac1`, #169 + #171). **Not rebased**, per LANE-RULES.
- **`git merge-tree` says it merges into current main with NO conflicts.**
  It did not at first: main's #171 note appends to the very `PAGE.md` paragraph
  this lane rewrote. The lane's history line was moved *above* main's anchor line
  and that line restored byte-for-byte. Verified read-only — no rebase, no
  checkout, nothing force-pushed.

## Files touched

    tools/lanes-page.py                        records, cards, accordions, --repo/--history-ref
    tools/gates/lanes-page-truth-gate.py       gate 77 leg [6]  (no new number)
    tools/gates/lanes-page-truth-redfirst.sh   22 mutations + 3 no-ops
    docs/domains/PAGE.md                       the convention + the card + the fifth family
    handoffs/plans/172-todo-v2-PLAN.md         the approved plan
    handoffs/2026-08-20-172-todo-v2.md         this file

Exactly the set declared in the plan. `run-all.sh` and `lanes-status.sh` were
declared as *possible* and neither was touched.
