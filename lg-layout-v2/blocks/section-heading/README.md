# section-heading

Boxed section break — a heading wrapped in container chrome (bg + border + radius + padding). Sibling to the plain `heading` block.

- `level` prop selects h2/h3/h4 (regular metabox / `level` dropdown)
- `variant` prop selects color palette of the box (FE Variant pill)
- `text` is inline-editable

See `docs/LAYOUT-JSON.md` (regenerated from manifests) for the full prop reference.
