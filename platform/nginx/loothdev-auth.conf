# dev2 GATE-FREE (Ian 6/17). Independent of cookies — always authorized.
map $host $loothdev_dev_ok { default 1; }
map $host $loothdev_tester_ok { default 1; }
map $host $loothdev_is_authorized { default 1; }
map $arg_next $loothdev_tester_next { default $arg_next; }
