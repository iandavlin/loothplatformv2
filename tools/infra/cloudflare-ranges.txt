# Cloudflare edge ranges — the ONE source of truth for this box's CF trust boundary.
#
# THREE consumers read this file and they must never disagree:
#   1. tools/infra/install-auto-ban.sh   — generates the nginx `geo` vouching block
#   2. tools/infra/lg-auto-ban-render.py — refuses to ever ban a CF address
#   3. platform/mu-plugins/lg-auto-ban.php — decides whether CF-Connecting-IP is trustworthy
#
# WHY IT IS ONE FILE. If the PHP half and the nginx half disagree about what
# "arrived through Cloudflare" means, the trust boundary silently stops matching
# itself: WordPress bans the address in one column while nginx enforces against
# another, and the feature reads as working while blocking nobody (or the wrong
# body). One file, three readers, no drift.
#
# Snapshot fetched 2026-08-20 from https://www.cloudflare.com/ips-v4 and ips-v6.
# Cloudflare changes these rarely and announces it. To refresh:
#   curl -s https://www.cloudflare.com/ips-v4 https://www.cloudflare.com/ips-v6
# then re-run install-auto-ban.sh so the generated geo block is rebuilt too — a
# refreshed file that nobody regenerates from is a file that changed nothing.
#
# Format: one CIDR per line. `#` comments and blank lines ignored by every reader.
# --- IPv4 ---
173.245.48.0/20
103.21.244.0/22
103.22.200.0/22
103.31.4.0/22
141.101.64.0/18
108.162.192.0/18
190.93.240.0/20
188.114.96.0/20
197.234.240.0/22
198.41.128.0/17
162.158.0.0/15
104.16.0.0/13
104.24.0.0/14
172.64.0.0/13
131.0.72.0/22
# --- IPv6 ---
2400:cb00::/32
2606:4700::/32
2803:f800::/32
2405:b500::/32
2405:8100::/32
2a06:98c0::/29
2c0f:f248::/32
